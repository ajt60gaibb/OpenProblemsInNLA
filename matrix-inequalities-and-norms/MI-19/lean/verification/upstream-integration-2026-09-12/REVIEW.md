# MI-19: upstream integration review, 12 September 2026

**PASS.** Merged upstream main `02b807770fca860ef810cc048d6849224b8f93e1` into the published proof branch. Infrastructure PR #153 is now upstream, together with the GitHub mathematics-formatting changes from #152. The merge required no manual conflict resolution. This record reviews the integration; the existing Linux archive remains the evidence for the unchanged mathematical proof.

The nine frozen mathematical, Challenge, Comparator and toolchain/dependency inputs are byte-identical to the actually verified proof commit `cd44ce9bcb84ebc79a1aa934918d1f76b2a9c6e7`. Every previously published file under this project's `lean/` directory is unchanged; only this integration evidence directory was added. Original raw verification logs and artifact hashes remain untouched. Full hashes are in [integration-checks.json](integration-checks.json).

The maintainer's canonical-page changes equal exactly the new formatter applied to the common-base page. The final merged page equals exactly that same formatter applied to our entire premerge published page. Thus the original target, assumptions and quantifiers, our Lean-verified status, original mathematical attribution, George Stepaniants's Caltech Department of Computing and Mathematical Sciences formalization credit, scope qualifications and verification links are retained. No email was added. All 217 ID/path mappings agree with current upstream. Only this problem's formal-verification status differs from main; no status from another pending proof PR was imported.

The actual `harness.py`, immutable `source-lock.json`, bootstrap/selftest/verify wrappers, manifest validator, harness tests and CI checker toolchain files match the successful infrastructure revision `214c142d6bfe0f0c338808f188062acbbad0fb19` byte for byte. This integration does not change Comparator, exporter, Landrun, sandbox adapter, derived probe, kernel replay or permitted axioms. The executable infrastructure changes from that revision are confined to the project selector and the workflow's changed-path parser. The selector now compares registered projects against the base tree and rejects removed or renamed projects; changed-path handling uses NUL delimiters and disables rename detection. Maintainer documentation now expressly states that read-only host mounts do not provide host-file confidentiality and calls for isolated, credential-free runners. These are preserved, not overridden. The exact new selector, workflow, formatter, renderer and Lua filter are retained in `source/`.

Validation passed: metadata and exact export coverage; permanent-ID checks against both `origin/main` and current upstream; required catalog generation; all 17 ID tests for this branch; and the all-entry math-format check. A single actual run of the common upstream suite passed all **77 tests**, including new selection and renderer regressions, plus all **12 harness tests**, with no skips. Those shared test bytes are identical across the three integrated proof branches; their raw logs are copied here with that scope explicit.

The canonical TeX and PDF were regenerated by the new upstream renderer and GitHub-math Lua filter. XeLaTeX reported no overflow or missing-character warnings. All **3 pages** were rendered and visually inspected; the original formulas, norm bars, set notation and matrix row separators remain legible. [Rendered-file hashes and visual review](rendered-files.json) bind the final outputs. Our publication changes pass whitespace checks and no merge markers remain. The full staged upstream merge reports pre-existing Markdown hard breaks and raw-archive EOF whitespace; each affected file is byte-identical to upstream and was deliberately preserved (see `whitespace-scope.json`). A new remote CI run may be triggered by the push; this record does not claim that an unobserved run has passed.

Reviewer: OpenAI Codex agent `/root/formal_review_standards`. This is an integration and document review, not a replacement mathematical peer review or a new local Linux execution.
