# MI27 pencil-integral bridge — independent pre-body referee B

**Verdict: APPROVE all six exact helper headers for implementation.** This review covers `development/MI27-C11-referee-a-v1/PENCIL-INTEGRAL-STATEMENTS.md`, SHA256 `babdeb1319fe505a514c3edfccba6be2e9c7233722986b2f3305701e43fb66f4`. No mathematical, measure-order, boundary, normalization or circularity blocker was found.

Reviewer: `/root/nr04_mf14_final_referee_b`, independent nonauthor AI agent, 19 September 2026. This is a bounded pre-body mathematical/statement review. I inspected the exact plan, the previously stated general improper shift-kernel interface, and relevant pinned API signatures. I authored/edited no Lean proof, invoked no Lean/Lake/Comparator, and changed no publication or completion count.

| Exact header | Assessment |
|---|---|
| `c11_posDef_normalizer` | Correct for every finite dimension, including n=0. Positive-definite P admits its invertible Hermitian inverse square root S, with SᴴPS=I. |
| `c11_negativeCount_affine_identity_spectral` | Correct for every real t and Hermitian C, including t=0, singular affine pencils, and repeated or zero eigenvalues. The strict count is the indicated finite sum. |
| `c11_normalized_pencil_count_integral` | Correct: I+C>0 places every eigenvalue c in c>−1. The scalar integral applies separately to all eigenvalues, and finite summation gives both absolute integrability and the stated trace-log/resolvent value. |
| `c11_shifted_pencil_count_integral` | Correct at every fixed r≥0, including r=0. Congruence by a normalizer turns the shifted pencil into I+tC and preserves its strict negative count; the trace-kernel bridge supplies the integral value. |
| `c11_pencil_count_integrable_prod` | Correct with the displayed product measure and coordinate order. Its finiteness can be established from nonnegative measurable sections and the independent improper shift-kernel integrability, before using Fubini. |
| `c11_relative_entropy_pencil_integral` | Correct with both the explicit `Integrable` conjunct and the correction `relEntropy X Y - trR (X - Y)`. It is a general positive-definite formula, not merely a trace-one specialization. |

For the normalizer, the pinned matrix API supplies `PosDef.isStrictlyPositive`. The CFC negative-half power remains strictly positive, hence Hermitian and a unit, and `CFC.conjugate_rpow_neg_one_half` supplies S P S=I. Hermiticity converts the first S to Sᴴ. Thus positive semidefiniteness alone is not being substituted for invertibility. The n=0 header is mathematically valid: the matrix algebra is a singleton, its identity is a unit, and S=I normalizes its only matrix. If a chosen implementation API requires nontriviality, the empty case should be handled directly rather than strengthening this header.

The affine spectral statement uses the same eigenbasis as C: each eigenvalue becomes 1+t cᵢ. At t=0 every such value is 1 and the count is zero. At a crossing where 1+t cᵢ=0, the strict indicator excludes that coordinate exactly, including repeated crossings. A finite-spectrum CFC composition is legitimate even though the step function is not globally continuous. It must use continuity restricted to the finite spectrum; no globally continuous step-function premise is needed.

For the normalized integral, cᵢ=0 contributes zero. If cᵢ>0, its strict support is t<−1/cᵢ<0; if −1<cᵢ<0, its support is t>−1/cᵢ>1. Consequently each scalar contribution is supported away from the singularities at t=0,1. The cᵢ=−1 boundary is excluded by I+C>0. Totalized values w(0)=w(1)=0 alone would not prove integrability near the singularities; the actual support and the established scalar-tail integrability do. Only finitely many scalar contributions are summed.

For the shifted pencil, P=X+rI and Q=Y+rI remain positive definite for r≥0. With C=Sᴴ(Y−X)S, direct algebra gives I+C=SᴴQS>0 and Sᴴ(X+t(Y−X)+rI)S=I+tC. This step uses arbitrary Hermitian-pencil congruence, including singular pencils. It preserves negative counts, not the individual eigenvalues or the negative-part trace. No commutativity, simultaneous eigenbasis, distinct-eigenvalue or invertible-pencil assumption is introduced.

The product integrability statement has the correct orientation: z.1 is r with measure μ=volume restricted to (0,∞), and z.2 is t with measure ν=volume on all of ℝ. The earlier joint-count measurability is in (t,r) order, so the stated coordinate swap is necessary and sufficient. The literal rational weight is measurable under its totalized real definition. Both measures are sigma-finite, and measurable real functions give the required almost-everywhere strong measurability.

For G(r,t)=w(t) count(X+t(Y−X)+rI), nonnegativity gives ‖G‖=G. For μ-almost every r, one has r>0, hence an integrable t-section by the preceding fixed-r helper. The pinned `integrable_prod_iff` then asks for integrability of r↦∫‖G(r,t)‖dt. This equals the shift kernel on the restricted measure. The previously stated `c11_shiftKernel_integral_Ioi` already supplies its integrability for general positive-definite X,Y, together with its independently derived entropy-minus-trace value. The plan is therefore not assuming product integrability or the desired entropy-pencil equality in order to prove either one.

After product integrability is established, `Integrable.integral_prod_right` supplies integrability of the inner-r function over all t, and `integral_integral_swap` gives the equality of orders. For every fixed t, the unshifted pencil M=X+t(Y−X) is Hermitian, so the strict-count layer cake yields ∫ᵣ₍₀,∞₎ count(M+rI)=tracePos(−M), including zero and repeated eigenvalues. Multiplication by the fixed finite scalar w(t) gives the desired integrable outer function. The opposite order is the independently established shift-kernel integral. This is an ordinary absolutely integrable Bochner identity, not a principal value or an equality relying on a default value for a nonintegrable integral.

The correction sign is correct: the boundary term at infinite identity shift is trR(X−Y), so the integral is D(X‖Y)−trR(X−Y). Trace-one inputs make that correction zero only after specialization. Omitting it for arbitrary positive-definite X,Y would be false. The X=Y case gives zero count/negative-part integrands and zero corrected entropy, as required.

Pinned API signatures were checked at Mathlib `0df444a360eaa60ab8c11dca51a86af692955474`, including the strict-positivity matrix bridge, negative-half-power conjugation, strict-positive real powers, product integrability criterion, integrated-section integrability and Fubini swap. No compiler success is claimed by this statement review.

The six conclusions contain no extra entropy-integral premise and no new definition that packages the target equality. They still require proof bodies and actual source-matched verification. Even after they are proved, the exact hockey-stick substitutions, trace normalization and supplied finite cutoff remain obligations for frozen C11. Full C11/MI27 is not declared complete; count increment is zero.
