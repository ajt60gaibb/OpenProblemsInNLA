# MI-27 final independent source and local-evidence review

**Verdict: APPROVE** the complete original-target formalization in the immutable MI27-v1 packet, its authenticated actual local execution evidence, and the separately frozen publication metadata described below. I found no blocking mathematical, statement-fidelity, proof, reuse, attribution, or metadata issue. This is a final source-review approval, not a claim that the still-unrun GitHub Linux Comparator, external kernel, or sandbox checks passed.

Reviewer: `/root/nr04_mf14_final_referee_b`, an independent AI agent. Date: 19 September 2026. I authored none of the MI27 definitions, statements, helper proofs, implementation modules, or repairs. Earlier work on MI27 was independent pre-body review and source inspection. Root and `/root/nr04_mf14_final_referee_a` authored code and are excluded from this independent referee role. I did not rely on their claimed PASS, their proof maps, or another final referee's verdict. I ran only read-only source/provenance/metadata audits and wrote private review artifacts; I ran no Lean, Lake, Comparator, or Actions job and changed no source packet or public worktree.

The governing source is the permanent canonical `matrix-inequalities-and-norms/MI-27/README.md` at base `71563f17926cd826a892c2bba0e294894ee57a5c`, together with the complete retained Holden Markdown and TeX proof. The exact reviewed packet is `final-review-packets/MI27-v1/REVIEW-SNAPSHOT.json`, SHA-256 `d9a8bb1b4cf6e228d6c211c57f58cfd79e710f3961dffe65cb06db6c39249790`. Its 147 listed files were authenticated individually. All 52 implementation modules were inspected: 44 MI27 modules, seven reused MI24 modules, and the aggregate `Solution.lean`. The isolated Challenge is additional. The 51 dependency modules are byte-identical to the copies already fully read during my stable and final-assembly inspections; the aggregate was then read independently. The exact hashes, source-to-contract map, import closure, local command provenance, and supplemental copies are retained with this report.

I applied the frozen repository review protocol and the pinned Tau Ceti rubrics at `afb424eda89e8ac96d9eb69f6a88972055a4cd1b`, covering scope, correctness, proof quality, generalization, reuse, API design, naming, placement, documentation, and attribution. This is an adaptation of that guidance, not an official Tau Ceti service run or human peer review. The retained Schiffer/Forsythe examples were inspected as statement/workflow patterns; neither supplies imported mathematical facts.

**Original target and concrete semantics.** The final declaration `NLA.MI27.logarithmic_commutator_bound` quantifies over every natural dimension with `1 ≤ n` and every pair of complex positive definite matrices with the literal complex trace of their sum equal to one. It proves the full trace norm of `B * logM (A + B) - logM (A + B) * B` is at most `-trR A * Real.log (trR A) - trR B * Real.log (trR B)`. The coefficient is exactly one. There is no added commutativity, dimension upper bound, rank, spectral separation, conditioning, derivative, entropy identity, or existence-of-witness hypothesis. The manuscript's separate order-two optimality construction and ancillary projection corollaries are not advertised as formalized; that is consistent with the unchanged original upper-bound target.

`Mat n` is the actual complex square matrix space. `trR` is the real part of ordinary trace; `logM` is `CFC.log`; the positive part is continuous functional calculus of `max t 0`; the trace norm is the trace of the one-half power of `Xᴴ * X`; and the operator norm is that of the actual Euclidean continuous linear map. Entropy and relative entropy use ordinary matrix products and this logarithm. The unitary flow is the real-parameter matrix exponential, conjugation is actual multiplication by the flow and its adjoint, and the hockey-stick quantity is the trace of the actual positive part. These definitions do not contain the desired conclusion or a substitute scalar model. All twenty implementation headers match the frozen Challenge, and `definition_names` is empty: there is no replaceable-definition escape hatch.

**Contracts C01–C10.** Kernel-mode LeanCert establishes only the exact fixed positivity `0 < 1/2`; that result is consumed in the positive-commutator argument, not attached as a disconnected certificate. Spectral semantics use a genuine Hermitian eigenvector unitary and count all eigenvalues with multiplicity. Normalization derives the positive scalar trace weights and strict densities from the original assumptions, including positivity of the dimension. The positive trace/operator bound comes from actual matrix order and trace positivity. The effect transformation `2Q - 1`, its contraction bound, and cyclic trace manipulations give the dimension-free positive-commutator constant one.

The positive-part variational formula supplies an attained spectral projection, excluding zero eigenvalues without excluding singular matrices. The dimension factor in C07 is used only for continuity, and disappears from the final estimate. The unitary flow's exponential derivative, adjoint/product rule, invariants, and matrix order are proved with their actual noncommutative order. C09 uses a fixed endpoint optimizer and the scalar mean-value theorem, followed by the reverse endpoint inequality. It does not differentiate a moving spectral projection at an eigenvalue crossing, and introduces no unwanted factor of the hockey-stick parameter. The other ordering follows by unitary invariance and reversing flow time.

C10 obtains positive lower spectral bounds from strict positive definiteness and uses `M = ‖ρ‖ + ‖σ‖ + 1`, followed by `R = 1 + M/ε`. This establishes one cutoff for every unitary conjugate and proves both exact zero tails and continuity on the finite interval. The numerical plan's earlier suggested simpler cutoff is not the route implemented, but the frozen existential contract is unchanged. The dated STATE and supplemental metadata explicitly explain this valid divergence and supersede historical draft comments.

**C11 and the noncommuting entropy identity.** I inspected the full dependency chain, rather than accepting a cited entropy formula. Identity-shift trace expansions use two generally different eigenbases and squared overlap weights with proved row and column marginals. They do not assume simultaneous diagonalization. Scalar logarithm estimates establish the primitive's endpoint values, including the necessary trace correction at infinity, and integrability is established from the nonnegative derivative and finite limit.

The negative count is the trace of the strict-negative spectral projection. Zero eigenvalues are excluded, and multiplicity is retained. The layer-cake formula and measurable difference-quotient construction justify the count and its use under integration, including singular pencils. The rectangular negative-weight argument proves injectivity of a concrete finite-dimensional linear map by contradiction between a strict negative sum and a nonnegative sum. Its dimension bound yields congruence monotonicity; equality uses a genuine unit/inverse premise. Empty coordinate spaces, zero weights, singular congruence maps in the one-sided result, and repeated eigenvalues are accounted for.

The normalizer is the actual CFC inverse square root of a positive definite shift and is proved a unit, including dimension zero in the helper statement where appropriate. The normalized perturbation has every eigenvalue strictly greater than minus one. Trace-log identities pass through positive determinants and determinant quotients; the proof does not assert the generally false matrix logarithm congruence identity. The inverse-congruence identity has its required unit premise. The scalar integral treats zero, positive, and negative eigenvalues separately and covers the entire range greater than minus one. The derivative and reflected negative-tail sign agree with the stated weight and strict crossing count.

For Fubini, the source proves joint measurability, section integrability, and full product integrability first, using nonnegativity to identify the norm integral. Its coordinate and measure order is the nonnegative shift parameter with restricted Lebesgue measure, then the real pencil parameter with Lebesgue measure. The resulting general identity is for `D(X || Y) - trR(X - Y)`. The trace correction is removed only after specializing to equally normalized densities. Thus the entropy integral is derived, not hidden in an assumed integrability or boundary assertion.

On the central pencil interval, positive semidefiniteness makes the negative count vanish, including the boundary points. The two tail substitutions establish the actual bijections, derivatives, absolute Jacobians, transformed matrix coefficients, homogeneity, and integrability. They yield the forward hockey-stick term divided by the parameter and the reverse term divided by its square. Exact zero tails then give the frozen finite-interval identity. The degenerate interval `R = 1` remains included.

**C12–C16 and the derivative.** The algebraic weighted-entropy identity is proved for arbitrary matrices by trace linearity. Positivity and unit trace of the mixture are proved separately. The mixture-relative-entropy terms use the same original cutoff `R`, then truncate at precisely `L = R / (b + aR)` and `U = a + bR` before substitution. Both endpoints lie in `[1,R]`, and the required matrix-order tails follow from the stated pair of bounds. The derivatives and forward/reverse coefficients agree with the two rational kernels after weighting and swapping the states. No common-cutoff premise is silently replaced, and `R = 1` makes both transformed intervals degenerate.

C13 and C14 use symbolic primitives and finite-interval FTC, with positive denominators and the required logarithm inequalities. There is no numerical quadrature. They prove nonnegativity and the binary-entropy upper bounds on the two kernel masses. C15 applies C10 once, uses C12 with identical finite intervals at the two flow times, and integrates C09 against the nonnegative kernels. This gives the all-real-time entropy-change estimate without differentiating an improper integral.

For C16, the source proves trace-entropy tangent inequalities using actual overlap weights between two eigenbases, then controls the remainder by continuity of CFC logarithm on self-adjoint units. Along the positive definite unitary mixture path this gives the stated derivative, with the correct `-Complex.I` commutator sign after cyclicity of trace. Repeated eigenvalues require no special exclusion. Neither a scalar-only logarithm derivative nor an unproved Fréchet derivative of matrix logarithm is substituted for the claimed noncommuting trace derivative.

**C17–C20 and the final assembly.** Multiplication of the skew commutator by minus the imaginary unit yields a Hermitian matrix with the identical Gram matrix and hence the identical concrete trace norm. The sign functional calculus uses value zero at zero and provides a Hermitian contraction. Its product with the matrix is the actual absolute value; positivity and square-root uniqueness identify its trace with the Gram-square-root trace norm. No division by the commutator norm occurs, so the zero and singular cases are covered.

C19 normalizes the original pair internally, applies the all-time entropy bound and actual derivative, and bounds that derivative by the Lipschitz constant. Binary entropy is independently nonnegative. C20 combines the spectral-sign witness, dual estimate, and operator-norm bound to obtain exactly the original statement. The final theorem has no extra substantive assumptions. The stable-scope approval is therefore extended to the complete target, not merely to its helpers.

**Reuse, API, and computation.** I inspected the pinned Mathlib CFC, Hermitian spectral, exponential, one-dimensional Jacobian, Bochner product-integrability, and improper-integral APIs relevant to the delicate steps. Their actual hypotheses are supplied in the source. Existing real quadratic-form inertia APIs did not directly discharge this complex Hermitian pencil argument; the explicit finite-dimensional rectangular proof is appropriate and remains reusable. Seven MI24 support modules are byte-identical to their accepted canonical-base versions, which the supplemental audit checks directly with Git blob reads. Their authorship is preserved, and their reuse does not count as a new MI24 completion. Subject-specific modules separate the spectral, scalar integral, congruence, entropy, and assembly work. Fixed contract names and concrete bridges are appropriate to this project. No custom axiom, implementation placeholder, unsafe declaration, native evaluation shortcut, or imported Challenge was found in the implementation closure.

**Actual local evidence, independently authenticated.** `SOURCE-AND-LOCAL-AUDIT.json` records my read-only verification, not a new compiler run. I inspected the serial runner itself and authenticated its hash, the actual compiler hash, the local environment, all nine recorded dependency-package paths and clean tracked source trees at their locked revisions, 41 lossless original receipt archives, and all 689 reuse edges for the reviewed closure. Every one of the 52 current source hashes and output hashes follows through exact dependency closures to a recorded successful fresh local command. Each source/log/receipt/archive/output binding was checked against the actual retained files. The reviewed fresh-command intervals do not overlap. Old receipts can contain unrelated failed work; only the exact source-matched successful commands supporting this closure are approved here.

The actual recovery-118 aggregate completed 55 modules: the 52-module proof closure, the isolated Challenge, and two type-diagnostic modules. Its fresh `Solution` invocation took approximately 19.19 seconds, with `--threads=1 --memory=4096`. The aggregate's twenty actual transitive axiom reports contain exactly `propext`, `Classical.choice`, and `Quot.sound`; its twenty `#assert_trust kernel` checks are present. The twenty intentional Challenge placeholders remain solely in the separate reference environment. The diagnostic source files differ only in their first import (`Solution` versus `Challenge`). Their 1,296,589-byte logs are byte-identical, including all twenty raw elaborated types and universe lists. This is strong local preflight evidence, not Comparator or external kernel/sandbox evidence.

The root evidence audited is `verification/MI27-local-20260919/LOCAL-REPLAY-AUDIT.json`, SHA-256 `17230c39a125c0f67931aa3259679ff7fa6e14b90541c353fe99cb3dd938a390`, and `verification/MI27-type-preflight-118/AUDIT.json`, SHA-256 `8ebb6afdb40ed9c83abad156d5e6fd522bddfcb000da71181afd34af1cc620db`. The raw type-log digest is `680cfb9b070f49d58f7a27292a73080d6099648b60ba838c86ba64903234a4fd`. Actual local execution used Lean 4.33.1, Mathlib `0df444a360eaa60ab8c11dca51a86af692955474`, and LeanCert `621a43d7cf21f87872392a01e874f2f1dbddc926`. No standalone `lake build Solution` or GitHub Linux verification is asserted by this report.

**Supplemental metadata and the sole configuration delta.** I reviewed all six files in `development/MI27-publication-metadata-v1` and retained exact private copies. The JSON-compatible `formalization.yaml` validates against the frozen v0.4 schema. Its twenty contract entries equal the implementation map and the independently audited modules. It accurately identifies the full original upper-bound scope, the unformalized separate optimality result, the C10 alternative, actual local results, and pending final-review/Linux/publication gates. Counts remain zero for this still-unpromoted verification. Credit preserves Holden's mathematical authorship and George Stepaniants's Department of Computing and Mathematical Sciences, California Institute of Technology affiliation, together with relevant original and library authors. No email-shaped string occurs in these six files.

The immutable snapshot inherited top-level Lake manifest name `NLANR04` although its lakefile says `NLAMI27`. I approve the explicitly recorded publication correction in `PUBLICATION-CONFIG-DELTA.json`, SHA-256 `0b405fd5db695ce98ae1210073e2e80a28973edb96db8d208117b62894769971`: after JSON parsing, the only change is that top-level name. Every dependency entry and pin is identical. Before/after file digests are authenticated in `SUPPLEMENTAL-METADATA-AUDIT.json`. This correction neither changes proof bytes nor invalidates this source verdict. Later truthful insertion of completed review reports and real run evidence remains a separate publication step.

After the metadata review, root supplied `PUBLICATION-DOCUMENTATION-DELTA.json`, SHA-256 `8a09b465f2f178e357cba4fde53a238894af6594573c5426ed6bb43a7f7fa061`. I read it and its pattern-provenance record. Omitting the unrelated Schiffer source excerpt from publication while retaining its pinned reference/hash, and retaining the Forsythe license, changes no mathematical dependency. I directly compared all 53 publication Lean files, including Challenge, to the immutable snapshot and found exact equality. This documentation delta does not alter the source approval; its separate license-provenance addendum remains another reviewer's scope.

There are no unresolved source-review blockers. Promotion and completed-verification counting still require the other wholly nonauthor final review and successful real non-root Linux Comparator/kernel/sandbox verification of the exact published inputs. Those external gates were unrun at this review and are not inferred from local proof success.
