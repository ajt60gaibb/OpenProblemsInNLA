# MI-27 final independent source review

Reviewer: `/root/new_math_nr01`, 19 September 2026. Verdict: **PASS for the full original coefficient-one upper bound, at the exact source snapshot below.** No correctness or target-fidelity blocker was found. This is one nonauthor AI source review and an independent audit of retained local execution evidence. It is not a human referee report, a new compiler run, a Linux Comparator run, or approval to increment a completed-verification count before the remaining gates.

The reviewer authored no MI-27 proof source. Earlier involvement consisted of an independent pre-body review of the rectangular negative-weight/inertia statements. All 44 MI-27 source modules, all seven imported MI-24 modules, `Solution.lean`, the isolated `Challenge.lean`, and the Comparator configuration were read in full. The preparatory MI-27 read-set hashes were checked against the immutable packet; the seven MI-24 bodies and remaining wrapper files also match the audited packet. This review made no proof edits and started no Lean, Lake, LeanCert, external kernel, sandbox, or Comparator process.

## Exact scope

The immutable packet is `final-review-packets/MI27-v1`, whose `REVIEW-SNAPSHOT.json` has SHA256 `d9a8bb1b4cf6e228d6c211c57f58cfd79e710f3961dffe65cb06db6c39249790`. Its 147 listed files were authenticated. The independent evidence audit is `EVIDENCE-AUDIT.json` beside this report; `verify_evidence.py` is the actual executable audit source. The report manifest binds their bytes and the separately reviewed publication metadata.

The unchanged canonical target is the MI-27 page at upstream revision `71563f17926cd826a892c2bba0e294894ee57a5c`: for every positive dimension and every pair of complex positive definite matrices with `tr(A+B)=1`,

`||B log(A+B) - log(A+B) B||_1 <= -tr(A) log(tr(A)) - tr(B) log(tr(B))`.

The exported theorem `NLA.MI27.logarithmic_commutator_bound` covers that entire target. `Mat n` is the actual finite complex matrix type; `logM` is the natural CFC logarithm; `traceNorm` is the real trace of the positive square root of `Xᴴ X`. There is no half-trace-norm convention, substituted integral definition of entropy, commutativity assumption, simple-spectrum restriction, condition-number restriction, or unproved entropy identity supplied as a final premise. The density normalization and positivity of the scalar traces are derived from the canonical assumptions. The seven MI-24 modules are dependencies, not an extra solved target.

The full retained Holden mathematical solution in Markdown and the canonical page were read. Holden's separate order-two optimality construction and other consequences are outside the advertised formal target; this review does not certify the entire manuscript. The original inequality needs the upper bound, not that supplementary construction.

## Mathematical review

The twenty frozen contracts form a sufficient chain, and their supplied proof bodies discharge the previously substantive intermediate obligations internally.

* C01–C07 use actual spectral calculus and Euclidean operator norm. The exact positivity of one half from kernel-mode LeanCert is consumed in the centered-effect commutator estimate. The spectral projection realizing the positive-part variational maximum is zero on the kernel; singular intermediate Hermitian matrices and repeated eigenvalues are retained. A dimension factor in the regularity estimate is not propagated into the final coefficient.
* C08–C10 use the genuine `exp(itH)` unitary trajectory. Exponential differentiation is along scalar multiples of one fixed matrix, where the commutation requirement is valid. The hockey-stick flow estimate fixes an endpoint optimizer and uses a scalar mean-value bound; it does not differentiate a discontinuous spectral projection. Moving the second argument by inverse unitary conjugation cancels the otherwise dangerous gamma contribution. A single positive-definiteness cutoff works for every unitary conjugate. The implemented `M = ||rho|| + ||sigma|| + 1`, `R = 1 + M/epsilon` construction differs from an early suggested formula, but proves exactly the frozen existential contract without a new hypothesis.
* C11 is proved, not cited as an axiom. The two fixed spectral bases and overlap weights give the scalar identity-shift formulas. The improper endpoint limit includes the correct `tr(X-Y)` correction for arbitrary positive definite matrices; only density normalization later cancels it. Nonnegative derivative integration establishes the required improper integrability. The congruence argument proves negative inertia using injective projection to negative coordinate spaces; the rectangular cardinality lemma includes zero dimensions, the inequality allows singular congruences, and equality requires an actual inverse. The logarithmic trace congruence is justified through determinants, not a false noncommutative matrix-log rule. Scalar pencil integrals include zero and both signs of the spectral parameter. Negative-count measurability is obtained from finite-spectrum difference quotients, not a continuous eigenbasis. Product integrability precedes Fubini. The Möbius substitutions prove their maps, inverses/Jacobians, and integrability requirements, yielding the ordinary Umegaki identity and its finite cutoff form.
* C12–C15 truncate each mixture contribution at its valid transformed cutoff before changing variables. Positive-part homogeneity and the algebraic entropy weights do not assume commuting matrices. The kernel primitives and mass estimate are symbolic, include the degenerate interval `R=1`, and introduce no numerical quadrature. The same uniform cutoff is used at both trajectory times; finite differences are bounded before taking the scalar derivative.
* C16 derives the actual entropy derivative independently. The trace tangent inequality uses fixed spectral overlap weights and the scalar logarithm inequality. Continuity of the CFC logarithm on positive definite inputs and a little-o product argument establish differentiability of the trace expression. It does not apply a scalar logarithm derivative to noncommuting matrices or impose simple eigenvalues.
* C17–C20 identify the skew rotation's unchanged Gram matrix, prove the Hermitian trace norm by positive-square-root uniqueness, and use the spectral sign as a contractive dual witness. The sign is zero on zero eigenvalues. The zero-commutator case requires no division by a norm. The finite-time entropy estimate bounds the derivative, and the witness removes the dual parameter to give coefficient exactly one.

No target-changing custom interpretation was found in the definitions. The twenty deliberate proof holes in `Challenge.lean` are reference statements only. The actual proof import closure does not import Challenge. Scanning the proof closure found no `sorry`, `admit`, new `axiom`, `native_decide`, `unsafe`, or implementation substitution.

## Actual retained-execution evidence

The independent script was executed successfully with Python only. It authenticated 41 losslessly archived original local receipts, recursively traced every one of the 52 proof modules to a fresh successful actual local compilation, checked source and transitive dependency bindings throughout reuse chains, and checked current output hashes. Each relevant recorded fresh command has exit code zero, the pinned compiler hash, one thread and a 4096 MiB limit; its retained log hash matches. The audit does not mislabel all historic failed development runs as successful.

The source-matched aggregate is actual `recovery-118`, with 55 closure/diagnostic modules. Its `Solution` command completed successfully and its twenty actual transitive axiom reports contain only `propext`, `Classical.choice`, and `Quot.sound`. The code also contains the twenty corresponding kernel trust assertions. The external local replay audit has SHA256 `17230c39a125c0f67931aa3259679ff7fa6e14b90541c353fe99cb3dd938a390`.

The separate-environment type preflight has SHA256 `8ebb6afdb40ed9c83abad156d5e6fd522bddfcb000da71181afd34af1cc620db`. Both actual diagnostic commands, sources, dependency outputs and logs were authenticated. Their twenty raw type representations and universe lists are byte-identical. This is useful diagnostic evidence; it is **not Comparator**, a kernel replay, or a sandbox test.

The reviewer has authenticated saved evidence rather than independently rerunning the compiler. The direct serial macOS Lean development commands are the actual tested method; a standalone `lake build Solution` was not run by this reviewer or established by these receipts. Real non-root Linux Comparator, external kernel and sandbox checks on the exact published source remain required. No GitHub run is claimed here.

## Requested review standards and publication metadata

The retained Tau Ceti correctness, generality, proof-quality, reuse, attribution and reviewing guidance were read and applied manually to this standalone archive. This is not an official Tau Ceti CLI/service run or endorsement. Adversarial attention was given to concrete meanings, original quantifiers, circular helper premises, discarded exceptional cases, and misuse of integral or noncommutative calculus results. Full-target generality passes. Small private trace-map and cutoff helper duplications are optional maintainability improvements, not correctness defects; no claim of Mathlib upstream API readiness is made.

A bounded search of the pinned Mathlib matrix/analysis APIs confirmed the use of existing square-root uniqueness, inverse-half conjugation, CFC-log continuity, improper-integral, one-dimensional Jacobian, and injective-dimension results. No existing full entropy/hockey-stick or inertia replacement was located in the inspected areas. This is a bounded reuse audit, not an exhaustive search of every library declaration. The archived Schiffer Challenge and Forsythe Challenge/numerical-target documents were read for statement separation and precise scope patterns; no theorem from either project is silently imported.

Publication metadata in `development/MI27-publication-metadata-v1` was read separately. The JSON-form YAML validates against the retained exact v0.4 schema with Python `jsonschema` 4.26.0. Its twenty declarations agree with the frozen contract list. It distinguishes the actual local proof, diagnostic-only type match, pending independent reviews, unrun Linux Comparator and unpublished state. Its zero `sorry` count concerns the proof environment; it explicitly discloses the isolated Challenge placeholders. The reviewed metadata keeps `whole_problem_verified=false` and count change zero until final gates.

The README and metadata credit George Stepaniants, Department of Computing and Mathematical Sciences, California Institute of Technology, for the formalization, with substantial AI assistance. Mathematical authorship remains with Sidney Holden, Center for Computational Biology, Flatiron Institute, Simons Foundation; original problem and library/source authors remain credited. No contact email occurs in the reviewed metadata. No human review or source-author endorsement is claimed.

The only declared publication configuration delta is the top-level Lake manifest package name `NLANR04` to `NLAMI27`; an independent JSON comparison confirms every dependency entry is identical. The after-manifest SHA256 is `40b2066ba8b1302e6f72e635069265d872adcb70e701a97cc10b83fb10b93f55`; the delta record SHA256 is `0b405fd5db695ce98ae1210073e2e80a28973edb96db8d208117b62894769971`. This does not alter any reviewed mathematical source. `STATE.md` from the immutable packet explicitly supersedes the preserved historical draft comments; it must remain with the published proof.

## Handoff

The full original mathematical upper bound and these exact proof sources pass this independent final review. The publication metadata is accurate for its recorded pre-publication state. Its pending fields may be updated to the actual later outcomes with retained evidence. A second wholly nonauthor final review and successful exact-source Linux checks are separate gates; this report does not pre-approve either. No problem status, completed count, branch, commit, or publication was changed by this reviewer.
