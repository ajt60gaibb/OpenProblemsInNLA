# IV-04 Lean formalization

The complete approved target is recorded in `SPEC.md` and the independent
statement interface is in `Challenge.lean`. `Solution.lean` currently proves
only the exact corner-coordinate identity for a 2-by-2 specialization. The
full interval-complexity theorem is not claimed verified.
