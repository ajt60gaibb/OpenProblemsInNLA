# Independent mathematical audit: PR141 / TR-01

PR head: `2c7655f234bbb3b3134133ebae34eb479e9469e1` in `/private/tmp/nla-pr141`.
Immutable proof revision: `ed21181197ac839eac95f549404f94e7e3aa6e10`, independently cloned into `/private/tmp/nla-tr01-proof` and checked out detached.
Manuscript: `manuscript.tex`, all 3331 lines read. SHA-256 `fdfae5f25863860cf55ee8439c1f2f6dbcd6b3768585b83889d242943fafebb5`.

**Verdict: PASS for the mathematical manuscript and complete canonical-target correspondence. No substantive mathematical blocker found.** This verdict independently assesses the argument; submitted review reports were not treated as proof. The separate agent `audit_tensors` owns Lean/kernel verification and whether the public “Lean verified” claim has been reproduced. The packaging reviewer owns the canonical PDF and has separately identified a routine frame/subspace notation correction in resolution prose.

## Exact target correspondence

The canonical Problem statement is byte-identical to origin/main `7a149f0fbd80a24404c249170d31d88542125b4c` (statement SHA-256 `09626bec495ec3391c801dbfbae97fd17ede8c863880878d4ac33a73eedbd2c8`). TR-01 retains its ID, path, and original target.

Theorem 1 (`manuscript.tex:112`) proves the same width k=min(n,ceil(Cr/epsilon^2)), using two independent iid Rademacher diagonals and two normalized real Walsh matrices, followed by an independent uniform k-subset, sampled without replacement. Matrix orientation is correct: Omega=sqrt(n/k)D1 H D2 H S, whereas X=H D2 H D1 V, so its sampled Gram is exactly V^T Omega Omega^T V=(n/k)X^T E_J X (`:92–110`). Here the manuscript V is an orthonormal frame for an arbitrary deterministic r-dimensional subspace. The operator-norm Gram event is equivalent to simultaneous squared-norm preservation on that subspace.

The supremum over frames lies outside the probability (`:121–152`), so the claim is for each fixed subspace rather than one draw working simultaneously on every subspace. The universal C is independent of n,r,epsilon,V. The range epsilon<1 contains canonical epsilon<1/2. The proof covers n=1, full sampling, arbitrary intermediate sampling fractions, bounded ranks, and large ranks, at the prescribed width itself. It makes no post hoc choice of a smaller width. This is a full affirmative mathematical resolution, stronger in epsilon than required.

Primary workshop source checked directly: Amsel et al., arXiv:2602.05394v3, Definition 5.3 gives the same two-round law, while Problem 5.6 asks for width of order O(r/epsilon^2). The manuscript's distinction between that formulation and the precise canonical width is accurate. The workshop condition n=Omega(log r) adds no restriction when 1<=r<=n. Source: https://arxiv.org/html/2602.05394v3 .

## Analytic proof audit

1. **Signed trace to both spectral edges (`:434–668`).** The two-projection decomposition is constructed including repeated eigenvalues, endpoint eigenspaces, and the entire complementary kernel. Each two-dimensional block of A=(P-delta I)(E-theta I) has characteristic polynomial z^2-tz+a^2 with t=lambda-delta-theta+2delta theta and a^2=delta(1-delta)theta(1-theta). Nonreal conjugate roots contribute at least -2a^(2p); scalar blocks contribute nonnegative even powers. There are at most r two-dimensional blocks. If |lambda-theta|>theta eta and delta<=theta eta^2/64, then |t|>63theta eta/64 and a<=theta eta/8, forcing a real root of magnitude greater than theta eta/2. The endpoint cases also satisfy the needed strict bound. Thus the compensated trace inequality is valid pointwise despite A being nonsymmetric and its even trace possibly negative. It is legitimate to use an absolute value outside the trace expectation.

2. **Graph contraction and entry cumulants (`:830–1651`).** Factoring a distinguished projection edge P0=VV^T through dimension r and splitting its degree-two vertex with an identity edge produces two distinct dimension-r boundary vertices in a connected even-degree graph. Such a graph has no bridges. The input-output conversion preserves the signed contraction and norms, and its boundary operator has norm at most one. Summing its two boundary labels gives an absolute bound r. This uses rectangular compatible coordinate spaces and preserves cancellation; no absolute value is wrongly moved inside the graph sum.

   The Leonov–Shiryaev product identity is proved by finite moment/partition algebra. Nonzero sign blocks use one coordinate from one independent layer and have even size. Connected joins therefore give connected positive-even-degree graphs with normalized H and projection edges. Block labels may coincide across distinct cumulant blocks; no erroneous injectivity is introduced there. The bound `(2q)^(12q) r` for cumulants of nP entries follows with the stated constants. Walsh translation/sign symmetry is valid for the same fixed V and proves the necessary endpoint XOR condition. Averaging both independent layers gives E P=(r/n)I. Centering removes all singleton entry cumulants, including diagonal ones.

3. **Equality-graph count (`:1673–2253`).** A selector equality block visited m_u times has degree 2m_u, so the quotient graph is connected with every degree at least four and total degree excess 4s. At unmarked degree-four vertices the only local possibilities are internal doubled paths, a doubled edge plus a loop, or a four-edge terminal. This yields the loop bound ell<=4t+12s+2. Compressing maximal internal doubled paths retains O(s+t) vertices/half-edges, including returning links and links parallel to other edges. The half-edge/port/length code is surjective onto possible graph shapes; auxiliary vertex names need no additional permutation factor because local transition pairings plus a starting directed edge recover the original numbered position partition. The explicit exponent 72(s+t), traversal factor 3^p L^(2s), and special s=t=0 doubled-cycle/path cases justify N<=8p 3^p L^(74(s+t)).

4. **Binary constraints and cumulant partition count (`:2254–2622`).** Injectivity is kept until forbidden adjacent nonparallel pairs and loop/nonloop pairs are removed. Their XOR equation would equate distinct labels. Remaining nonzero pair rows have four distinct endpoints. Large blocks contain at most 6d occurrences and touch at most 12d vertices. Every other odd-incident vertex belongs to a disjoint-pair row; a basis of these rows uses at most h rows and covers at most 4h coordinates. Hence t<=12d+4h. The homogeneous binary system admits n^(v-h) unrestricted assignments, so relaxing injectivity afterward is safe in an absolute upper bound.

   The large-block, disjoint-pair, loop-pair, and parallel-pair encodings cover every surviving occurrence partition and give the stated exponent 84d+12h+9s+2t+1. In particular disjoint-pair occurrences are controlled by degree mass on a support of at most 4h vertices; remaining high parallel multiplicities are controlled by the original excess 4s. The dimension factor is exactly `(delta theta)^p kappa^(-s) r^(-d) n^(-h)`.

5. **Trace summation and Bernoulli tail (`:690–803`, `:2631–2731`).** Combining the two counts gives exponents 83s+76t+84d+12h. Summing t<=12d+4h gives 83s+996d+316h, all at most 1000 per loss. Since kappa,n>=r and r>=2(4p+1)^1000, three geometric sums each have ratio at most 1/2. The residual 128p(4p+1)<=64^p for p>=2 yields K0=576*4^24. The selector estimate |E w^b|<=theta is valid for every density 0<theta<1, including densities above 1/2. The compensated trace estimate then gives the Bernoulli failure bound at moment order ceil(log2(3000r)) and effective width at least 64K0*r/eta^2.

6. **Prescribed-width completion (`:2750–3141`).** The common uniform-order-statistic coupling gives a genuinely uniform k-subset independent of X. On count success it is sandwiched between Bernoulli samples of densities (1+-epsilon/4)k/n. Only their marginal Gram probabilities are required. When the upper density reaches or exceeds one, the deterministic bound G<=n/k I controls that edge, including equality; no invalid Bernoulli parameter is used. The count tails and squared distortion endpoints give failure <=2gamma+2exp(-epsilon^2 k/48).

   The small-rank Frobenius second moment is correctly derived from exact fixed-subset inclusion probabilities and a Rademacher fourth moment: E||G-I||_F^2<=r(r+1)/k. For R0=2^20000 and C=200R0+8196K0, ranks below R0 meet the required failure bound at the same k. Above R0 the logarithmic moment order satisfies the enormous but finite rank cutoff by the displayed monotone-log calculation. C supplies every Bernoulli effective-width requirement, and the final numerical failure is strictly below 0.01. Full sampling is exactly isometric. All constants are universal; their size affects practicality, not validity of the existence statement.

## Primary graph-source verification

The core external graph result was checked in Mingo–Speicher's primary arXiv manuscript, https://arxiv.org/pdf/0909.4277 . Definition 8 allows different finite-dimensional coordinate spaces and rectangular edge operators. Theorem 11(1) bounds the input-output operator norm by the product of edge norms. Proposition 13 preserves graph sums under the relevant modifications, and Lemma 15 converts a two-edge-connected graph using arbitrary distinct specified boundaries. This is the same conversion cited as Lemma 16 in the manuscript's journal-version references; numbering differs in the older arXiv version. The source permits the hypotheses and dimension-r boundary construction used here. Publisher record: https://doi.org/10.1016/j.jfa.2011.12.010 . The other essential cumulant, two-projection, and sampling facts are proved directly in the manuscript rather than requiring unexamined external hypotheses.

## Independent computational corroboration

No submitted verification code was executed for this mathematical audit.

- `pr141-independent-check.py/.json`: exhaustive enumeration of 18,482 equality partitions for p=2,...,5; 512,922 joint equality/cumulant partition pairs for p=2,...,4; 142,704 surviving binary-rank inequality checks; 3,000 exact rational two-projection block checks, including 461 negative even traces; explicit scalar constant checks. PASS.
- `pr141-walsh-check.py/.json`: all 256 two-layer sign draws at n=4 for a rational orthonormal rank-two frame; exact mean and 192 XOR-forced zero covariances; 1,024 conditional without-replacement sampling identities over all widths and subsets; resulting averaged Frobenius bound. PASS.

These are finite sanity checks of sensitive identities and counts. The mathematical verdict rests on the full analytic argument, not extrapolation from these finite cases. This audit makes no independent formal-kernel certification or novelty claim.
