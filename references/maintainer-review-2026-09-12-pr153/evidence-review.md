# PR 153: documentation, provenance, and Linux evidence review

**Verdict: PASS for the retained Linux checker evidence, immutable source identity, documented review gates, and reviewed documentation repairs.** The parent has qualified the general attribution instruction and added the accurate host-confidentiality limitation. No blocking documentation/evidence finding remains in this scope. This report is not blanket approval of the harness or workflow implementation, which have separate reviewers. It does not certify an NLA mathematical theorem.

Frozen PR head: `eaf3b80698555171804bef29e3a9e2b1607bc875` in `/private/tmp/nla-pr153`. Tested upstream commit: `214c142d6bfe0f0c338808f188062acbbad0fb19`. Published branch merge base: `5adea969c17391693978ada2674d25bb5c3daeb1`; fetched main when review began: `d7fcfe7bfe8c02cd98d9463fa5a8ba12850d5bdb`. Reviewer: Codex agent `/root/audit_inequalities`, 2026-09-12. I did not implement the submitted checker and did not use its AI review verdicts as proof of correctness. All my writes are scratch reports and evidence; no PR or remote mutation was performed.

## Live identity and retained-byte checks

I queried GitHub's live artifacts and jobs APIs and downloaded the decoded log for checker job `103577715893`, independently of the submitted review prose. GitHub confirms [run 34702904248](https://github.com/sgstepaniants/OpenProblemsInNLA/actions/runs/34702904248), the tested commit, a successful `select` job, a successful executed `checker-controls` job, and a skipped `verify` job. The build/control step is successful rather than skipped. The artifact's workflow-run identity names the same repository, branch, and commit.

The retained original `lean-checker-controls.zip` is 8,089 bytes and hashes to `d063d6562d710b66b90e8ff88d0efdb5b5d2806d2948537428ae3f246a74a01e`, exactly matching the digest currently returned by GitHub for artifact `10300566765`. All 10 non-directory ZIP members are byte-identical to the retained extracted files. All 35 entries in `EVIDENCE-MANIFEST.json` match both recorded SHA-256 and byte count; the manifest itself hashes to `124dcd81a75fa9622bedf210343406e81d00b0edaca289c980a44b7de4ca9065`.

All 692 live checker-job message lines match the checker portion of the retained `run.log` after removing the `gh run view` job/step labels, removing fractional timestamps, and normalizing literal ANSI escape bytes to the retained printable `^[` representation. Raw log byte identity is not claimed: the individual-job endpoint and retained run-log representation have different fractional timestamps and escape serialization. The artifact digest comparison above is exact byte identity and does not need these normalizations.

The 12 retained NLA source snapshots all match the tested commit. At frozen PR head, the executable harness, workflow, schema, tests, source lock, and other pre-existing files are unchanged from the tested commit. Its only existing-file difference is the appended operational-audit paragraph in `docs/lean/README.md`; all other changes since the tested commit add the evidence bundle. Thus the retained run applies to the frozen executable implementation, while it must not be represented as a fresh run of later integration repairs.

The [live job](https://github.com/sgstepaniants/OpenProblemsInNLA/actions/runs/34702904248/job/103577715893), immutable tested source, artifact hash, per-file hashes, and retained source snapshots establish more than a green badge. I did not download or independently rebuild the Linux binary executables: their recorded hashes identify what that run reports using, not a separate binary reproducibility experiment by this reviewer.

## What the raw controls establish

I read the raw sandbox, kernel, Comparator, sorry, native, and result logs, rather than accepting `OPERATIONAL-REVIEW.md` or generated verification JSON as the authority.

- Both build and export sandbox modes finish successfully as UID 1001. The logs record six private namespaces, inability to reach the host process/listener, denied AF_UNIX creation, empty effective capabilities, and `no_new_privs`. Direct outer writes, truncations, read-only truncation opens, `.lake` symlink writes to an outer file, and outer creation are denied. The build positive control writes its designated `.lake` fixture; export writes there are denied. Final fixture integrity is explicitly checked.
- The nested Bubblewrap process starts, then fails at UID-map setup. The inner attempted write is not reached. `OPERATIONAL-REVIEW.md` accurately retains this limitation rather than claiming an executed inner write was denied. Four malformed adapter invocations return the expected exit 2. These are finite tests on that host, not proof against every possible operating-system exploit.
- Raw replay accepts the honest inductive/quotient fixture, rejects the invalid raw proof of `False` by type mismatch, and rejects the altered `Quot.lift` after primitive regeneration. The intermediate kernel-acceptance line in the quotient case is followed by the intended post-check rejection; the report accurately distinguishes those phases.
- Five full Comparator fixtures show both Challenge/Solution builds and exports. The valid match succeeds; constant-kind, forbidden custom-axiom, and actual theorem-type mismatches fail at the advertised phases. The misleading historical fixture name `simple_kind_mismatch` is correctly described as another forbidden-axiom case in the operational review.
- The two added negative fixtures build and export, then reject `sorryAx` and the actual generated native axiom `checked._native.native_decide.ax_1_1`. Their expected exit 1 is treated as a successful rejection test, not as verification of those candidate proofs.

The receipt explicitly records `mathematical_verification: none; checker fixtures only`. Its derived CI sandbox source hash, `31057195baf238807cacbb4126c5b07f02cec55a4e4437de5f3a755b3fada803`, matches the retained derived source bytes. The original source and CI-derived source are separately retained. The harness reviewer owns whether the derivation and checker logic enforce every intended property; this review establishes accurate evidence identity and reporting.

**The per-problem matrix was empty and skipped.** The run does not execute a real NLA project's dependency/cache preparation, mathematical exports, theorem correspondence, or catalog promotion. It does not establish operational success for an arbitrary Mathlib/LeanCert project. The README's final paragraph and operational review's scope section state this accurately. Each theorem still requires a fresh run and independent source-to-statement review.

## Source lock and attribution

The source lock hashes to `b3833b07916e5db77579b9cc53ca582282f6a841f36d6a60d693e5b02d342b6b`, matching the live artifact receipt. I independently rehashed all 58 files downloaded by the separate harness auditor from [Forsythe commit 8d1b0c05](https://github.com/sgstepaniants/Forsythe/tree/8d1b0c0545a77b40245e84705aa7d273e6c81e62/lean-proof). Every byte count and SHA-256 matches the lock. The six retained original Forsythe source snapshots also match these fetched bytes. The locked files are checker/exporter/sandbox sources, regression probes, manifests, and licenses; this does not fetch the Forsythe mathematical proof.

The NLA Apache license is byte-identical to the pinned Forsythe `lean-proof/LICENSE`, and the retained Landrun MIT license is byte-identical to the pinned Landrun license, including its copyright attribution. Original Comparator source headers retain Lean FRO copyright and Henrik Böving attribution. The exported source set retains the Comparator/exporter Apache licenses. The recorded one-line Lean 4.33.1 replay adaptation exists as the pinned patch, and NOTICE distinguishes upstream code, the NLA driver, and the CI probe derivation. It explicitly disclaims endorsement by tool authors. This is source/notice correspondence review, not independent authentication of a person's authorship or legal advice.

I fetched the schema and license directly from their pinned GitHub revision. The schema and its license are byte-identical to the vendored copies. The schema's SHA-256 is `25ff6b25ca4511635aff4443cf20480c15e59dddf19591c730950b442ea54fce`; the schema README accurately calls it metadata validation rather than proof checking. See the [pinned schema source](https://github.com/mathlib-initiative/formalization.yaml/blob/99c678e569c7c4c0772db297c5ddd5e4c9b6322e/schema/v0.4.schema.json).

The pinned LeanCert README and toolchain support the documented Lean 4.33.1, default native trust, explicit kernel route, and file option. I also inspected the actual `#assert_trust` syntax and axiom-check implementation in the pinned `LeanCert/Tactic/Verification.lean`; the stated declaration check exists. This verifies the documentation's API claims, not the correctness of all LeanCert numerical algorithms. See the [pinned verification source](https://github.com/alerad/leancert/blob/621a43d7cf21f87872392a01e874f2f1dbddc926/LeanCert/Tactic/Verification.lean).

The pinned Tau Ceti rubric index and correctness rubric support the cited review angles and the distinction between kernel acceptance and semantic fidelity. NLA's protocol is explicitly an adaptation, omits Tau Ceti-specific roadmap requirements, and does not claim an official Tau Ceti run or endorsement. See the [pinned rubric index](https://github.com/TauCetiProject/TauCetiReview/blob/afb424eda89e8ac96d9eb69f6a88972055a4cd1b/rubrics/README.md).

## Documentation gates and publication clarification

`docs/lean/README.md:28–32` and `docs/lean/REVIEW.md:5` require two independent statement reviews before proofs, recorded against exact bytes, covering definitions as well as theorem signatures. Mathematical boundary changes reopen review. `REVIEW.md:17` requires at least two independent final reviewers, excludes self-review, and allows one reviewer to cover multiple explicit responsibilities. The responsibilities cover fidelity/scope, proof quality, and reuse/documentation/attribution. These gates complement rather than replace compilation, axiom checking, and sandboxed Comparator verification.

The CONTRIBUTING addition only links the new procedure. It retains the prior requirement that `Lean verified` applies to the complete original target with reviewed verification evidence. The general docs preserve problem/proof source authors separately from formalization authors, require truthful AI disclosure, and prohibit treating a successful build, reviewer approval, or partial certificate as complete verification. The frozen PR changes no canonical problem page, `RESOLVED.md`, or `problem_ids.json` relative to its published base. No problem ID, path, mathematical target, or status is changed by this infrastructure patch.

**Attribution clarification, repaired:** frozen `docs/lean/README.md:54` says unconditionally to credit George Stepaniants in generic metadata guidance. The parent's corrected text qualifies this with “For George Stepaniants's contributions” and retains his Department of Computing and Mathematical Sciences, California Institute of Technology affiliation and the instruction not to add a contact email. I inspected the actual diff. This prevents unrelated future formalizations from being directed to misattribute work, while retaining the requested affiliation and actual roles.

**Confidentiality clarification:** I read and approve the parent's added `tools/lean/HARNESS.md` paragraph: the sandbox mounts host files read-only, so candidate code can read host-readable data and expose it through logs; authoritative use therefore needs an isolated credential-free runner. Write/network restrictions are not a confidentiality guarantee. This correct limitation is compatible with the observed controls and the separately trusted infrastructure/Challenge/Lakefile/dependencies model.

The workflow reviewer independently reproduced full-project deletion/rename and quoted/Unicode/newline path-selection gaps in the original implementation and has repaired selection, the workflow detector, and tests. The new README sentence explains the intended fail-closed deletion/rename rule. Those are functional changes beyond the tested frozen head; this report does not infer their runtime correctness from the retained 214c142d run. Their source review, new relevant tests, and fresh final-head CI belong to the workflow/integration reviewers. The frozen PR must not be approved solely on the evidence PASS above while ignoring those separate functional findings.

The documentation repairs inspected in the integration worktree have these SHA-256 hashes: `docs/lean/README.md` is `27d1baf38609e50049f077e8004dabe598dc53cadab73acdb8a7a1e1e4a48be6`; `tools/lean/HARNESS.md` is `6e7bca698cbdce70599d59d710e5b3ad1757f796b4b05e18bcb78820e9bc0fd4`. All historical evidence files remained unchanged during those repairs, confirmed with Git. Frozen file hashes and these integration hashes are separately recorded in the scratch integrity JSON.

## Evidence and limits

Independent scratch evidence is under `/private/tmp/nla-review-inequalities/pr153-evidence/`: live GitHub artifact/job metadata, downloaded live checker log, `local-integrity-checks.json`, pinned schema/license responses, LeanCert README/toolchain/verification source, and the inspected Tau Ceti rubrics. No submitted project code was executed by this reviewer. No fresh Linux sandbox or Lean build was performed locally. Public GitHub reads and local hashing/ZIP/source comparisons are the executed checks.

The artifact's live expiry is 2026-12-11; the committed original archive and extracted raw logs preserve the inspected bytes afterward. Passing this evidence/documentation audit permits the infrastructure's scoped claims. It cannot promote a mathematical problem, replace the separate harness/workflow audits, or certify later changed source without appropriate revalidation.
