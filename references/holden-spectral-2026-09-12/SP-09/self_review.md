# Self-review — SP-09

No gap found in the two-point argument during self-review. Inspect especially
the strict inequality in the dimension-intersection argument: spectral values
at exactly t belong to the near set, while the far subspace uses >t. Finite
dimension supplies strictness on every nonzero vector of that subspace.

Checked: scalar A; p=1 and p=n-1; repeated eigenvalues of B; coincident threshold
distances; t=0; non-real and noncollinear spectra; either matrix satisfying the
two-point hypothesis; all finite k, not a limit k->infinity. A unitary realizing
a spectral bijection exists by normal spectral decompositions. Repetition
scales both capacities and counts by exactly k and preserves coverage.

The original normal-pair target permits arbitrary spectral cardinalities. That
hypothesis is not proved away. Only a subclass result is asserted. Independent
review and a prior-art search remain necessary before submission as new work.
