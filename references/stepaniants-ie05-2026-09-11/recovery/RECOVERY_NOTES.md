# Recovery record

## What was available

The current working filesystem contained none of the prior `nla_work` files. Recoverable conversation history nevertheless contained the IE-05 construction, the earlier rational-verification source, its complete reported stage maxima, the two growth quotients, and the location of the single changed entry.

The IE-05 files in this archive are reconstructed files, not an untouched copy of a surviving original archive. The new rational program reproduces all the reported values. An additional implementation checks the explicit integer-column matrices via block-inverse Schur complements.

## Second result

The prior IE-04 material described a strictly pivoted high-growth matrix, a small entrywise perturbation box, and a probability-versus-tail contradiction. It did not contain a finished submitted proof or a completed certificate.

During recovery, that outline was turned into the proof now in `IE-04/solution.md`: use strict lower entries `-1/2`, final column all ones, the radius `delta_n = 2^(-(n^2+n+1))`, and a per-stage amplification envelope `B_n = 2^(n+2)`. The complete deterministic lemma and Gaussian lower bound are proved in the manuscript. The default verifier also produces whole-box certificates for dimensions 2 through 16.

Thus IE-04 should be described as **recovered outline, completed during reconstruction**, not as an already finished proof recovered unchanged.

## Work not represented as solved

The retained IE-16 history consisted of an exploratory numerical optimization/search program. No proof or verified counterexample was recoverable. It has therefore not been put in a solution folder or counted as a solved problem. There were no other recoverable finished results in the available record.

No old numerical-search output has been promoted to a theorem. No unobserved three-hour execution, external review, GitHub submission, or original file preservation is claimed.

## Review status

Both included proof claims address the exact displayed repository targets and come with explicit scope limitations. All code was run during reconstruction, and both PDFs were compiled and visually inspected. The two mathematical checking routes for IE-05 were implemented in the same recovery session; this is not an assertion of independent human or separate-agent review.
