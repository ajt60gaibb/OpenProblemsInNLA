# Maintainer review of PR #103

Reviewed commit: `6f5861fa392db467dc23abd096a02a08573146b8`.
Date: 11 September 2026. Verdict: **PASS for the complete RA-10 nuclear-norm target with C=11.**

I independently read the complete proof, reconstructed the compression argument and constant assembly, checked the primary integral representation and inspected both final PDFs. Further independent readers in the other maintainer task corroborated the proof and supplied separate exact/high-precision diagnostics, archived in `independent-sept11/`. This is mathematical AI-agent review, not proof-assistant certification, human peer review or a priority determination.

## Compression and spectral reductions

The resolvent eigenvector equation gives the two displayed block equations with the stated noncommuting factor order. The substitution into the lower block gives `w^T F w = mu*s*(||u||^2-||w||^2) + mu^2 u^T H u`. Combining it with the Schur complement gives the lower bound for `||u||^2`. The identity for `v^T(C-A)v` follows directly. With `Delta=(cI-H)+`, the scalar remainder in equation (7) is `mu*[s(1-2mu)+c(1-mu)]/[2(1-mu)]`, which is nonnegative because `0<mu<1/2`.

Summation over positive eigenvectors is legitimate: their compressed components satisfy `sum uu^T <= I`, and the variational positive-trace bound applies to their full orthogonal projection. This proves Lemma 2 without simultaneously diagonalizing A and PAP. Adding the same `rP` to both matrices leaves their difference fixed and supplies the singular-compression limit for each fixed s>0.

For the selected rank-k subspace, compression min-max gives `h_i <= a_i`, so `L >= 0` and `d <= L`. Pinching yields `e0 >= R+L`, with the complementary trace contributing the optimal tail plus L. The trace identity and triangle inequality give the next two excess estimates. The ridge resolvent bound for B0 versus C is correctly restricted to the selected subspace, where B0 has spectrum at least c; its zero orthogonal complement is not incorrectly treated as bounded below by c. These estimates combine to `5*g*e0`.

The elementary ordered-eigenvalue nuclear perturbation argument is valid: the common upper matrix `U=X+(X-Y)-=Y+(X-Y)+` bounds each ordered eigenvalue by min-max, and summing gives the trace norm. Consequently `r <= e(B)`, `e0 <= e(B)+r`, and `5g(e(B)+r)+gr <= 11g e(B)`. Since each tail eigenvalue is at most c, `tau_s >= g*tau`, in the correct direction for the relative-error conclusion.

## Full function class and endpoints

[Chansangiam, Proposition 1.1, page 2](https://arxiv.org/pdf/1304.7936v1) gives the required positive integral representation without an f(1)=1 normalization. Endpoint masses supply nonnegative constant and linear terms. The weighted measure condition is exactly what is needed for finite scalar spectral integrals; their finite-dimensional positive integrands also justify integrating the nuclear-norm inequality. Realification of complex Hermitian matrices is valid under the original all-dimensions real-symmetric operator-monotonicity hypothesis.

The selected rank-k projection is retained even when the approximant has rank below k. The constant term therefore contributes `f(0)(I-P)` with nuclear norm `(n-k)f(0)`. Repeated eigenvalues, arbitrary permitted eigenbasis selections, epsilon=0 and f(0)>0 are covered. If the optimal input tail is zero, the hypothesis forces A=B, and the output error is exactly its optimal constant tail. No division by zero, Loewner ordering, commutation, positive definiteness or spectral gap is assumed.

The target agrees with the nuclear specialization of the relaxed-assumption question in [Persson–Meyer–Musco, closing Section 5](https://arxiv.org/html/2311.14023v2#S5). Their counterexample to constant one is consistent with a finite constant eleven. Sharpness of eleven and other Schatten/Frobenius norms are not claimed. The previous commuting result and lower bound of two retain separate attribution.

## Source and document checks

The independent task directly compared the complete original target and all 203 permanent ID/path pairs with published `aaa88c40`; all are unchanged. I recomputed all 13 current recorded source/PDF fingerprints: every value matches (`PR-103-document-hashes.json`). I rendered and visually inspected all nine pages: the three-page canonical problem and six-page proof. Displays, factor order, metadata and historical scope agree with the source, with no clipping, overlap or missing mathematical content. The PDF manifest and result are under `pr93-103-pdf-qa/`.

No mathematical source correction is required. The fork-workflow execution approval remains separate and unresolved; this review does not authorize or trigger it.
