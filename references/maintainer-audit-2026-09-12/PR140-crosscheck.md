# Independent local cross-check: PR #140 / IE-26, Sections 6–7

**Verdict: PASS for the assigned local-moment and dyadic-matrix steps. No blocking mathematical gap found.** Reviewed head: `fe894049e88c8a88baaeddbff9ca5c187ffff4be`. Source: `/private/tmp/nla-pr-audit-20260912/root/140/solution.md`, lines 205–329, with the definitions and derivative/comparability inputs in Section 5. This is a bounded informal mathematical cross-check, not a separate full-target, PDF or provenance review.

1. **Positive-real weak bound, lines 216–221.** For smooth analytic f with Re f>=0, its real boundary part has L1 norm 2pi Re f(0). The nonconstant imaginary part is the periodic Hilbert transform of that real part. Markov, weak (1,1), and the constant imaginary part give a bound C|f(0)|/u for the union controlling |f|>u. For thresholds small relative to the constant imaginary part, the trivial full-circle measure gives the required bound; for larger thresholds that part contributes no exceptional points. I independently checked [Laugesen, Theorem 12.1, printed p.67](https://arxiv.org/pdf/0903.3845v2): the theorem applies to the smooth L2 boundary data here. Replacing f by f composed with a disk automorphism gives the harmonic-measure version. The actually used f±(rz) extend analytically past the closed disk, even though the original localized boundary density is discontinuous.

2. **Localization and normalization, lines 223–241.** Outside J, points on the selected interval and neighboring cells remain distance comparable to ell from the support of G_far. Integrating the differentiated kernel gives C alpha/ell; integrating over angular distances O(ell) makes the variation O(alpha). Thus one positive b_I normalizes both the weights and their reciprocals. At z_I=e^-ell e^(ix_I), the local kernel is O(1/ell) on support of length 8ell, giving |G_loc(z_I)|<=C alpha. The sector condition makes both f±=exp(±iG_loc/(2alpha)) positive-real, and the value bound is absolute after dividing by 2alpha.

3. **Harmonic measure and counting, lines 238–246.** Precomposition is at zeta=e^(-(ell-t0))e^(ix_I); indeed r zeta=z_I. Since ell=qh=2pi q t0, ell-t0 is comparable to ell. The Poisson density at zeta is at least c/ell on every selected cell and its stated neighbors. Applying the weak bound at threshold u^p, p=1/(2alpha), gives C ell u^-p angular measure. The local derivative bound implies bounded multiplicative variation of |B_loc| and its reciprocal on each disjoint grid cell. Dividing cell measure by h therefore gives Cq u^-p. As 1<p<2, raising the cell-comparison constant to p stays uniformly bounded.

4. **Sharp cutoff and second moments, lines 248–272.** The coefficient of the absolute imaginary-kernel integral is alpha/2. On a symmetric interval of radius L=O(ell), the integral of |sin u|/(cosh t0-cos u) is 2 log[(cosh t0-cos L)/(cosh t0-1)], bounded by 4 log(ell/t0)+C. Thus the exponent is exactly 2alpha log(ell/t0)+C alpha, not 4alpha log(ell/t0). This yields cutoff Cq^(2alpha). Layer cake gives q+Cq integral_1^(Cq^(2alpha)) u^(1-p) du; since 2-p>0, the resulting exponent is 1+2alpha(2-p)=4alpha. Constants may depend on alpha. For qh>1/100 the whole-circle proof gives m^(4alpha); m<=200pi q and 4alpha<2 permit the claimed comparison. No missing logarithm occurs away from alpha=1/4.

5. **Pointwise matrix bound, lines 289–306.** The single-node Poisson term gives beta_j<=Cm^-1/|R(rz_j)|. Residues and local comparability give 1/|P'(z_j)|<=C_alpha m^-1 w_j^-1. The regularized product numerator is at most Cw_k, while the denominator is at least c(1+d(k,j))/m. Radial separation covers d=0; the shift bound covers d>=1. This yields the exact weight-ratio bound used next.

6. **Block norm and no-log summation, lines 308–329.** Choose consecutive blocks of length R except possibly one final shorter block. At distance R<=d<2R, each block has only O(1) neighboring blocks in either direction. Two interacting blocks fit in one circular interval of O(R) indices, with the entire circle used at the largest scales. The entrywise bound implies a Frobenius (hence spectral) block bound C_alpha/R times sqrt(sum row w²) sqrt(sum column w^-2), even with a distance mask. Applying Lemma 4 on the same containing interval cancels b_I exactly and gives C_alpha R^(4alpha-1). The scalar matrix of block norms has bounded row and column sums, proving the same full-scale norm bound. The near diagonal is handled at constant scale. Finally sum_R R^(4alpha-1) is geometric because alpha>1/4; its alpha-dependent denominator is allowed. There is no implicit endpoint claim.

**Optional clarification only:** “blocks of length at most R” in line 308 should be understood as the concrete partition into length-R blocks and one possible remainder. Arbitrary partitions into tiny blocks do not have bounded incidence, but the required valid partition exists explicitly, so this is not a proof gap or a required correction.
