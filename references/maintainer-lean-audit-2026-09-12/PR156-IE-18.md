# Independent review of PR #156 — IE-18

**Verdict: PASS.** The submitted Lean proof settles the complete canonical four-step maximum identity negatively. No mathematical, statement-fidelity, or proof-source trust blocker was found. The reviewed remote kernel evidence supports promotion to **Lean verified** for that identity alone.

**Reviewed head:** `b8000224c44004b754bf64f76641aa2c75ad9148`. Review date: 2026-09-12. Reviewer: independent OpenAI Codex agent `/root/audit_spectral_linear`; this is an AI-agent review, not external human peer review. Files were read from `/private/tmp/nla-lean-audit-156`; no repository files were changed.

## Exact scope and statement fidelity

I read the complete `Definitions.lean`, `Proof.lean`, `Challenge.lean`, `Solution.lean`, project configuration, manifest, numerical-target specification, formalization metadata, and canonical README. I compared the original statement at `5adea969c17391693978ada2674d25bb5c3daeb1` with the submitted canonical statement. The target is preserved; display formatting and the added verification notice do not change it.

All Lean locations below are relative to `linear-systems-and-elimination/IE-18/lean/`.

- **Residual and norm:** `NLA/IE18/Definitions.lean:21–46` uses the coordinate sum of real squares, its real square root, and the unsquared quotient of Euclidean norms. It does not accidentally use the default supremum norm on a function type. The coefficient is exactly `vᵀ(I−M)v / ‖(I−M)v‖₂²`; the residual explicitly sends zero to zero. `fourStepResidual` composes this two-step map twice. On the admissible domain, exclusion of one from the spectrum makes `I−M` invertible; totalized real division therefore adds no spurious nonzero-vector behavior. Both denominators actually used in the witness are also proved positive rational values.
- **Spectral factor:** `Definitions.lean:52–70` squares the actual scalar pair quotient, including its prescribed zero-denominator value. The finite supremum is taken in nonnegative reals and then coerced to reals. It equals the desired maximum because the finite pair set is nonempty for `n≥2`. The filter is on distinct **indices**, not distinct eigenvalues, and `hM.eigenvalues` is Mathlib's genuine complete eigenvalue list, with multiplicities.
- **Full quantifiers:** `Definitions.lean:75–78` quantifies over every natural dimension at least two and every nonzero real symmetric matrix with one excluded from its spectrum. Positive definiteness and diagonal structure are proved properties of the chosen witness, not added restrictions on this universal assertion. `IsGreatest` expresses both attainment and the upper-bound condition of the original maximum identity. The proof violates the upper bound itself, so its conclusion does not exploit a technical failure of attainment.
- **Exports:** `Challenge.lean:8–34` and `Solution.lean:15–41` have the same three complete signatures: the actual residual certificate, admissible strict counterexample, and negation of the universal conjecture. There are no extra hypotheses or hidden section variables in the implementation.

I checked the pinned Mathlib definitions: [Hermitian.lean](https://raw.githubusercontent.com/leanprover-community/mathlib4/0df444a360eaa60ab8c11dca51a86af692955474/Mathlib/LinearAlgebra/Matrix/Hermitian.lean), definition `IsHermitian` and `isHermitian_iff_isSymm`; [Spectrum.lean](https://raw.githubusercontent.com/leanprover-community/mathlib4/0df444a360eaa60ab8c11dca51a86af692955474/Mathlib/Analysis/Matrix/Spectrum.lean), definitions `eigenvalues₀`/`eigenvalues`, characteristic-polynomial multiplicity theorem, and real-spectrum range theorem; and [PosDef.lean](https://raw.githubusercontent.com/leanprover-community/mathlib4/0df444a360eaa60ab8c11dca51a86af692955474/Mathlib/LinearAlgebra/Matrix/PosDef.lean). These support the interpretations above. I also checked the paper's residual equation (9) and Conjecture 10, equation (27), against the canonical finite-step target: [arXiv v4](https://arxiv.org/html/2312.04776v4).

## Proof and independent exact calculation

`Proof.lean:23–109` derives each residual directly from the map, with exact rational algebra. `:111–140` proves matrix admissibility, positive definiteness of both `M` and `I−M`, and the actual spectrum. `:143–194` proves an upper bound for every actual spectral pair and attains it at eigenvalues `1/10` and `3/5`; their unequal values force unequal actual indices. Thus this is a maximum proof, not merely evaluation of a selected pair.

I independently reconstructed the operations using Python `fractions.Fraction`, without running submitted diagnostic code:

| Quantity | Independent result |
|---|---|
| `M`, `I−M` diagonal entries | `(1/10,1/2,3/5)`, `(9/10,1/2,2/5)` |
| First denominator and coefficient | `61/50`, `90/61` |
| First residual | `(-2,8,15)/61` |
| Second denominator and coefficient | `1381/93025`, `3140/1381` |
| Second residual | `(289,-756,1125)/84241` |
| Squared amplification | `1920682/21289638243` |
| Three unordered pair values | `1/289`, `1/121`, `9/2401` |
| Actual proposed maximum | `1/121` |
| Squared amplification minus `(1/121)²` | `6831066919/311701593515763 > 0` |

`Proof.lean:196–221` proves the square-of-Euclidean-norm identities, nonnegativity, and the strict **unsquared** inequality. The comparison correctly squares `1/121` a second time; it does not confuse the pairwise square already present in the proposed norm factor. `:228–254` places the witness amplification in the actual amplification set, contradicts its claimed greatest value, and instantiates the unrestricted universal assertion at dimension three.

## Trust and verification evidence

The proof source contains no `sorry`, custom axiom, native proof mode, unsafe declaration, custom elaborator, or external execution command. The three deliberate `sorry` placeholders occur only in the separate Challenge target; Solution imports Proof and Definitions, never Challenge. LeanCert is used only for the exact scalar inequality at `Proof.lean:209–210`, with both the file option and the call explicitly selecting kernel trust. All eight internal/public audited results have `#assert_trust kernel` and axiom-report commands. Comparator lists all three public exports, permits only `propext`, `Classical.choice`, and `Quot.sound`, and leaves `definition_names` empty.

The toolchain and dependency revisions are pinned: Lean 4.33.1, LeanCert `621a43d7cf21f87872392a01e874f2f1dbddc926`, and Mathlib `0df444a360eaa60ab8c11dca51a86af692955474`. A direct Git content diff confirms that the four Lean files and four configuration/manifest files are unchanged between the README's immutable proof revision `7b8512e21c50adc8597dcdbed32f2aec13c3b43e` and the reviewed PR head.

For the newer [GitHub run 34706412720](https://github.com/ajt60gaibb/OpenProblemsInNLA/actions/runs/34706412720), I read the downloaded `result.json`, `comparator.log`, kernel controls, Comparator controls, and sorry/native negative-control logs under `/private/tmp/nla-lean-evidence-156/lean-IE-18/verify-20260912T165055Z-4205/`. They record all three public exports, fresh project builds, only the three permitted axioms, successful default-kernel replay, and rejection of invalid raw proofs, quotient mismatches, statement mismatches, sorry, and native axioms. The root reviewer independently retrieved this run and matched its 91 recorded inputs to the PR tree. The artifact itself identifies repository commit `1116667218bde91fefb2207707ceec547e438bab`; correspondence from that run to the PR head rests on the root reviewer's input comparison, not on claiming they are the same Git commit.

**Limits:** I did not run a local Lean build or independently reexecute the Linux harness. The remote evidence is reviewed evidence, not a local rerun. I did not independently audit all transitive Mathlib/LeanCert implementation code; the recorded default-kernel replay supplies the relevant proof check. The stronger parameter-family underestimation result and separate asymptotic convergence question remain outside this formalization. Attribution to Colbrook for the mathematical counterexample and Stepaniants for the Lean formalization is preserved.
