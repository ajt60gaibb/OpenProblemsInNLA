# PR #155 / MI-19 independent review

**Verdict: PASS — the reviewed Lean declarations settle the complete original MI-19 target negatively and support `Lean verified`.** No mathematical or statement-fidelity blocker was found.

- Reviewed head: `2f5eaed203c086a0a2edec4d4666d0baea67af81`.
- Review date: 12 September 2026.
- Reviewer: independent OpenAI Codex agent `/root/audit_tensors_complexity`. This is an AI-agent source/fidelity review, not external human peer review.
- Read in full: the actual `NLA/MI19/Definitions.lean`, `NLA/MI19/Proof.lean`, `Challenge.lean`, `Solution.lean`, Lake configuration, lockfile, Comparator configuration, formalization manifest, canonical statement, and original informal proof. Submitted referee verdicts were not used to establish mathematical correctness.

## Statement fidelity

`Definitions.lean:23–46` defines exactly the intended permutation sums. Inversions count every pair of original indices `i < j` with `σ(i) > σ(j)`. Terms use `A i (σ i)` and the natural power of the real parameter embedded into the complex numbers, preserving `0^0 = 1`. Restriction is `S.image σ = S`, meaning setwise preservation; the original full inversion count is retained. There is no substitution by pointwise fixation, an initial-segment restriction, or a product of smaller permanents. The zero-based singleton `{1 : Fin 4}` is precisely the paper's interior singleton `{2}`.

`SubsetConjecture` at lines 51–54 quantifies over every natural `n ≥ 2`, every complex matrix satisfying `Matrix.PosSemidef`, every real `0 ≤ q ≤ 1`, and every nonempty proper subset. The pinned [mathlib PSD definition and Gram theorem](https://raw.githubusercontent.com/leanprover-community/mathlib4/0df444a360eaa60ab8c11dca51a86af692955474/Mathlib/LinearAlgebra/Matrix/PosDef.lean) include Hermitian symmetry and nonnegativity on all finitely supported complex vectors; for `Fin n`, this is the usual unrestricted complex-vector condition. No positivity of individual entries or positive definiteness is assumed.

The definitions agree with the preserved canonical statement and with the inversion convention and subset formulation in [da Fonseca, equations (1.1), (5.1), Conjecture 3](https://arxiv.org/pdf/1804.02231). The paper permits the full set as well; that case is equality and does not affect the canonical proper-subset target or this counterexample.

The [complex partial order](https://leanprover-community.github.io/mathlib4_docs/Mathlib/Analysis/Complex/Order.html) compares real parts while requiring equal imaginary parts. This causes no false counterexample through incomparability: both witness sums are explicitly proved real, and a strict inequality is proved. More generally, Hermitian conjugation pairs a permutation term with its inverse; inversion number is unchanged under inversion, and setwise-preserving permutations are closed under inversion. Thus both sums in the original domain are real, consistent with the English inequality.

## Actual proof and independent arithmetic

`Proof.lean:23–31` proves the displayed Gram identity entry by entry and derives PSD over complex vectors. Lines 34–99 use mathlib's recursive permutation equivalence to enumerate every permutation, then reduce the actual inversion counts and products. The six-pair formula at lines 44–57 retains all increasing pairs at order four. No external coefficient table is assumed.

I independently used Python's `itertools.permutations` and exact `fractions.Fraction` arithmetic, with the displayed integer matrices entered directly. All 16 entries of `X* X = A` agreed. Enumeration covered 24 permutations, of which exactly six preserve the interior singleton. It reproduced:

| Quantity at `q = 7/8` | Exact value |
| --- | --- |
| Full q-permanent | `335001935775/16384` |
| Restricted q-permanent | `167502585675/8192` |
| Full minus restricted | `-3235575/16384` |

As an additional check on the convention, the six preserving permutations have full inversion counts `0, 1, 3, 4, 4, 5`, not the inversion counts of a reindexed three-element complement. The independently reconstructed coefficient lists, in ascending degree, are `[4999700, 4886140, -199231, 4712758, 9568969, 4886140, 115600]` and `[4999700, 4741130, 0, 4741130, 9482260, 4999700, 0]`. This calculation corroborates the proof; it is not a substitute for kernel verification.

Lines 112–130 establish the exact complex gap, prove the real scalar negativity with explicit kernel-mode LeanCert, transfer it through `Complex.real_lt_real`, and derive the strict reverse inequality using subtraction. Lines 133–157 discharge every witness hypothesis and specialize the universal conjecture at dimension four to obtain its negation. The public `counterexample` and `not_subsetConjecture` in `Solution.lean` export those proved results without added premises. Their type texts match the Challenge declarations after whitespace normalization.

## Trust and verification evidence

The proof import chain is `Solution → Proof → Definitions`; it never imports `Challenge`. The two deliberate Challenge `sorry` placeholders therefore do not support either result. The actual proof/definitions contain no custom axiom, `sorry`, native-decision shortcut, external implementation, or unchecked evaluation. The Lakefile is declarative TOML, defaults to `Solution`, and pins LeanCert `621a43d7cf21f87872392a01e874f2f1dbddc926`; the manifest pins mathlib `0df444a360eaa60ab8c11dca51a86af692955474` and the other eight dependencies. The toolchain is Lean 4.33.1. Comparator selects both public theorems, allows no definition holes, and permits only `propext`, `Classical.choice`, and `Quot.sound`.

I inspected the downloaded artifacts of [upstream GitHub run 34706412117](https://github.com/ajt60gaibb/OpenProblemsInNLA/actions/runs/34706412117), including the result receipt, actual Comparator output, dependency log, and kernel/sorry/native rejection controls. The receipt identifies verification commit `fa12c9f16606143b63c7129c39229935a5061030`; I independently checked all **146 recorded project input hashes** against the reviewed PR worktree and found zero mismatches. The actual run accepted both public declarations with no definition holes. The public theorems and five audited internal declarations report only the standard three axioms. Comparator records successful Lean default-kernel replay and exit status zero; invalid-proof, `sorryAx`, and native-trust controls were rejected as intended. The coordinating reviewer separately checked live run identity and the unchanged shared harness.

This audit reviewed successful Linux verification evidence; it did **not** rerun Lean or the Linux sandbox locally. The exact-arithmetic diagnostic was independently written and run locally. No submitted diagnostic script was executed, and no tracked file or remote state was changed.

## Scope

The formalized result is a complete negative answer to the original universal yes/no question. Exact rank two, polynomial identities for every q, positive-diagonal-perturbation variants, and the separate MI-18 monotonicity problem are not exported formal claims. The PR states these limits explicitly. The original ID, canonical path, and mathematical target remain intact.
