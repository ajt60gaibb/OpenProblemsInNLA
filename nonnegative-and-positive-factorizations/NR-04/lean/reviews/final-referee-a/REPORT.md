# NR-04 independent final referee A

Verdict: **APPROVE** the frozen mathematical statement and proof source. This is an independent AI source review, not a compiler or Comparator acceptance and not human peer review. Reviewer: `/root/nr04_mf14_final_referee_a`; nonauthor of all NR-04 source in this packet.

Reviewed packet: `NR04-v1`, snapshot SHA-256 `7664e1a0059009b92b24cc1203033d0b3426a2b523c51b32316626d2addf8301`. I read all 42 Lean files (including the isolated Challenge and aggregate), the canonical problem and entire retained mathematical manuscript, numerical target plan, freeze and Comparator configuration, and the supplied local aggregate log/axioms/receipt. I recomputed every file hash registered by the snapshot: no mismatches. This report covers original-target fidelity, proof correctness/quality and a bounded pinned-Mathlib reuse/API/attribution review. No packet source was edited.

## Statement fidelity

The canonical question quantifies arbitrary real nonnegative 9-by-6 and 6-by-9 factors of the squared-distance matrix indexed 1 through 9. `distanceNine` is the same matrix after adding one to both zero-based indices. `HasNonnegativeFactorization` is an unrestricted existential real factorization, and the C15 conclusion explicitly expands the original question. C14 uses `IsLeast` on the natural feasible-width set, proving both seven-term existence and minimality. The conclusion is not merely ordinary rank, a rational-factor search, or a conditional geometric reduction.

I compared every one of the 15 Challenge theorem signatures with its uniquely located implementation; all are identical after whitespace normalization. Shared `Definitions.lean` contains transparent standard matrix/convex/affine definitions, with no conclusion concealed in a predicate. Its and the Challenge's hashes agree with the preproof freeze. `definition_names` is empty. The final lower bound includes every natural k <= 6, including zero, and the nonsymmetric general rank-three statement. Normalization, rank bounds on factors, nonzero columns, and geometric general position are not extra premises of the canonical answer.

## Proof audit

- C01-C07: the only LeanCert calculation is exact positivity of 8 in kernel mode, actually consumed by the nonzero leading-minor argument. The rank upper bound is a literal three-column real decomposition. The seven-term nonnegative upper witness is proved by selector summation and a universal four-sign-case reflection identity; center zero and endpoint labels are retained.
- C10 normalization: `low_rank_factor_obstruction` derives nonempty positive mass from rank three. A zero-mass nonnegative column is proved identically zero. `filledColumnNormalized` replaces such a column by an existing normalized one without increasing rank; its convex weight remains zero. Weights are exactly `columnMass W r * H r j / columnMass M j`, proved nonnegative, summing to one, and reproducing the normalized actual product. No zero-column case is discarded.
- C09 contacts: three distinct coordinate zeros would force the vector to vanish because the corresponding principal 3-by-3 minor has positive determinant. Coordinate sum one excludes that. `FiniteSection` proves actual section extreme-point finiteness by injecting chosen positive supports into a finite powerset, and obtains its exact finite extreme-point hull via compactness and Mathlib Krein-Milman. `IncidenceCount` then applies weighted double counting, including the degree-one exception; it does not postulate a supporting-normal assignment.
- C08 small sections: the rank-at-most-three case identifies the containing column span and actual affine plane. The rank-four case constructs a linear height using genuine subspace dimensions and identifies its zero slice on the normalized hull. `SliceRecombination` proves the finite crossing-hull identity, treating zero total mass, one-sided/empty families, zero heights, repeated generators and zero weights. Counting z+p*q leaves only the 3+3 partition; duplicate crossings already yield at most eight distinct candidates.
- The distinct 3-by-3 exception is closed, not assumed away. Every 2-by-2 crossing rectangle has a common point in its open diagonals by explicit positive reciprocal-height weights. The actual rank-three column affine plane is parametrized injectively by R^2. If all nine crossings were extreme, a finite shear and selected minimum would produce an injective actual slope key with positive increasing-triple orientations. Open crossings force odd comparison parity. The nine rectangle conditions sum to an impossible parity relation. At least one of the nine candidates is therefore nonextreme, giving the actual eight-extreme-point bound. These lemmas preserve the full hull and actual points under their affine transports.
- C11-C15: Sylvester is proved by rank-nullity for W restricted to range(H) and a kernel injection. This forces one factor's rank <= 4. Applying C10 directly or to the correctly reversed transpose factorization gives N <= 8, contradicting N >= 9. The lower theorem is then combined with the explicit seven-term upper witness.

The finite-incidence and crossing-parity implementation is a substantive alternative to the manuscript's normal-cone/Euler exposition. It proves the same full statements, supplies the missing geometry internally, and does not claim to have formalized a general polytope Euler theorem. Computation is confined to small exact data/algebra and parity; there is no interval search over unrestricted real factors.

## Trust and observed verification evidence

A recursive source scan found no `sorry`/`admit`, custom axiom, native-decide, unsafe/extern/implemented-by escape, or Challenge import in the solution source. The 15 intentional placeholders are confined to `Challenge.lean`. The aggregate imports `FinalBounds` independently and prints axioms and asserts kernel trust for all 15 contracts. `comparator.json` permits only `propext`, `Classical.choice`, and `Quot.sound`.

The supplied `local-evidence/AGGREGATE.log` reports exactly those permitted axioms for each target, and `AXIOMS.json` agrees. `RECOVERY-087.json` records actual macOS `Solution.lean` compilation with `--threads=1 --memory=4096`, exit 0, 2026-09-19 07:49:42.680904 through 07:49:48.113106 UTC, aggregate source hash `392fb7344c2a6de4fa76e5357d80df1f34b685f71e67cb1cab3a37c3034e6568`, log hash `22d7dfbfca235cd625dd2971d76a29f7ecd44e2c7fa9565b301212ad488a4356`, output hash `d2d50975956cad150f9a2d794f431682961f35a1d6264951a062ec747d1422a1`. Its 40 NR04 dependencies are explicitly marked source-matched reused successful local outputs; their registered source hashes match the frozen packet. I inspected that evidence and the serial runner's matching logic; I did not reexecute it or independently reconstruct every historical compilation.

I ran **no Lean/compiler and no Comparator**. No non-root Linux Comparator run is present in this packet. Source approval cannot replace the required fresh Linux sandbox/raw-kernel/Comparator and exact publication correspondence gates. This review changes no completed-problem count.

## Reuse, presentation and attribution

I searched the locally available Mathlib checkout and confirmed HEAD `0df444a360eaa60ab8c11dca51a86af692955474`, matching the manifest. Bounded searches covered matrix rank/rank-nullity, convex/extreme/affine section material, double counting, and geometry/facet/Euler terms. I inspected `Matrix/Rank.lean`, `FiniteDimensional/Lemmas.lean` search results, `Analysis/Convex/KreinMilman.lean`, and `Analysis/Convex/Extreme.lean`. The implementation appropriately reuses rank/span, convex combinations, Krein-Milman and bipartite double counting. Mathlib's `image_extremePoints` is for linear equivalences, whereas the helper here needs injective affine embeddings, so that extension has a reasonable scope. The search found no directly applicable bounded plane-section/Euler result; this is a bounded search, not a library-wide absence claim.

Namespaces and modular decomposition expose each geometric obligation. Mathematical attribution to Matthew J. Colbrook and earlier Hrubes/Gillis--Glineur reflection work is retained. George Stepaniants receives the required Caltech department affiliation, Codex assistance is disclosed, no email is introduced, and Apache 2.0 is declared.

Nonblocking presentation observations: several internal docstrings and the frozen Challenge/target plan retain historical draft or “still unresolved” wording even though later imported files close those obligations; the manifest top-level project name is inherited as `NLAPF03` while `lakefile.toml` names `NLANR04`. These do not change a theorem or invalidate this source approval. Publication metadata should describe the final implementation and current scope clearly; any mathematical source change requires an appropriate review continuation. `formalization.yaml` and final publication/README status are outside this frozen proof packet and were not approved here.

No blocking mathematical or statement-fidelity finding remains in the reviewed frozen source.
