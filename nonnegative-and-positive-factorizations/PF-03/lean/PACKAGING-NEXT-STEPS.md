# Consolidation history and pending Linux evidence

This candidate belongs at
`nonnegative-and-positive-factorizations/PF-03/lean/` in its own upstream PR.
The original PF-03 ID, canonical path, target and Holden resolution remain
unchanged. The packaging agent did not compile or publish the initial package. Root has now
completed steps 1–3: exact local compilation, archived actual evidence and two
wholly nonauthor final reviews. Steps 4–6 remain pending; the numbered list below
retains the original consolidation checklist for traceability.

1. Verify the packet hashes and the copied 59-module implementation closure
   against the intended final local sources. Preserve frozen Challenge,
   Definitions, RawData, numerical-plan and Comparator bytes. The Lake change
   selects Solution by default and adds its library target; it changes no
   mathematical contract. Retain the original literal-data generators and
   primary-source bindings with their provenance when consolidating evidence.
2. Run the new Solution aggregate with the coordinator's serial local checker.
   Record exact source hashes, command, compiler/dependency pins, exit code,
   log and output hashes. Retain actual origins for every reused output. Check
   all 25 axiom reports and trace real consumption of the kernel-mode LeanCert
   endpoint certificates through the final theorem. A source scan is not this
   axiom/body-graph audit. A standalone Lake build must remain unclaimed unless
   actually run.
3. Copy the actual pre-proof statement reviews and successful header evidence
   into the project's review/evidence tree. Obtain at least two fresh final
   nonauthor AI-agent reviews on exact source hashes. Exclude /root and
   /root/recover_lean_sources as final independent proof referees. Cover scope,
   correctness, degeneracies, numerical coverage, imported results, reuse,
   documentation and attribution under pinned docs/lean/REVIEW.md and Tau Ceti
   revision afb424eda89e8ac96d9eb69f6a88972055a4cd1b. Resolve material findings
   against the final bytes and record reviewer limitations.
4. Only after local proof development succeeds, publish the intended proof
   revision and run the repository's real pinned non-root Linux Comparator,
   exporter/raw-kernel, sandbox and negative controls. Check that the workflow
   selects PF-03 and all 25 unchanged contracts. Record the immutable commit,
   run/job IDs, logs, artifacts, input/source lock and output hashes. A later
   commit needs explicit source correspondence; no green badge substitutes
   for the exact-input comparison.
5. Replace pending fields in STATE.json, formalization.yaml, the implementation
   map and README only with observed results. Fill per-result actual axiom
   sets, final source hashes, referee report hashes and execution links. Keep
   local Lean, metadata validation, referee reading and GitHub Comparator
   evidence distinct. Do not turn the permitted-axiom list into an alleged
   observed axiom report. This metadata uses JSON syntax, which is valid YAML,
   and the pinned v0.4 schema; rerun validation after updates.
6. Before a complete-target promotion, link the immutable proof and exact
   verification evidence from the existing canonical README and RESOLVED.md
   entry. Regenerate affected indexes, TeX and PDF and inspect the PDF. Run the
   permanent-ID validator, catalog generator and prescribed safeguard tests
   against the published base. Submit one PF-03 PR to
   `ajt60gaibb/OpenProblemsInNLA:main`, preserving author roles and publishing
   no email. Partial helpers and this packaging packet increase neither the
   mathematical-resolution count nor the completed-verification count.

Required final records are deliberately absent until real checks occur.
Do not create placeholder PASS logs, invented run identifiers, or referee
approvals. If evidence is still pending, retain the current mathematical
Solved status and describe the formalization's actual incomplete scope.
