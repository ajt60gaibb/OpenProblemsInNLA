# PF-03 final independent formalization review

**Verdict: PASS for the exact reviewed mathematical source and the fidelity of the recorded local evidence.** This is an independent **AI-agent nonauthor review** by `/root/pf03_final_referee3`, completed 19 September 2026. It is neither human peer review nor a new Lean/Comparator execution. The exact published-source non-root Linux Comparator, exported-kernel replay, sandbox and rejection-control gates remain required and unrun for this candidate. This review does not increase the completed-problem count.

I did not author the PF-03 definitions, reference statements, implementation, packaging or local compiler harness. I did not use the other final referees' verdicts as evidence. I inspected the files and performed the independent static, rational-data and receipt audits retained in `audit.py` and `AUDIT.json`. No candidate file was edited and no Lean compiler was started.

## Exact scope and inputs

The mathematical packet is `development/PF03-agent-packaging-v1/project`, with handoff SHA256 `b282175f2f9ad97cf324280013ee3c880477ca5bbaeb4b6a3dfdc112df57829c`. Its 61 Lean files are byte-identical to the publication package. I checked all 213 entries of the publication evidence freeze, SHA256 `dc090fe6ccba8a12f344cb2392267a8f8ffad3be5cf1b552bb821e40fb0099d1`; every recorded file hash matches.

I read the retained original canonical target and the full Holden manuscript at upstream snapshot `71563f17926cd826a892c2bba0e294894ee57a5c`, and independently parsed both primary JSON inputs. The manifest records exact hashes for those inputs, every Lean source, the contract/configuration files and the consulted review/library sources. The solution aggregate has SHA256 `bc6410c59b42efa2a0f0c605a8648e63f426be35546bdb1350a6f36268267917`.

This approval attaches to those bytes, not to an unspecified future commit or an unexecuted build. Later metadata may record completed reviews and actual Linux results; any changed mathematical source requires renewed review of the change.

## Fidelity, definitions and nonvacuity

`RationalBoundaryFactorability` retains the original universal quantifiers: every natural order at least five and every rational symmetric boundary matrix. `RationalFactor` allows an arbitrary positive finite number of rational nonnegative columns. It does not bound that width by seven, by matrix order, or by cp-rank. Allowing only positive widths in the CP predicate does not omit the zero matrix: a single zero column represents it.

`SymMatrix` is the actual subtype of symmetric matrices, with inherited topology; `CPSet` consists of real nonnegative finite Gram factors, and `realCast` is the ordinary rational-to-real embedding. The frontier therefore is in the real symmetric matrix space, not the nonsymmetric ambient space where every symmetric matrix would trivially lie on a boundary. The final two theorem statements are unconditional; no certificate, halfspace representation, rank or cone property survives as an external premise.

The witness differs from Holden's explicit order-444 matrix. The proof constructs some finite rational whole-cone inequality matrix and appends five zero rows. This gives order at least five and a zero diagonal coordinate. `cp_zero_diagonal_frontier` constructs a continuous symmetric perturbation decreasing that coordinate, proves each perturbed matrix is outside CP by diagonal nonnegativity, and takes its limit. It also proves CP membership of the unperturbed witness through the actual seven-column real factor. This establishes a nonvacuous counterexample to the exact original universal assertion. It does not formalize the source's extra order-444, strictly positive-entry or minimal cp-rank claims; the README and manifest correctly exclude them.

## Complete proof path

1. `alpha_certificate` proves that the specified positive real `rpow` has cube two and lies strictly between the two exact 40-digit rational endpoints. The direct LeanCert entry point receives kernel trust, depth zero, precision `-160`, and disabled rational fallback for the cubic bounds. The retained log records both accepted dyadic certificates. I inspected the pinned LeanCert implementation: kernel mode does not fall back to native verification. The enclosure is consumed by `cubicLower_le`, the local/cross/barycentric sign proofs, and hence the final obstruction; LeanCert is not an unused import.
2. Cubic arithmetic is derived from the proved cube equation. `CubicIndependence` uses the actual minimal polynomial and rational independence; no field or arithmetic oracle is postulated. `rational_ray_zero` treats an arbitrary real scale, including zero, and uses a nonzero rational two-by-two minor to exclude any nonzero rational point on the seed line. It does not invert the scale or assume the desired irrational-ray conclusion.
3. Orthogonality, symmetry, trace zero and all seven local restrictions are checked against actual rational cubic coefficients. The local PSD argument uses symmetry, the exact kernel equation, the nonzero third coordinate and both positive leading minors. Two minor signs alone are never mistaken for full PSD. Upper-triangular checking is completed by proved symmetry; the reverse orthogonality is obtained from Mathlib's square-matrix inverse identity.
4. C09 proves equality of the triangle, barycentric coefficients and all actual generators with their proposed caches. C10 proves all 147 QG cubic entries equal the defining products before using them in cross terms. All 189 distinct-group pairings have strict positive lower bounds; reversed groups are supplied by proved symmetry. The finite index dispatch covers every permitted pair, including the repaired explicit upper-index cases for groups two through five.
5. C12 expands a conical combination with arbitrary nonnegative **real** coefficients into all seven groups. Every diagonal and cross-group contribution is nonnegative. If the quadratic value is zero, all pair terms vanish. Either all coefficients are zero, or one nonzero coefficient forces every other group to have zero coefficients, and the exact local-kernel theorem supplies its nonnegative seed-ray scale. The scale's sign comes from the triangle's first coordinate, not from an assumed orientation. The converse follows from the proved seed kernel. C13 then rules out all nonzero rational zeros.
6. The no-line property follows from the checked positive rational functional on all 21 generators. `one_variable_projection` handles positive, negative, zero and empty coefficient families explicitly. `rational_projection` eliminates arbitrary numbers of real variables by induction, including dimension zero. `rational_cone_halfspaces` encodes both directions of `x = Vλ` and `λ ≥ 0` before eliminating λ. Thus it proves equality with the **whole real cone**, not just inclusion or a rational-point surrogate. There is no unproved facet-completeness premise.
7. Literal absence of lines gives real and rational injectivity of the inequality matrix and a rational left inverse. `gram_factor_transport` proves that every rational Gram factor lies in its column range using a zero residual Gram matrix. The trace identity sums over every column at an arbitrary finite width. All reduced column quadratic values must be zero, and all rational columns therefore vanish, contradicting the identity Gram matrix. The final construction discharges the halfspace and kernel premises and negates the canonical target.

## Independent finite-data and contract checks

My own AST-whitelisted parser reads rational Lean arrays directly; it does not import the author's generator or verifier. All eight arrays match the primary JSON. Independent exact Fraction arithmetic rechecked 49 seed Gram entries, 63 local restriction entries, 21 kernel rows, 14 strict local minor bounds, seven nonzero rational minors, 147 generator coordinates, 147 QG cubic entries, all 189 strict cross terms, all 21 positive-functional values and the three positive barycentric values and identities. These checks passed. The minimum cross lower bound is approximately `0.017344504`; the approximation is only a display of an exact positive rational result.

All 25 implementation source signatures match their frozen Challenge signatures after removing whitespace/comments. The implementation's complete local import closure excludes Challenge and contains no `sorry`, `admit`, custom axiom, `unsafe`, native verification or implementation replacement. Challenge's 25 deliberately admitted reference proofs remain isolated. `comparator.json` names all 25 contracts, allows only `propext`, `Classical.choice`, `Quot.sound`, and exposes no replaceable definition holes. Source comparison is supplementary and is not reported as Comparator execution.

## Existing local execution evidence

I independently traversed the original command records, rather than accepting the archiver's PASS. All 30 compressed receipts have the recorded compressed and decompressed hashes. For all 60 modules in the complete Solution closure, I followed the 415 reuse edges back to fresh successful commands, checked source and transitive source hashes, direct dependency output hashes, archived log hashes, and the current local `.olean` output hashes. Every origin command has exit code zero and the one-thread, 4096 MiB arguments. The actual compiler binary hash and runner hash match the records. The current Mathlib and LeanCert Git revisions equal their advertised pins and their tracked source worktrees are clean.

The fresh recovery-047 `Solution` command completed successfully. Its log contains all 25 expected transitive axiom reports, each limited to the three permitted standard axioms; the exact source executes all 25 `#assert_trust kernel` commands. Earlier unrelated/failed development commands are not relabeled as successes. A separate standalone Lake build is explicitly marked unrun.

This checks the integrity and internal consistency of the retained evidence. Hashes and local logs are not an independent external attestation of process execution. I have not rerun the compiler, rechecked exported proof terms, or run Linux Comparator. Those distinctions are preserved in the candidate metadata and must remain preserved in publication claims.

## Reuse, quality, documentation and attribution

I applied `docs/lean/REVIEW.md` and the retained Tau Ceti correctness, proof-quality, generality, reuse and attribution guidance at the repository's pinned scope. This is manual adaptation, not an official Tau Ceti service run. I inspected the isolated Challenge pattern from Schiffer, the exact numerical-target pattern from Forsythe, and the repository's MI-28 aggregate pattern. The PF-03 architecture uses the relevant separation of reference statements, implementation, numerical specifications and mechanical gates without importing unrelated mathematical claims.

Pinned Mathlib searches located and confirmed the reused finite extrema, linear-map left inverse, Kummer/minimal-polynomial, matrix dot-product, finite reindexing and topology APIs. I also inspected `Geometry/Convex/Cone/DualFinite`: its introductory discussion mentions Minkowski–Weyl, but the file does not supply the required rational-coefficient representation of a real cone used here. The explicit elimination proof fills that interface. The abstract Gram-zero lemmas found in `Matrix/DotProduct` require star-order structure; the direct rational sum-of-squares argument here is transparent. The finite sum/quad bridge lemmas have actual consumers and do not introduce a parallel general-purpose quadratic-form theory.

Computation is appropriately reduced: one fixed root enclosure, rational cubic normal forms, one QG cache with proved identities, symmetry, a two-minor local kernel argument, and symbolic projection/padding instead of 54,264 facet candidates or a 444-by-444 Gram expansion. The transparent `change` steps expose the stated matrix/cast/finite-sum definitions; they do not alter the objects or invoke opaque coercion assumptions. Some frozen helper premises are stronger than needed (`hker` in C21 and the dimension premise in C23); the proof discharges them and proves a stronger underlying result, so this is not a narrowing of the final target. A future generic-library cleanup could trim them without changing this exact-contract submission.

`formalization.yaml` passes the retained pinned v0.4 schema. It credits George Stepaniants, Department of Computing and Mathematical Sciences, California Institute of Technology, for formalization and preserves Sidney Holden's mathematical and seed-data authorship and affiliation. Codex assistance, absence of source-author endorsement/human peer review, computation scope and pending external gates are disclosed. Historical draft comments are explicitly identified as frozen history rather than current execution status.

**No source change is required by this review.** Final Linux checks and metadata updates binding their actual results to the published source remain publication gates, not facts established by this report.
