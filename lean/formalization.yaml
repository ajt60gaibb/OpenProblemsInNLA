# yaml-language-server: $schema=https://raw.githubusercontent.com/mathlib-initiative/formalization.yaml/main/schema/formalization.schema.json
# The schema version is declared below. Statement lock fills source and alignment
# fields; final verification completes and validates the record. For a future
# schema, replace this file from the official upstream template. The nonempty
# template values keep the file valid before initialization and must be replaced
# with truthful project metadata.
version: "v0.4"

project:
  name: "Uninitialized Lean proof verification project"
  authors:
    - "TBD — replace during statement lock"
  responsible_maintainers:
    - "TBD — replace during statement lock"
  license: "Apache-2.0"

sources:
  - title: "Original proof files under ../proof/ — replace during statement lock"
    authors: []
    id: ""
    type: "other"
    location: "../proof/"
    relationship: "formalizes"
    license: ""
    author_endorsement: "not-contacted"

related_formalizations: []

classification:
  arxiv: []
  msc2020: []

status:
  scope: "Template not yet initialized."
  sorry_count: 0
  sorry_in_definitions: 0
  axioms: []
  main_results:
    - declaration: "ProofProject.replace_me_after_statement_lock"
      file: "Solution.lean"
      sorry_count: 0
      axioms: []
      comparator_config: "comparator.json"
      literature_dependencies: []

automation:
  methods:
    - method: "agent"
      models:
        - "TBD — record the exact Codex model during final verification"
      framework: "OpenAI Codex"
      tool_setup: "Record the statement, proof, certified computation, Comparator, and independent review actually used; omit machinery not used by this project."
      cost:
        wall_time: ""
        spend_usd: ""
        hardware: ""
      prompting_notes: "TBD — summarize any material instructions used during formalization and review."
  spend_usd: ""
  notes: ""

fidelity:
  divergences: "Not yet assessed."

review:
  status: "unchecked"
  reviewers: []
  notes: "Not yet reviewed."

alignment:
  namespace: "ProofProject"
  statements:
    - source: "Template placeholder"
      lean: "ProofProject.replace_me_after_statement_lock"
      module: "Challenge.lean / Solution.lean"
      status: "not-formalized"
      note: "Replace this entry during statement lock."

acknowledgements: "Uses Mathlib, LeanCert, leanprover/comparator, the mathlib-initiative formalization.yaml standard, and review principles adapted from Tau Ceti."
