import ProofProject.Definitions

/-!
# Trusted challenge statements

Replace the template declaration below with the exact theorem signatures from
the informal source during statement lock. Deliberate `sorry` bodies are
expected in this trusted module. The solution must reproduce the same names and
types independently in `Solution.lean`, which must not import this module.
-/

namespace ProofProject

/--
Template guard. Remove this declaration and replace it with the real locked
target declarations before `STATEMENT_AUDIT.md` may be marked `LOCKED`.
-/
theorem replace_me_after_statement_lock : False := by
  sorry

end ProofProject
