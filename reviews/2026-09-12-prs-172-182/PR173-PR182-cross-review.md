# Independent focused cross-review: PRs #173 and #182

**Verdict: PASS for both; no mathematical or statement-fidelity blocker found.**

Reviewer: OpenAI Codex AI agent `/root/audit_functions_randomized`. Date: 2026-09-12. Reviewed actual worktree heads: PR173 `af65452c26fba4223555fa302d318a2ca50457c3`; PR182 `0212a62c25e02f38a74ce9af5504e37630309a3f`. I independently read both canonical statements, all actual project Lean files (Definitions, mathematical implementation, Challenge and Solution), and Comparator configurations. I did not use supplied review verdicts as evidence. This is a bounded mathematical/source cross-review, with no tests, contributor-code execution, Git operations that mutate state, or repository edits. The integrating agent reports that live CI inputs, default-kernel replay and rejection controls are already authenticated; I did not repeat that operational audit.

## PR173 — MI-23

The formal target retains all complex positive-definite inputs in every positive dimension; arbitrary real `p≥1`, `t∈[0,1]`, and both real parameter regions `(r,s≥1)` and `(r,s≤0)` (`NLA/MI23/Definitions.lean:25–81`). Generalized means retain the exact noncommuting factor order. Powers use `CFC.rpow`, not entrywise powers or an assumed rational substitute. I also inspected the repository's retained Mathlib Rpow source: its definition applies continuous functional calculus to the nonnegative-real scalar power; the used composition, integer-power and positivity lemmas have their required invertibility/strict-positivity premises supplied.

`orderedEigenvalues` starts from the complete complex characteristic-root multiset, maps real parts and sorts decreasingly. That mapping is justified, not used to discard imaginary parts silently: `FunctionalCalculus.lean:42–81,101–139` proves real positivity, exact list length, reconstruction of the complete complex root multiset, and the genuine determinant product, first for positive-definite matrices and then for products through an explicit invertible similarity. Repeated eigenvalues retain multiplicity. `LogMajorized` requires every nonempty proper prefix inequality and equality of total products (`Definitions.lean:56–60`). Generic positivity of all means and real powers proves that these semantic obligations apply throughout the original domain, including negative exponents.

The operator norm is explicitly the norm of `Matrix.toEuclideanCLM` on complex Euclidean space (`Definitions.lean:86–91`). `SpectralNorm.lean:73–91` identifies the largest characteristic root of `X²Y²` with `||XY||₂²` by transferring its characteristic polynomial to the positive-definite Gram matrix `Y X² Y=(XY)* (XY)`. The proof uses the scoped L2 operator norm, the Hermitian spectral theorem, and the C-star norm identity. `NormBounds.lean:16–73` proves the entry lower bound and Frobenius upper bound for this same norm, using unit coordinate vectors and the Gram trace. No entrywise matrix norm has replaced the intended norm.

The actual witness has `A=D²`, `B=D T⁸ D`, with positive `D` and an explicitly verified positive LDL factorization of `T`. `Witness.lean:18–132` proves positivity, inverse factors, and all fractional-power identities via genuine CFC composition; it obtains the actual means `G=D T D` and `H=D T⁷ D` at `r=s=1,p=2,t=1/8`. `Arithmetic.lean:109–138` supplies the exact entry/Frobenius gap and connects its strict positive sign to norm separation. The gap is not a freestanding numerical certificate. `Proof.lean:47–97` transfers that separation to the first ordered eigenvalue, proves both lists have length three, invokes the actual `k=1` prefix, and negates the whole universal conjecture. One admissible witness in the first parameter region suffices to refute the universal statement over both regions.

I checked [Ghabries et al., Conjecture 2.1, p. 6](https://arxiv.org/pdf/2105.13356): the target is the corrected eigenvalue conjecture, distinct from the earlier singular-value conjecture. The primary text also discusses semidefinite inputs; this strictly positive witness refutes that broader conjecture as well as the retained positive-definite target. **PASS for the complete negative resolution.**

## PR182 — FR-12

`NLA/FR12/Definitions.lean:27–43` uses the exact real sign-matrix condition `AAᵀ=nI` and counts the subtype of individual labeled matrices. There is no normalization or quotient by signs, permutations, or equivalence classes. `Semantics.lean:25–56` proves finiteness by an injective Boolean encoding of every entry and proves equivalence to Mathlib's Hadamard predicate in positive dimension. Thus `Nat.card` is a genuine finite count, not the default cardinal of an infinite type. The conjecture retains an arbitrary positive real constant and every positive multiple of four, with actual base-two logarithm and real exponentiation. This matches [Ferber, Jain and Zhao's original counting formulation](https://arxiv.org/html/1808.07222), §1.1, Conjecture 1.4 in the earlier manuscript.

The doubling map is the actual block matrix `[A B; Pσ A, -Pσ B]`. `Doubling.lean:16–83` verifies each block, every sign and all row inner products. Cross-block inner products cancel because the same permutation appears in both lower blocks. For injectivity (`Doubling.lean:86–112`), the upper blocks recover `A` and `B` exactly, and the lower-left block recovers `σ` because distinct Hadamard rows cannot coincide (`Semantics.lean:65–77`). No unproved family size or assumed injection appears. Counting its domain gives `m! H(m)²≤H(2m)` (`Doubling.lean:121–134`). The stronger informal matching factor `(2m−1)!!` is explicitly outside the formal exports; the smaller proved family suffices.

`Growth.lean:19–98` establishes nonemptiness at every power of two from the actual order-one matrix, bounds a factorial by keeping its upper half, and proves for every `k≥0`

```
H(2^(k+2)) ≥ 2^(2^(k+2) * k*(k+1)/8).
```

The induction exponent is exact: the factorial contribution plus twice the prior exponent is precisely the next claimed exponent. The shift by two avoids truncated subtraction and ensures admissible orders. `Growth.lean:101–140` proves the logarithm identity and chooses `k>16C+2`, giving a strict violation of the bound for every proposed positive real `C`. The estimate works for every sufficiently large such `k`, so the argument also yields arbitrarily large counterexamples and refutes the asymptotic big-O claim; it is not merely one exceptional small order. The final theorem explicitly proves positivity and divisibility by four of the selected order. **PASS for the complete negative resolution.**

## Trust and limitations

All eight MI-23 and seven FR-12 Solution exports match their respective Challenge signatures and are selected by Comparator with no definition holes. Source scans found only the deliberately isolated Challenge placeholders; actual mathematical implementations and Solution imports contain no proof holes, custom axioms, unsafe evaluators or native-decision proof commands. Both allow only `propext`, `Classical.choice`, and `Quot.sound`. MI-23's rational point certificate explicitly selects kernel mode; FR-12 has no numerical certificate. This independent cross-review supports both complete-target promotions alongside the integrating agent's separately authenticated CI evidence. It does not claim a local Lean rerun, a new audit of all dependency implementations, or external human peer review.
