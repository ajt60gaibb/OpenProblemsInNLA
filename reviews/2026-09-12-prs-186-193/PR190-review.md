# PR 190 independent mathematical review

**Verdict: PASS for the stated partial results. No merge-blocking mathematical issue found.**

Reviewed head `43754370fadf404837fc2330b9cdf55ffd92fc1c` against published base `5830ed4fb06da0659414a3deb2a40ad327aca052`. This review independently read the complete 865-line manuscript `references/holden-ra14-2026-09-12/package/report.tex`, the original and amended RA-14 statements, and the relevant primary-source theorems. It does not rely on the submitted PASS report. Both worktrees remained clean; all executed package code and generated outputs were confined to scratch copies.

## Proof audit

- **Original target and model retained.** The canonical README retains the exact two-sided oracle, one product per query, arbitrary adaptive real queries, 0.99 success for every fixed matrix, orthonormal right output, and simultaneous finite-parameter objective. The statement has not been substituted or renumbered. The manuscript's measurability convention makes the probabilistic model meaningful.
- **Universal rank lower bound (lines 182–264).** The Gaussian completion lemma correctly retains a fresh rectangular Gaussian block after adaptive queries in either direction. A new row response has affine support of dimension `k-r>s`, so the row-response rank induction is valid. In the lower-bound construction, exact success on a rank-k matrix requires its row space exactly. The split according to whether `X-perp` is contained in the proposed output handles the otherwise delicate large-rank case. Conditional success is zero in both cases. Integrating over the random seed is legitimate. This proves a worst-case lower bound of k, including k near n.
- **Promised rank and global upper bounds (lines 266–311).** k Gaussian transpose queries recover the entire row space almost surely under rank at most k; extension handles rank deficiency. n column queries learn the matrix exactly. The right-sided block Krylov construction charges initial products, alternating products, and final compression: at most `k(2t+2)`. The branch argument absorbs `log(1/epsilon)` into `log n` when the Krylov expression is below n. Thus the global capped upper bound and the uniform large-rank `Theta(n)` regime follow.
- **Spectral-to-PCA reduction (lines 316–553).** The residual bound implies invertible leading overlap. Applying it to `(-F_0^T y,y)` gives the stated weighted Loewner inequality; its contraction factorization is correct. The trace comparison explicitly handles noncommuting matrices. The Fejer polynomial identity, tail estimates, monotonicity for head eigenvalues, derivative sign, and both scalar numerator cases are valid. The exact truncated-cosh certificate exceeds 10000, and the resulting `9/2000` coefficient is below `1/200`. The Ritz construction contains the proof polynomial without needing the unknown spectral scale. Its degree and final compression consume at most `k ceil(10/sqrt(epsilon))` products. The b=0 and invariant-subspace cases are covered.
- **Growing-rank lower bound (lines 558–652).** The stated conditioned deformed-Wigner theorem matches [Simchowitz–El Alaoui–Recht, Theorems 1 and 2.2 and Proposition 2.1](https://arxiv.org/html/1804.01221v2). With `g=min(4 epsilon,1/2)`, interlacing and the stronger event give the postprocessor's gap promise throughout `0<epsilon<1/2`. The stronger event is nested inside the conditioning event, so the composed success probability is at least `0.99^2`. Both oracle directions coincide on the symmetric hard inputs. The subtraction of the additional queries is explicitly absorbed by a universal lower threshold on log n. All imported dimension conditions remain polynomial in k and inverse accuracy; no finite-dimensional cap is illicitly applied to this lower bound.
- **Padding (lines 664–709).** The known-block restriction has full row rank when the residual is below its scalar eigenvalue. Taking its coefficient kernel yields an r-dimensional orthonormal extracted subspace. Completing each perpendicular tail vector into a perpendicular full vector proves the residual bound without a multiplicative loss. The bounded symmetric rank-one family in [Bakshi–Narayanan, Theorems 5.2 and 6.5](https://arxiv.org/html/2304.03191v1) has the required norm below 2 and second singular value 1. The stated sufficiently-large-dimension condition is retained. The universal upper-bound dependency was also checked in [Musco–Musco, Algorithm 2 and Theorem 10](https://arxiv.org/html/1504.05477).

## Independent execution

The submitted source was inspected before execution. Scratch root: `/private/tmp/nla-pr-190-independent-run`. Runtime: Python 3.12.14, NumPy 2.3.5, SymPy 1.14.0.

- All 18 supplied unit tests passed; log: `/private/tmp/nla-pr-190-test-rerun.log`.
- The full experiment suite reran successfully: 270 Krylov trials (including the 19 expected insufficient-depth failures), 60/60 successful promised warm starts, 20 scalar grids, and no query-budget violation. The minimum warm-start trace fraction was `0.9999999999999969`; maximum scalar coefficient divided by epsilon was `3.471373263311974e-10`. Full output: `/private/tmp/nla-pr-190-independent-run/results/validation.json`.
- Fresh checks, written without importing submission code, verified an exact noncommuting trace identity and majorization (`341208/78775 <= 61/7`), exact adaptive two-sided transcript completions with different rank-4 ranges after three queries, Fejer identities through degree 19, and the rational cosh certificate. Script: `/private/tmp/nla-ra-independent-checks.py`; results: `/private/tmp/nla-ra-independent-checks.json`.

## Scope and limits

The PASS covers the universal rank lower bound, exact promised-rank complexity, large-rank regime, deterministic warm-start reduction, lossless padding, and matching bounds under the stated polynomial dimension restriction. It does **not** establish RA-14's remaining simultaneous finite-parameter regimes, evaluate the universal exponent/threshold, certify floating-point versions of exact-arithmetic algorithms, or independently re-prove the imported literature. The canonical page and manuscript accurately retain **Partially resolved**. This is informal independent AI-agent review, not formal verification or external human peer review. Repository identity checks and CI integration are delegated to the coordinating reviewer.
