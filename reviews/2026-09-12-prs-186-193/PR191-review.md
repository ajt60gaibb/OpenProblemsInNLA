# PR 191 independent mathematical review

**Verdict: PASS for the stated partial results. No merge-blocking mathematical issue found.**

Reviewed head `e15ce4854d1a9c9fa457b93da78fca04febf200a` against published base `5830ed4fb06da0659414a3deb2a40ad327aca052`. This review independently read the complete 1237-line manuscript `references/holden-ra04-2026-09-12/src/report.tex`, the original and amended RA-04 statements, and the relevant primary-source results. Submitted audit assertions were not treated as proof. The reviewed worktree remained clean; package execution used a separate scratch copy.

## Proof audit

- **Original target retained.** The canonical diff adds a partial-results section and changes status; the original mathematical text remains intact. The manuscript uses the same rectangular matrix, Gaussian block, `m=b ceil(k/b) <= rank(A)`, b-step squared-singular-value gap, unperturbed exact-arithmetic Krylov space, projected rank-k SVD, both residual norms, and ordered right-singular-vector energy errors. It does not replace the conjecture with raw-matrix conditioning.
- **Graph transfer (lines 178–270).** Truncating an identity-head graph to its first k columns supplies a `(k,L)`-good block, including nondivisible k. The power-space identity has the correct `s+q_0-1` depth. [Chen et al., Definition 3.1, Imported Theorem 3.2, and Problem 1.1](https://arxiv.org/html/2508.06486v2) explicitly use the same projected SVD and right-singular-vector guarantee; this is a legitimate imported dependency. The generalized starting-block provenance was checked in [Meyer–Musco–Musco, Appendix F](https://arxiv.org/html/2305.02535v3).
- **Gaussian and separation facts (lines 275–366).** The square-Gaussian inverse estimate follows from row distances and a union bound. A closed relative window cannot contain b+1 leading eigenvalues because its endpoint ratio is strictly greater than `1-Delta`. Filling that deterministic window to b indices retains the far-node bounds. The residue-class Vandermonde witness proves generic head invertibility even with admissible repeated nodes. Sorted subsets inherit the b-step gap.
- **Scalar-filter bound (lines 375–449).** Each head column is isolated by a degree `m-b` scalar polynomial and a vector orthogonal to the other b-1 selected Gaussian rows. The normalization scalar is conditionally standard Gaussian; no independence among different columns is claimed. The two failure budgets and Frobenius estimate give the displayed graph bound, then the larger general iteration term.
- **Recursive fractional-moment bound (lines 473–661).** Deleting a row leaves a one-dimensional polynomial nullspace depending only on other rows. Evaluation at its missing node is nonzero because the far-node subproblem is invertible. Polynomial division gives the exact recursive identity. The truncated half-moment argument correctly conditions before integrating the missing Gaussian row, enlarges the truncation event, and bounds the subproblem norm and its Gaussian entries together; it never assumes their independence. The recurrence, chi-square tail, Markov threshold, and logarithmic simplification are valid. Translation improves relative gaps; a union bound at t interior Chebyshev nodes and discrete cosine interpolation control the entire closed tail interval. The separate tail event completes the requested failure probability. This establishes the all-input extra-`t log m` result, with the full requested order when `Delta <= 1/m`.
- **Closed regimes (lines 685–909).** Exactly b-fold clusters admit exact block Lagrange isolation, including repetitions of the last head level in the tail. The displayed inverse-block union bound gives the claimed graph size. b=1 and b=k follow as stated. For t=2, the leave-one-out linear polynomial has the displayed single-slope formula; all three failure events have budget delta/3. The unsimplified bound is below the stated `4000 n^12/(delta^6 Delta^2)`. If rank(A)=m, the powers from 1 through t eliminate the zero tail and span range(A), proving exact optimal truncation at depth t+1 and the component energies, without division by a zero tail singular value.
- **Auxiliary obstruction and remaining task (lines 918–1102).** The fixed-gap raw monomial family has the correct exact test-vector bound and convergence in probability. It is explicitly distinguished from the algorithmic conjecture. The sufficient interpolation estimate (IE) is identified as unproved, not assumed in a theorem. The failed matrix-product interpolation example is algebraically correct. The cited nonsymmetric global bilinear small-ball premise is present in [Nie, Theorem 1.6](https://arxiv.org/html/2111.05553v2); [Shao's v2 primary manuscript](https://arxiv.org/pdf/2507.10144) indeed leaves its random structural factors conjectural. Neither auxiliary citation is used to supply the missing proof.

## Independent execution

All submitted scripts were inspected before execution. Scratch root: `/private/tmp/nla-pr-191-independent-run`. Runtime: Python 3.12.14, NumPy 2.3.5, SymPy 1.14.0, mpmath 1.3.0; threadpoolctl 3.6.0 was installed only in `/private/tmp/nla-review-ra-deps`.

- Seven supplied exact symbolic groups passed, including six t=2 identities and 23 recursive identities. Results: `/private/tmp/nla-pr-191-independent-run/rerun/exact.json`; log: `/private/tmp/nla-pr-191-exact-rerun.log`.
- The full numerical suite completed: 117 approximation rows, 50 t=2 bound rows, 12 high-precision graph probes, and eight raw-conditioning rows. The maximum observed t=2 column-bound ratio was `0.5138122716462756`. The depth summary matches the recorded conclusions, including the exact-rank floating-point fixture that needs tested depth 8 for its numerical tolerance although the exact theorem gives depth 7. Results: `/private/tmp/nla-pr-191-independent-run/rerun/numerical_summary.json`; log: `/private/tmp/nla-pr-191-numerical-rerun.log`.
- Fresh exact checks imported no submission code. Three new rational fixtures with `(b,t)=(2,4),(3,3),(2,3)` checked 23 recursive columns, tangential interpolation, inherited gaps, translation, and exact zero-tail recovery. They include repeated nodes, nonaligned clusters, and a head spectral dynamic range of `10^18`. Script: `/private/tmp/nla-ra-independent-checks.py`; results: `/private/tmp/nla-ra-independent-checks.json`.

## Scope and limits

The PASS covers the two weaker all-input bounds and the requested iteration order in the specified small-gap, repeated-cluster, t=2, endpoint, and exact-rank regimes. The general growing-b, growing-t constant-gap/nonzero-width-cluster case remains unresolved. The canonical README and manuscript accurately state **Partially resolved** and preserve the original target and count treatment. Finite checks do not certify universal probability inequalities or exact arithmetic, and this audit does not re-prove the imported convergence theorem. This is informal independent AI-agent review, not formal verification or external human peer review. Repository identity checks and CI integration are delegated to the coordinating reviewer.
