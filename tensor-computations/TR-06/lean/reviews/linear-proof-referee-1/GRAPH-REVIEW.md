# Independent proof review: graph-Jacobian linear inequalities

Reviewer: `/root/tr06_statement_referee_1`, AI agent, nonauthor of GraphJacobian.lean. Phase: post-proof auxiliary contribution review. Date: 2026-09-24. The pre-proof approval was supplied by root; I independently checked both that boundary and the implementation.

**Verdict: APPROVE for integration.** No substantive changes requested. This covers only the two linear graph inequalities, not nonlinear graph-volume finiteness or complete TR-06.

Final source SHA256 `c1d34a779cbd1d99987cdcf28090298c7e2745745a32be12b2787f1d02f6173c`. Root-approved draft SHA256 `78c9b8dd1fbdcf6368563de240ed3d45641f0a66217d4d02dcf8bebe225ced3b`. Frozen common Definitions SHA256 `e5fe5314f4c17df41b5bec54f2c407b2d1eba8ba59be5d85525de13e262f8898`. Snapshots are under source/. I read every source proof and definition, the approved draft, plan, approval record and author evidence. All three map-definition blocks are byte-identical to the approved draft; source-audit.json records the check.

pairedL2Map is the actual pair (A x,B x) in WithLp 2 (F×G). I checked the pinned ProdL2 inner-product instance and proved the reviewer-owned identity that its squared norm is ‖A x‖²+‖B x‖². Thus no sup-norm product is substituted for the intended Hilbert geometry. rangeInverseOperator restricts A to its actual induced-norm range before inversion. The reviewer-owned specialization rangeInverse_eq_induced proves by rfl that this operator is exactly the frozen inducedDerivative when specialized to Tensor and AngularOutput. No arbitrary inverse on the full ambient codomain is used.

The first theorem is exactly ‖T‖≤normDet(graphL2Map T), for arbitrary finite-dimensional real inner-product E,F with no dimension lower bound. Its private Gram identity computes entries in an orthonormal eigenbasis of T* T. The adjoint identities and symmetry put the Gram matrix into diagonal form 1+lambda_i with lambda_i≥0. Its determinant is the product of those factors. The product bounds each lambda_i because all factors are at least one, then the quadratic eigenbasis expansion bounds ‖T x‖² by D²‖x‖². The proof establishes nonnegativity before converting square inequalities to norm inequalities and applies the genuine continuous-linear operator bound. No numerical certificate, eigenvalue approximation, or conclusion hidden in a hypothesis occurs.

The argument also handles E of dimension zero: the eigenbasis index is empty, the product is one and every quadratic sum is zero. It never selects an eigenvalue or a nonzero vector. The source/ReviewBoundary.lean additionally specializes the theorem to EuclideanSpace ℝ (Fin 0), with no added premise. Zero-dimensional output or auxiliary G causes no problem; in the second theorem injectivity of A is the explicit genuine hypothesis that constrains possible dimensions.

For the second theorem, the paired map is factored exactly into A restricted to range(A), the graph of its actual range-inverse operator, and the L2 product inclusion range(A)×G→F×G. That inclusion is proved a linear isometry. The private isometry-composition lemma uses normDet_comp restricted to the actual intermediate range. Equal finite dimensions of the range equivalence justify normDet_comp_of_finrank_eq; no false unrestricted rectangular multiplicativity is used. I read these pinned normDet composition/codomain restriction lemmas. NormDet_codRestrict keeps A's original Jacobian. Multiplication by this nonnegative Jacobian preserves the first inequality, giving exactly the approved second statement.

The local unused-section-variable linter setting is a style setting for an intentionally retained finite-dimensional interface. It introduces no axiom, oracle or trust bypass. The theorem is independent of any chart smoothness or geometric volume assumption. Its subsequent use for angular mean still requires the nonlinear correspondence and graph-volume results.

Attribution includes George Stepaniants and the requested Department of Computing and Mathematical Sciences, California Institute of Technology affiliation, no contact email, and Matthew J. Colbrook's original proof credit. Existing Mathlib spectral/Gram/normDet APIs are reused. The documentation truthfully distinguishes these linear inequalities from complete verification.

Independent verification: the reviewer built frozen Definitions, AlgebraFiber and GraphJacobian into a fresh isolated build using pinned Lean 4.33.1, pinned Mathlib 0df444a360eaa60ab8c11dca51a86af692955474 and LeanCert 621a43d7cf21f87872392a01e874f2f1dbddc926. No author-produced proof olean was used. GraphJacobian-receipt.json records command, environment and hashes. logs/GraphJacobian.log has exit zero and no warnings/errors. Both exported closures are exactly [propext, Classical.choice, Quot.sound], including the private helpers, and both #assert_trust kernel checks pass. The independent norm-geometry, frozen-definition correspondence and dimension-zero probes also compile and pass kernel assertions. No sorry, admit, custom axiom, native_decide, unsafe or Challenge import appears.

Limits: these are independent macOS source reruns against pinned dependency caches, not fresh dependency builds, Linux replay or Lean4 Comparator. This approval cannot promote TR-06 or discharge its generic-locus, bounded-graph-volume, radial-integration or finite-expectation targets.
