# Exact-dimensional polynomial Segre boundary for pre-proof review

Author: /root/tr06_statement_referee_1 (AI agent), George Stepaniants formalization credit with Department of Computing and Mathematical Sciences, California Institute of Technology affiliation. Original TR-06 proof attribution remains Matthew J. Colbrook.

The exact proposed definitions and six universal propositions are in NLA/TR06/SegreBoundary.lean. They contain no proof bodies, sorry/admit placeholders, axioms or assertion of verification. The frozen Definitions and existing real RankOneCharts are unchanged. This file imports the newly implemented Complexification supporting module and uses a separate NLA.TR06.Segre namespace.

For a prescribed pivot pattern q0:Fin r→TensorIndex d n, Coordinate q0 is the dependent sum over summands of Option of the mode/non-anchor-index pairs. None is its amplitude, while some(j,u) is the factor coordinate in mode j away from q0_i,j. Parameter k q0 is the genuine EuclideanSpace over that finite coordinate type. It is not a function-space sup norm and not a redundant raw factor-vector domain.

The decoded factor equals one at its pivot and the free coordinate elsewhere. The decoded tensor summand is its amplitude times the pureTensor of those normalized factors. sum is the actual finite sum of these tensors. Each tensor coordinate has an explicit multivariate polynomial over the chosen RCLike field; pullback is ordinary polynomial substitution along those tensor-sum coordinate polynomials. amplitudesPolynomial is the product of the amplitude variables.

Proposed six obligations, to be approved before implementation:

1. DimensionStatement: for EVERY pivot pattern, card(Coordinate q0)=expectedDimension d n r, and the real and complex module finranks of Parameter equal that same number. No format positivity is assumed; if a pivot pattern cannot exist the corresponding universal assertion has no instance. The equality remains valid for r=0.
2. EvaluationStatement: for every RCLike field, the explicitly constructed summand and sum polynomials evaluate to the actual decoded tensors, and polynomial substitution evaluates to p of the actual sum.
3. DecompositionStatement: for 0<d and nonzero amplitudes, the decoded tuple is an actual nonzero-rank-one decomposition of its sum, and each summand's pivot entry equals its amplitude. Positive mode count is essential to absorb an arbitrary amplitude into one factor; it follows from the final problem's d>=3. No n_j>=2 is needed for this supporting statement.
4. RepresentationStatement: for every RCLike field, an actual rank-one tuple whose chosen pivot entries are nonzero is exactly represented by a parameter with all amplitudes nonzero. This is pointwise equality of actual tensors, with no factor scaling equivalence substituted for it.
5. NonzeroPullbackStatement: given ANY complex length-r actual decomposition a of A with p(A)!=0, pullback q0 p is nonzero for EVERY prescribed pivot pattern q0, even if the supplied witness has zero entries at that pattern. Its product with amplitudesPolynomial is also nonzero. Neither exact rank nor generic identifiability is a premise.
6. RealWitnessStatement: under the same complex decomposition witness, and 0<d, for EVERY prescribed pivot pattern there is a real Euclidean parameter with all amplitudes nonzero, an actual real decoded decomposition, and p(complexify(actual sum))!=0. No universal real/complex rank equality or real exactness is claimed. Exactness/identifiability later require the separately proved conditional complexification bridge and a generic certificate.

Bounded implementation split: first coordinate dimension, actual polynomial evaluation and scalar-general normalized-pivot reconstruction/decomposition; second the nonzero-pullback theorem; third the real-witness theorem. The first part may adapt the already proved elementary real pivot reconstruction, with scalar-general field identities, without changing the frozen real chart declarations.

For nonzero pullback, one viable elementary route uses a redundant raw-factor polynomial only as an AUXILIARY existence argument: compose p with the sum of pure tensors of symbolic factors. The original decomposition factors witness this polynomial nonzero. Multiply by all prescribed factor-pivot variables; the product remains nonzero in the complex polynomial domain, so some complex evaluation makes every chosen factor pivot and p(sum) nonzero. Normalize those factors to obtain a parameter in the exact-dimensional coordinate space, and use EvaluationStatement to witness pullback q0 p nonzero. This auxiliary factor-space argument supplies no measure or dimension conclusion. All null-locus and Hausdorff measure arguments must use Coordinate q0 of dimension k. If the reviewer prefers to avoid raw auxiliary factor polynomials entirely, complex factor perturbation plus the same normalization proves the identical statement.

The real-witness proof uses the nonzero product amplitudesPolynomial*pullback. A nonzero complex polynomial does not vanish at every real coordinate tuple, obtainable either algebraically using the infinite real subfield or from the proved real polynomial-nullity lemma plus nonzero Euclidean volume after finite reindexing. At a real nonzero evaluation, the amplitude product and pullback are separately nonzero. Scalar inclusion commutes with the decoded tensor formulas, so the sum evaluation is exactly the stated complexify expression. No positivity of amplitudes is needed or claimed.

These statements establish a nonvacuous polynomial parametrization in the correct dimension. They do not prove the generic addition certificate, regularity of addition, properness, exact-rank status of the new real point, relative tensor-locus nullity, finite graph volume, radial integrability or complete TR-06. Further compatibility between this flattened Euclidean parametrization and the existing real OrderedPivotData packaging can be stated separately if needed; neither representation changes the actual tensor sum.
