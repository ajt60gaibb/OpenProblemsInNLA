# TR-27 independent correctness review: Linux completion

Date: 2026-09-22. Reviewer: `/root/tr27_final_correctness`, the same fresh OpenAI Codex AI agent who wrote the [source-level correctness review](final-correctness-referee.md). I authored no TR-27 definitions or proof, package configuration, Linux verifier, or operator record. The mechanical operator authored ProjectiveGeometry and is correctly excluded from independent final-referee credit. This is independent AI review, not external human peer review or official Tau Ceti endorsement.

**Verdict: APPROVE the completed verification gate for the full original TR-27 negative answer and the exact proposed canonical notice.** No mathematical or mechanical defect remains from this review. The earlier source report and receipt remain unchanged historical records; this addendum closes their explicitly pending Linux gate. The [completion receipt](final-correctness-evidence/linux-completion-receipt.json) binds the reviewed evidence and notice bytes.

## Source continuity and exact scope

I independently read all 116 project blobs from publication commit `775e8b169119c4045b07db7666eda8c001ae3bd1` using Git and recomputed their SHA256 hashes. They equal the input receipt, retained source directory, transferred input archive and successful verifier result exactly. Every file bound by my prior source-review receipt is unchanged. All thirteen NLA modules (Definitions and twelve proof modules), Solution, the frozen mathematical boundary, dependency configuration and Comparator configuration retain the reviewed bytes.

The guest result identifies the distinct verification-only commit `59f8e715525a25cce2e4767a1372a94650c8071c`. I checked the explicit provenance relation and identical per-file hashes; I have not mislabeled that commit as the publication commit. The guest preparation contains no prior project build artifacts and records a clean checkout. The successful unchanged shared harness verifies source hashes before and after dependency preparation and the Comparator run.

The checked target remains the complete canonical universal implication, refuted by a fully admissible witness with projective rank three, border rank at most two and separate-factor Segre-square rank nine. My earlier full source review remains applicable: all geometric/image/rank bridges are proved, whole-variety decompositions permit arbitrary complex coefficients, repetition, zero terms and infinity, and the two tensor factors are independent. Smoothness, exact border rank two, longer finite delays and eventual-power strengthenings remain outside the Lean claim.

## Actual Linux evidence inspected

I read the complete Comparator log and the individual sandbox, raw-kernel, Comparator-regression, forbidden-sorry and forbidden-native logs. I also inspected dependency and cache logs, the user-service result, execution and preparation receipts, actual tool/dependency identities, the authentic LeanCert assertion implementation, and the finalized operational record. My review is of this actual operator-run evidence; I did not personally start or rerun the Linux verifier.

The full run occurred as non-root UID 1000 on aarch64 Linux with Lean 4.33.1, compiler commit `819816b2e0a3bf405af45ae5c7af2491d8f5bee6`, from 19:24:08 to 19:26:34 UTC on 2026-09-22. It exited zero. Comparator built and exported Challenge before building the proof modules and Solution. There were exactly 25 intentional Challenge placeholder warnings and no Solution warning or error. Both export lines list exactly the same 25 frozen configured declarations, including `NLA.TR27.projective_counterexample` and `NLA.TR27.original_conjecture_false`. This is not an auxiliary-only comparison.

I independently parsed both export lists and all 25 explicit Solution axiom reports. Every report contains exactly `propext`, `Classical.choice`, and `Quot.sound`. The log then records `Running Lean default kernel on solution.`, `Lean default kernel accepts the solution`, `Your solution is okay!`, and `EXIT_STATUS=0`. The successful result binds the same target list, input hashes and checker receipt. Raw intermediate export streams were removed by the pinned Comparator and are not retained; the evidence is the authentic completed comparison/replay and its full logged export lists, not invented raw-stream hashes.

## LeanCert and dependency authentication

The dependency log materializes all ten exact manifest revisions, and the read-only dependency-capture script compares their actual Git HEADs with those revisions. I inspected that script and checked its receipt against the manifest. LeanCert is `621a43d7cf21f87872392a01e874f2f1dbddc926`; Mathlib is `0df444a360eaa60ab8c11dca51a86af692955474`.

The retained actual `LeanCert/Tactic/Verification.lean` has SHA256 `2c576708b528acdde17796b0b715b83079f9cad377182dbd87c248323ff0a58c` and is recorded as byte-equal to its pinned Git blob. I inspected its `#assert_trust` implementation: it collects transitive axioms, rejects sorry/custom axioms, and rejects native compiler axioms in kernel mode. It is an effective check, not a local no-op replacement. The Linux log builds this module and Solution successfully; Solution contains exactly one `#assert_trust kernel` and one `#print axioms` for each of the 25 selected declarations. Successful assertions are silent, so their execution is established by those authenticated source bytes and successful compilation, not by claiming nonexistent success messages.

The shared harness, launcher and source-lock bytes match the publication repository. The prior bootstrap receipt authenticates the checker/exporter/Landrun binaries and runtime adapter; the preflight validates them again. Its older kernel-7.0.0-30 platform field describes bootstrap, while the current run is separately recorded on 7.0.0-31. Mathlib's cache utility decompressed 8,689 already-cached dependency files. The project was freshly built, but this is not a claim that every dependency object or verifier binary was freshly rebuilt from source. Default-kernel replay and the authenticated trust checks remain separate from that cache path.

## Real controls and operational qualifications

Both build and export sandbox modes ran as UID 1000 and passed the actual namespace, host-process isolation, network/AF_UNIX, capability and no-new-privileges checks. Writes, truncation, creation and symlink escape attempts outside the permitted area were denied; build writes inside the designated .lake area were allowed, while export writes there were denied. All four unsupported/widened adapter-option tests returned the required exit code two. The nested namespace attempt was launched and rejected at UID-map setup; this proves the recorded rejection, not successful entry into an inner namespace.

The raw-kernel controls accepted the honest inductive/quotient fixture, rejected a proof at the wrong type, and rejected a forged quotient at the post-check. All five Comparator regression fixtures had their specified outcomes; the historically named simple_kind_mismatch fixture actually exercises an illegal helper axiom, as the operational report accurately states. The separate forbidden-axiom fixtures failed with exit one specifically for `sorryAx` and `checked._native.native_decide.ax_1_1`. These expected failures are successful controls, not failed TR-27 proof runs.

The recorded AppArmor user-namespace prerequisite was restored before verification under the unchanged documented shared-CI policy. No proof, target, sandbox assertion or checker was weakened. The transient service's post-completion default properties are not presented as live-limit measurements; its lifecycle record explains that the unit had unloaded. The operator stopped the VM after preserving the evidence. These operational details do not expand the mathematical claim.

## Independent audits, publication notice, and limits

I wrote and executed [check-linux-inputs.py](final-correctness-evidence/check-linux-inputs.py), which independently compares all publication Git blobs, prior-review continuity, export names, trust assertions, axiom outputs, shared-driver bytes and dependency records. Its [retained output](final-correctness-evidence/linux-input-check.log) passes. After reading the operator's evidence-audit source, I personally reran that audit against the archives and logs; its [retained output](final-correctness-evidence/linux-recorded-evidence-audit.log) also passes. These are independent evidence audits, not a second Linux proof run.

I inspected the final [operational report](../verification/linux/OPERATIONAL-REVIEW.md), SHA256 `15bad3fdd90115149b76d2f30e207d164ca65f5d5e74547031ffa38e54595ab2`, and found its claims consistent with the actual logs, including their limits. The exact [proposed canonical notice](final-correctness-evidence/proposed-canonical-notice.md) correctly describes the complete negative answer, immutable proof/declaration links, actual verification, attribution, AI review and omitted optional results. It preserves George Stepaniants's requested affiliation and Colbrook's original mathematical credit, adds no contact email, and makes no human-peer-review or official Tau Ceti claim. I approve that notice for publication once the other independent completion review is also approved.

Current README/metadata status updates and this addendum were not inputs to the completed Linux run. They may document its result without changing the verified mathematical files or rewriting the retained historical source snapshot. Subsequent mathematical changes require renewed correspondence and verification review. The forthcoming documentation-only publication delta can be checked separately; it does not require pretending the completed run covered later prose.
