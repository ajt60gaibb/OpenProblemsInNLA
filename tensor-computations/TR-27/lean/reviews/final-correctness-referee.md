# TR-27 independent final proof, reuse, and attribution review

Reviewer: `/root/tr27_final_correctness`, a fresh OpenAI Codex AI agent. I authored none of the mathematical definitions, statement boundary, proof modules, packaging, or verification harness. This is an independent AI review, not external human peer review or an official Tau Ceti review or endorsement. The protocol is the adapted ten-angle protocol in `docs/lean/REVIEW.md`.

Date: 2026-09-22. Phase: final mathematical source, reuse/API, documentation, and attribution review.

**Verdict: APPROVE the complete mathematical source and current publication packaging at the SHA256 hashes in [source-review-receipt.json](final-correctness-evidence/source-review-receipt.json). No mathematical changes are requested. Authoritative Linux verification remains a separate pending gate.** This verdict alone does not authorize calling the canonical problem Lean verified.

## Scope and mathematical path actually inspected

I read the complete canonical TR-27 README and complete original Colbrook LaTeX manuscript, the frozen Definitions and all 25 Challenge declarations, NUMERICAL_TARGETS, all thirteen NLA modules, including Definitions and Counterexample, the API reconnaissance, package metadata, and earlier statement/module review findings. I did not infer correctness from author PASS reports.

The construction retains the exact twelve coordinates indexed by 0,2,...,12, all complex parameters and the infinity direction, and the unrestricted complex coefficients of the original problem. The computed quotient is a surjective complex linear map with precisely the specified one-dimensional kernel. Its three-term identity and the divided-difference extension use exact arithmetic. No interval calculation or approximate search is needed or inserted.

The full-image argument is substantive. The first parameter's twelfth power is integral by an explicit monic quadratic. The mixed monomial and second twelfth power are then integral; positive-power integrality and polynomial induction give integrality of the entire substitution. The noninjective lying-over theorem is used with the required kernel containment, and the complex Nullstellensatz produces an actual two-coordinate preimage of every point in the whole zero locus. The chart split and complex twelfth roots identify the entire range with the full scalar cone, including zero and infinity. There is no dense-subset substitution, unproved closure equality, or excluded output chart.

The homogeneous-kernel proof separates degrees 12*n and derives primality from the parameter polynomial domain. Nondegeneracy follows from a thirteen-point Vandermonde source basis and the proved surjectivity. Reducedness and classical closedness/irreducibility are proved through ordinary homogeneous ideals and the point-level Nullstellensatz argument. The classical geometry predicate contains no rank premise.

The tangent polynomial handles repeated parameters, zero, infinity and empty lists. Its first coefficient is nonzero; the degree bound becomes one smaller when infinity occurs, so the coefficient functional annihilates infinity as well. Adding the three fixed points would turn an eight-term center expression into a forbidden eleven-term tangent expression. Kernel-disjointness therefore preserves independence. The final independence theorem applies to the entire zero locus via the proved whole-image equality and actual Projectivization representatives.

The rank-three proof uses a dual functional on the union of the three fixed parameters and any two proposed parameters. At least one fixed parameter is absent from the short expression; its coordinate functional gives values one and zero on the target. Zero cone vectors are absorbed into zero scalars. The nine-term upper bound expands the actual tensor product.

For the square lower bound, the code collects distinct left and right parameters into separate finite sets. Independent coordinate functionals exist on each set. Contractions with a functional taking value one on the witness place the witness in each span. Every expansion has at least three nonzero coefficients by the rank-two exclusion. Pair functionals show that every pair from the Cartesian product of these supports occurs in the proposed decomposition, contradicting its at-most-eight length. This covers arbitrary complex coefficients, repetitions, cancellation, zero terms, infinity, and unrelated factors. It does not impose symmetry or merge tensor modes.

The semantic chain proves representative-scale invariance and equivalence of cone expressions with actual projective spans. Genuine finite decompositions prove the rank-length sets nonempty before infima are used as minima. The affine/projective border bridge uses all polynomial equations and all homogeneous components. Its reverse direction multiplies by a coordinate nonzero at the target, handling the cone origin and rank-zero boundary correctly. The final Counterexample module derives projective ranks three and nine and border rank at most two from these bridges, then contradicts exactly the frozen universal implication. No admissibility or lower-bound hypothesis is supplied from outside the constructed proof.

## Reuse, generality, and API quality

I searched the supplied extracted Mathlib source and inspected the actual relevant APIs, including Projectivization span/independence, homogeneous substitution, finite spanning coefficients, linear functional extension, polynomial extensionality, integral extension lying-over, Nullstellensatz, and independence under a map with disjoint kernel. The code reuses these APIs. Bounded searches did not locate existing complete projective-rank, Segre-rank, or affine/projective rank-locus closure bridges for these point representations. The local wrappers are appropriate; no unsupported scheme-level theorem is substituted for classical complex-point geometry.

The reusable semantic statements quantify over arbitrary complex vector spaces with finite-coordinate presentations. The witness-specific finite dimensions and exact numerical data are appropriately confined to the concrete modules. Names and module divisions identify the semantic, image, independence, border, rank and final-assembly steps. Exact ring/norm_num proofs avoid unnecessary numerical certification. The broad Mathlib.Tactic import in Algebra is a nonblocking build/maintenance cost; narrowing it is not needed for correctness.

The extracted Mathlib directory has no Git metadata; the source search does not independently authenticate its revision. The manifest pins Lean 4.33.1, LeanCert 621a43d7cf21f87872392a01e874f2f1dbddc926 and Mathlib 0df444a360eaa60ab8c11dca51a86af692955474. Dependency authentication and Linux kernel replay remain distinct mechanical gates.

## Independent checks and trust scope

I personally rebuilt Definitions and every implementation module in an isolated external output tree using the supplied Lean 4.33.1 macOS executable and existing package cache. All compiled with exit zero. I independently generated an audit importing Counterexample without Challenge and printed the transitive axioms for every one of the 25 required declarations. Each closure was exactly `propext`, `Classical.choice`, `Quot.sound`. Source searches found no proof placeholder, custom axiom, native decision shortcut, unsafe implementation, extern override, or Challenge import in the implementation modules.

The four mathematical boundary hashes (Definitions, Challenge, NUMERICAL_TARGETS, comparator.json) matched the preproof statement-freeze receipt. The retained Challenge intentionally contains 25 specification placeholders; those are not proof evidence and are not imported by the Solution proof path.

Earlier findings were checked rather than silently dropped: the metadata role correction concerned the draft schema, not the mathematics; the earlier geometry review's first failed audit omitted explicit theorem arguments, while the underlying modules compiled and my independent rebuild also passed. No unresolved substantive mathematical finding was identified.

These local executions do not constitute authoritative Linux Comparator replay. Only logs actually inspected in the final evidence section may be claimed. The final metadata, attribution and byte binding have now been inspected as detailed below; the authoritative Linux gate remains pending.

## Final packaging, attribution, and preserved prior records

The canonical package is `tensor-computations/TR-27/lean` in publication worktree `/private/tmp/nla-tr27-publication-20260922`, based on `daf313133bfe730c32a266ea85cd9ca0fbe2d5ed`. Every mathematical module is byte-identical to the source I read and independently rebuilt. The machine-readable receipt binds all thirteen modules, the frozen Challenge/specification/configuration, Solution, toolchain/dependency configuration, README, metadata, license, statement receipt, and the complete local author log. It also binds my independent execution logs and the original problem/manuscript bytes. The original canonical README, manuscript and permanent problem registry are unchanged relative to the campaign base.

I inspected Solution: it imports Counterexample and the actual LeanCert verification module, sets kernel trust, and includes one kernel assertion and one transitive-axiom print for each of the 25 selected declarations. It imports no Challenge. The Lake configuration has separate Challenge and Solution libraries and defaults to Solution; the Comparator selects all 25 reviewed statements, permits only the three standard axioms and has no definition holes. I did not personally compile the LeanCert wrapper; its actual execution is explicitly left to the pending Linux gate.

Formalization authorship is George Stepaniants, Department of Computing and Mathematical Sciences, California Institute of Technology, without a newly added contact email. The original construction and mathematical proof remain attributed to Matthew J. Colbrook. The Apache-2.0 license is retained. AI assistance and individual author/reviewer roles are disclosed, and authors are not counted as independent final referees. Optional source results (smoothness, exact border rank two, arbitrary finite delay, eventual saving) remain explicitly unclaimed. The complete original problem and source are retained and linked, including immutable source URLs in metadata.

Three documentation comments raised during this review have been resolved on the bytes bound by the receipt: the automation notes now list the completed module authorship; local elaboration is distinguished from the pending execution of Solution's LeanCert assertions; and arbitrary complex decomposition coefficients/variable parameters are distinguished from the fixed witness data. The README explains historical draft/unreviewed wording inside frozen files without rewriting the preproof boundary.

I inspected the retained complete-local-development log as supplementary evidence, but based the local check claims above on my own runs. The only logs personally executed or inspected for this source-level verdict are the three independent module-build logs and independent all-25 axiom log under `final-correctness-evidence`, plus `reviews/complete-local-development.log`. Earlier review reports were read for their findings; their claimed execution results were not promoted into a new Linux verification claim. The pinned-source API search and retained Forsythe Comparator regression example informed reuse assessment, not a claim to have run their control suites.

## Remaining mechanical gate

A successful fresh Linux Solution build, execution of all kernel-trust assertions, all-25 sandboxed Comparator statement/axiom checking, default-kernel replay, dependency authentication and required positive/negative controls remain mandatory. No such successful TR-27 Linux log existed for this source-level verdict, and none has been claimed. A separately dated addendum must bind and inspect those logs before reporting gate completion. This approval is not an official Tau Ceti endorsement or human peer review.
