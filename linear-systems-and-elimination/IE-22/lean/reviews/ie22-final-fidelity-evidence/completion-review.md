# IE-22 independent final fidelity referee — Linux completion

**Verdict: APPROVE the completed Linux verification gate for the exact previously source-approved IE-22 package.** No unresolved mathematical, scope, trust, or evidence-binding finding remains in this review. This approval is not a publication-diff review or a claim that I personally executed the Linux verifier.

Reviewer: `/root/ie22_final_fidelity`, an AI agent and independent nonauthor of the IE-22 boundary, proofs, vendored IE-21 proofs, packaging, and Linux execution. Date: 2026-09-22 UTC. The adapted Tau Ceti protocol is `docs/lean/REVIEW.md`; no external endorsement is claimed. My earlier full mathematical/source review, fresh independent 47-module local build and 20 exact complete-signature/transitive-axiom checks remain in `source-review.md` and `source-receipt.json`. Their immutable archived hashes are respectively `7d66594e418a608bcd7553409f82a54956e72894c63d211890e3b16eb6f8af87` and `a134f04cac877be3751aad66b1cb36a4f9f6d45017f360151d44ce16696544d2`.

## Exact approval boundary

- Publication input commit: `c45f9ccef20a2fa5cc4b988e93d4173dab853362`, with verified parent `19d9f19bd2b779caf5f3b40540e9a303a1226a5c` and ancestor base `daf313133bfe730c32a266ea85cd9ca0fbe2d5ed`.
- Actual guest verification-only commit: `3ee64b33ae1c626b3c7ffa1c4e561b6055623bb9`. It is explicitly distinct from the publication commit, while all 394 project bytes agree.
- Actual full run: `verify-20260922T221554Z-991`, 2026-09-22 22:15:54.403252 through 22:18:53.022964 UTC; final verifier exit 0.
- Frozen operator report SHA256: `5e7854c7ce6801da27aa87648fa0209adfaec427fcbef69b0ec9c89a3fb8c0cf`.
- Frozen 455-file evidence manifest SHA256: `a26606cc445414a8532aa1337ca5df6d0cb047dd1b42e416afed9fd8cac2a9e7`.
- Frozen 57-file core package manifest SHA256: `2a07dedc9ca3774cd9fc5a760f421dd47f105cc257c770f401ae73513dea1226`.
- Actual Comparator log SHA256: `723927d41cd514bd4a9ae73270350fc94dd7d9617f1d6f6cfeb039f486f95fb1`; actual result JSON SHA256: `a697f0e7b2c974bab5e9fc119622eda25b16d2d694560e600729dd8a3112b2d5`.

## Independently inspected evidence and checks

I read the raw Comparator, dependency, Mathlib-cache, sandbox, kernel-control, Comparator-regression, forbidden-sorry and forbidden-native logs, the operator report, transport/preparation/execution/dependency/export/lifecycle records, the scripts producing those records, all of the shared driver, strict adapter and runtime sandbox probe, the actual retained LeanCert verification module, and the relevant source-locked Comparator comparison, axiom and kernel-replay implementation and regression fixtures. I checked the actual success and rejection phases, rather than accepting the operator's PASS prose or names of controls.

My own `completion-audit2.py` independently reads publication Git objects, regenerates `input.tar` byte for byte, checks all 394 committed/archive/guest-snapshot/result source bindings, checks all 455 manifest members before and after, checks archive paths/types/unique members, compares all 431 retained evidence-archive files byte for byte, and rechecks all 57 frozen core files against the present package. It also authenticates the archived shared driver against the publication commit, the consulted retained Comparator sources against the current source lock, the runtime adapter against that lock, and the actual probe/environment against bootstrap receipts. The manifest's exact file set is complete. Both archives and all source/evidence bindings passed; no compiled project artifact or `.lake` input is present.

The input archive SHA256 is `eb5dc854bca9aa8025c0db4af0f3e0538ffba4d262b68dcaf08616dbb62d6931`; the evidence archive SHA256 is `88b84a1785e98114095ab9f74e2ecc4ad6f875f3a812ca6f197c93d62fef04b2`. The archived source is therefore exactly the package whose complete canonical correspondence I previously approved, including all mathematical quantifiers and the original manuscript target; there is no mathematical substitution in this verification run.

The actual Comparator log contains 47 distinct fresh `Built NLA...` modules, exactly the frozen 31 IE-21 and 16 IE-22 modules. Challenge is built and exported before Solution is built. Both exports contain the exact same ordered 20 selected declarations. The only warnings are the 20 deliberate Challenge placeholders. Authentic `LeanCert.Tactic.Verification` and Solution are freshly built. All 20 wrapper assertions are `#assert_trust kernel`, with all 20 actual `#print axioms` outputs exactly `{propext, Classical.choice, Quot.sound}`. The source uses LeanCert's real `collectAxioms` classification/rejection command, not a local replacement. Ordinary symbolic proofs need no artificial numerical certificate; the exact target constants and bounds remain unchanged.

The pinned Comparator checks exact target constant signatures and recursively compares definitions used in statements, separately traverses the solution's transitive axiom dependencies, and replays the exported solution into an empty Lean environment followed by quotient checks. The final raw log reports actual Lean kernel acceptance and Comparator success. Three actual raw-kernel fixtures accept the honest inductive/quotient closure, reject the malformed proof of False at replay, and reject a forged `Quot.lift` at the post-check. Five Comparator fixtures have their intended phase outcomes. In particular, the fixture named `simple_kind_mismatch` really tests an illegal helper axiom; the distinct `type_mismatch` fixture tests exact theorem type mismatch. The additional sorry/native fixtures each reject at the illegal-axiom phase with exit 1. No rejection is mislabeled as a successful problem proof.

Build and export isolation controls both show private user, PID, mount, network, IPC and UTS namespaces; hidden host parent/process lookup; denied host networking and AF_UNIX socket creation; zero effective capabilities and `no_new_privs`; denied outer writes/truncation/creation and symlink escape. Build alone can write its `.lake`; export cannot. The nested-namespace attempt is rejected while setting its UID map, so the evidence does not claim a nested child entered and tested a later write. Unsupported/writable-path adapter arguments reject with exit 2. The real strict adapter executes pinned Landrun under Bubblewrap; there is no fake adapter. The reported AppArmor prerequisite repair matches the committed shared CI policy hash and occurs before verification. All controls execute in the fresh run.

All ten dependency URLs and actual Git heads equal the manifest pins, including Mathlib `0df444a360eaa60ab8c11dca51a86af692955474` and LeanCert `621a43d7cf21f87872392a01e874f2f1dbddc926`. I inspected the read-only observer which compares the actual LeanCert file to its pinned Git blob; its retained SHA256 is `2c576708b528acdde17796b0b715b83079f9cad377182dbd87c248323ff0a58c`, also equal to the independent local cache bytes. Runtime tool validation rechecks the locked sources, bootstrap binary hashes and Lean version. The older bootstrap kernel is correctly distinct from the newer actual guest kernel. The Mathlib log explicitly reuses 8,689 cached upstream files with no downloads. This is not an upstream source rebuild; it does not compromise the demonstrated fresh project builds or final kernel replay.

After reading its implementation, I separately ran `verification/linux/audit_evidence.py`; it passed. Its output is retained in `completion-portable-auditor.log`. It supplements, and did not replace, the independent inspection and Git/archive audit described above.

## Corrections, limits and cleanup

One reviewer-only audit attempt assumed the local cached LeanCert directory retained a Git database; `git show` exited 128. I preserved `completion-audit.py` and `completion-audit.log`, and added corrected `completion-audit2.py`/`completion-audit2.log` using local byte equality while retaining actual Linux Git-observer evidence for pinned Git provenance. No frozen source or evidence was changed. An initially consulted older Comparator Main was not the current locked version; I checked its hash, selected the correct hash-matching retained source, and inspected the replay-API/primitive-list differences before drawing the final conclusion.

I reviewed the actual operator's Linux execution evidence rather than performing another Linux run, rebuilding checker executables, or rebuilding upstream Mathlib. The pinned Comparator retains export declaration lists/results but does not retain the temporary raw export streams; I claim neither raw-stream digests nor a new replay directly from those absent streams. I do not infer historical service limits from defaults queried after the detached unit unloads.

The separate additive cleanup receipt `reviews/publication/vm-cleanup.json`, SHA256 `09213d8441f7b22fa8c57ad4b79c1b0516d3d5ddcaf1eb875361ace7d0b23e90`, records actual Tart stop exit 0 and `Running=false`, `State=stopped`, retaining the VM disk and frozen report/manifest. This is outside the 455-file manifest and does not rewrite the operator's earlier lifecycle report; no inaccessible process-session completion is asserted.

The authenticated full-target source approval and successful authentic Linux gate now support completion for this package. Final publication metadata, canonical status promotion and PR scope must still receive their separate review. This referee changed none of those files.
