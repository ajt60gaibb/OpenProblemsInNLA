# IE-22 event/extraction intermediate author checkpoint

Thirteen helper lemmas compile warning-free with exactly the three standard axioms. This is an author checkpoint by `/root/ie21_final_fidelity`, not independent review and not completion of the remaining selected targets. The source snapshots preserve this stage before final imports/wrappers are added.

`ProjectionEvent` proves ordinary real Chebyshev tails, the exact standard-Gaussian radius tail2/(d*delta²) from unchanged IE21 Gaussian moments, closedness/measurability of the uncountable-threshold good event, its finite-grid cover/union bound, and exact algebraic assembly of the three literal failure terms from explicitly supplied mean/variance inputs.

`DeterministicBound` proves the singular-square finite minimum inequality, normalization of an actual nonzero good Gaussian vector, the exact m/(d*(1-delta)) singular-square bound, positive-probability nonemptiness, transfer to the genuine attained supremum, and the ambient-dimension n version. It provides a final bridge from an actual projected matrix's minimum inequality and failure probability to the literal deterministic bound.

The final selected `projection_good_event_bound`, `deterministic_finite_bound` and `supremum_finite_bound` are NOT yet introduced. They still require the separately authored GaussianVariance/GaussianEnergy/Spectral modules. No conditional helper is represented as a final result. All four frozen boundary files and31 vendored IE21 sources remain unchanged. The local builds use cached pinned dependencies and unchanged IE21 objects, with no authoritative Linux/LeanCert/Comparator claim.
