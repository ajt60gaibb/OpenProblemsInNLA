# IE-22 independent final correctness completion review

**APPROVE the complete frozen mathematical package and the completed authentic Linux verification gate.** Reviewer: Codex AI agent `/root/ie22_final_correctness`, 2026-09-22, a nonauthor of every IE-22 statement and proof. This is my independent examination of the completed operational evidence, joined to my earlier full-source correctness review; I did not rely on the operator's conclusions or another final referee's verdict. No mathematical, source-transport, trust, or operational defect was found. This approval does not preapprove later publication metadata, canonical-page changes, PR contents, or merging.

The exact operational record is `verification/linux/OPERATIONAL-REVIEW.md`, SHA-256 `5e7854c7ce6801da27aa87648fa0209adfaec427fcbef69b0ec9c89a3fb8c0cf`. The finalized evidence manifest is `verification/linux/evidence-manifest.json`, SHA-256 `a26606cc445414a8532aa1337ca5df6d0cb047dd1b42e416afed9fd8cac2a9e7`, containing 455 entries. I independently recomputed every entry, checked that the retained directory contains exactly those entries plus the manifest, and rechecked the bindings after my audit. I waited for the operator's final freeze before issuing this verdict.

The publication input is Git commit `c45f9ccef20a2fa5cc4b988e93d4173dab853362`, project `linear-systems-and-elimination/IE-22/lean`. I independently enumerated its 394 ordinary tracked Git blobs and compared all SHA-256 values with the input receipt, current unchanged input files, preserved `source/` files, input archive, extracted-guest preparation receipt, observed fresh-build snapshot receipt, and final verifier result. All agree. No project build artifacts or `.lake` are among those inputs. The distinct guest verification-only commit is `3ee64b33ae1c626b3c7ffa1c4e561b6055623bb9`; the report correctly distinguishes it from the publication commit. Guest clean-status and unchanged-source checks are retained before and after execution.

My prior independent source approval remains byte-for-byte unchanged: `source-review.md`, SHA-256 `3c21b611c77fae39425a7761b0143702bdb381842a576a4e74b6557a5301ab46`, and `final-source-review-receipt.json`, SHA-256 `fd6c9f4a6d40c802f4afd9e2a72a7ebc2afca61aa382e563e76e7487db063f62`. That review read the full original canonical target, complete retained Colbrook manuscript, independently frozen statements, and all 47 proof modules, and rebuilt them in my own empty local project output. Its earlier limitation to a macOS build with cached dependencies is not rewritten as a Linux claim. The Linux input contains exactly the same 57 approved core source hashes under package freeze `2a07dedc9ca3774cd9fc5a760f421dd47f105cc257c770f401ae73513dea1226`, all 47 proof hashes, and all four preproof mathematical-boundary hashes. Original pending metadata and historical review limitations are preserved in the authenticated verifier input.

I read the actual full Comparator, dependency/cache, sandbox, raw-kernel, regression, forbidden-axiom, and execution records. The sole complete verification attempt is `verify-20260922T221554Z-991`, begun at `2026-09-22T22:15:54.403252+00:00`, finished at `2026-09-22T22:18:53.022964+00:00`, exit 0. The 51 observer polls in the runner receipt are read-only dependency-capture attempts, not repeated proof attempts. Verification ran as UID 1000 on actual aarch64 Linux 7.0.0-31, using Lean 4.33.1 commit `819816b2e0a3bf405af45ae5c7af2491d8f5bee6`. The older bootstrap's Linux 7.0.0-30 platform string is historical tool-build provenance, not the run's host kernel.

I compared every retained shared-harness file with the exact publication Git blob and read the snapshot, environment, tool-authentication, control, and verifier implementation. Its source lock `b3833b07916e5db77579b9cc53ca582282f6a841f36d6a60d693e5b02d342b6b` pins Forsythe `8d1b0c0545a77b40245e84705aa7d273e6c81e62`. The runtime strict adapter matches its locked source; generated probe and environment hashes match the authenticated bootstrap and actual result. The harness validates locked sources and comparator/exporter/Landrun executable hashes before running. The retained wrapper and observer only arrange execution and read source/dependency evidence; they do not modify the proof, checker, or controls. The prior shared-CI requirement restoring `kernel.apparmor_restrict_unprivileged_userns=0` is documented before transfer and execution. I checked its workflow hash and the actual before/after command records; the strict sandbox tests subsequently passed.

All ten actual fresh dependency Git HEADs equal their manifest revisions and the raw dependency log's checkout revisions. Mathlib is `0df444a360eaa60ab8c11dca51a86af692955474`; LeanCert is `621a43d7cf21f87872392a01e874f2f1dbddc926`. The observer compared actual LeanCert Verification source with that checkout's Git blob, and I recomputed the retained source SHA-256 `2c576708b528acdde17796b0b715b83079f9cad377182dbd87c248323ff0a58c`. I examined its trust implementation: `#assert_trust kernel` collects transitive axioms, permits only the foundational three, and rejects sorry, custom, and native-compiler axioms. The package imports that authentic module, explicitly sets `leancert.trust` to `kernel`, and issues exactly 20 assertions and 20 axiom-print commands matching the frozen selection. These are analytic proofs using the authentic LeanCert trust audit; no numerical interval computation is claimed.

The build log's exact set of freshly built NLA modules equals the 47 proof-manifest entries: 31 unchanged vendored IE21 modules and 16 IE22 modules. It also freshly builds authentic LeanCert Verification and Solution. There are exactly 20 warnings, all intentional reference placeholders in the separately built Challenge, and no non-Challenge warnings or errors. The raw log gives exactly one selected transitive closure for each of the 20 Solution targets, each exactly `propext`, `Classical.choice`, `Quot.sound`. The Challenge and Solution exports separately enumerate all 20 frozen selected declarations, in the same order, including `canonical_sharp_constant`. The comparator configuration has no definition holes or extra permitted axioms. The full run ends with actual default-kernel acceptance and Comparator success. Thus my earlier full-signature local type checks now have the required authentic separate-export Comparator and default-kernel evidence.

Both build and export sandbox modes pass filesystem and symlink-escape checks, private user/PID/mount/network/IPC/UTS namespaces, denial of host-parent lookup/signaling and host-loopback access, denial of AF_UNIX socket creation, zero effective capabilities, and `no_new_privs`. Export cannot write `.lake`; build can write only its designated build area. All four prohibited adapter-option cases reject with exit 2. Nested namespace entry is rejected while setting up the UID map; the report correctly does not claim that nested entry succeeded. The raw-kernel controls accept the honest inductive/quotient fixture, reject an invalid True-to-False raw proof, and reject the quotient post-check mismatch. All five Comparator regressions give their precise required result: honest match succeeds, declaration-kind mismatch rejects, illegal helper axioms reject, and the actual theorem-type mismatch rejects. The misleadingly named fixture `simple_kind_mismatch` reaches an illegal-helper-axiom rejection, which is accurately disclosed. Both extra forbidden-axiom controls reject `sorryAx` and the generated `native_decide` axiom with expected exit 1. These are successful rejection tests, not failed full verification attempts.

The source archive has SHA-256 `eb5dc854bca9aa8025c0db4af0f3e0538ffba4d262b68dcaf08616dbb62d6931`, 1,597,440 bytes. The exported evidence archive has SHA-256 `88b84a1785e98114095ab9f74e2ecc4ad6f875f3a812ca6f197c93d62fef04b2`, 425,560 bytes; all 431 regular archive members match their retained files. The pinned Comparator removes its temporary raw export streams. This review therefore binds the exact logged separate export lists, result, tool-authentication chain and controls; it does not claim retained raw-stream hashes. Upstream Mathlib used 8,689 retained cache objects, with no downloads reported. This is a fresh project-proof rebuild and default-kernel export replay, not a rebuild of the entire upstream library from source.

The operator correctly identifies their prior authorship and does not pose as an independent final referee. The transient service's requested limits are retained separately from the unloaded service's default properties. Its successful journal and actual exit status support completion. The operator froze the report while the VM was still running. Later additive cleanup `reviews/publication/vm-cleanup.json`, SHA-256 `09213d8441f7b22fa8c57ad4b79c1b0516d3d5ddcaf1eb875361ace7d0b23e90`, records stop exit 0 and stopped state without changing the report or manifest. I independently executed a read-only Tart listing and confirmed `Running=false`, `State=stopped`; my observation is retained separately. I do not claim to have waited on the operator's host boot process.

My own `completion-audit.py` performs the independent byte, Git, archive, module-set, exact-export, axiom, dependency, control and manifest checks. It passed; `completion-audit.log` and `completion-audit-receipt.json` preserve its output. This retained-evidence audit is not represented as a second Linux proof execution. One initial metadata-inspection command used a wrong freeze-file location and raised `FileNotFoundError`; I corrected the lookup to `reviews/`, and no source, build, or verification execution was affected. No substantive failures were hidden or repaired. Only additive files in this review directory were written. The complete target is mathematically and operationally approved; publication review remains a separate gate.
