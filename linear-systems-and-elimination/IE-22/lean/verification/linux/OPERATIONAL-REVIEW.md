# IE-22 complete Linux verification: operational record

**PASS, first full verification attempt.** The unchanged shared verifier returned exit 0, all real controls passed, and Comparator accepted all 20 selected declarations against the independently reviewed Challenge. Authentic LeanCert kernel assertions compiled and the default Lean kernel accepted the exported solution. Every selected transitive axiom closure was exactly `propext`, `Classical.choice`, `Quot.sound`.

Operator: Codex AI agent `/root/infrastructure_audit`, 2026-09-22. This is a mechanical operation/evidence audit, **not an independent final mathematical review**. I authored IE22 GaussianPoincare, GaussianPoincareHinge and GaussianVariance, and the vendored IE21 SphericalLaw, RowLaw, GaussianPolar, GaussianMoments, SphericalMoments and SphericalMGF. Both fresh nonauthor final source approvals are in the immutable input; independent completion review is a separate gate.

The exact publication input is commit `c45f9ccef20a2fa5cc4b988e93d4173dab853362`, parent `19d9f19bd2b779caf5f3b40540e9a303a1226a5c`, with main base `daf313133bfe730c32a266ea85cd9ca0fbe2d5ed`, in `ajt60gaibb/OpenProblemsInNLA` at `linear-systems-and-elimination/IE-22/lean`. All **394** project files matched committed Git blobs, live host bytes, archive bytes and extracted guest bytes before verification. All **57** approved core files match freeze `2a07dedc9ca3774cd9fc5a760f421dd47f105cc257c770f401ae73513dea1226`. Challenge, Comparator selection, authentic kernel assertions and axiom-print commands enumerate exactly the same **20** targets, including `canonical_sharp_constant`.

The source archive is **1,597,440 bytes**, SHA-256 `eb5dc854bca9aa8025c0db4af0f3e0538ffba4d262b68dcaf08616dbb62d6931`. Its fresh guest Git commit is `3ee64b33ae1c626b3c7ffa1c4e561b6055623bb9`: a distinct local verification-only commit with identical project bytes, not the publication commit hash. The input contained no `.lake` or compiled project objects. Guest Git was clean before and after; all 394 source hashes remained unchanged. `source/` preserves the original pending metadata and source reviews. Later publication edits are not retroactively verifier inputs.

The retained Tart VM was started without graphics, audio, clipboard, host filesystem sharing or configuration changes. Verification ran as `admin`, UID 1000, on aarch64 Linux 7.0.0-31 with Lean 4.33.1 (`819816b2e0a3bf405af45ae5c7af2491d8f5bee6`), Go 1.27.1 and Bubblewrap 0.9.0. Boot restored `kernel.apparmor_restrict_unprivileged_userns` to 1; the authorized shared-CI prerequisite was restored to 0 before transfer, with exact commands, workflow hash and before/after values in `environment-repair.json`. No control or sandbox rule was bypassed or modified. There were no failed preparation or verification attempts.

Detached service `nla-ie22-full-20260922-r1.service` invoked the unchanged command:

```bash
cd /home/admin/nla-ie22-full-20260922/repo
/home/admin/mf21-harness/tools/lean/verify.sh \
  linear-systems-and-elimination/IE-22/lean /home/admin/nla-lean-tools
```

It ran from `2026-09-22T22:15:54.403252+00:00` to `2026-09-22T22:18:53.022964+00:00`, exit 0. Original log directory: `/home/admin/nla-lean-tools/logs/verify-20260922T221554Z-991`, retained under `successful-verification/`. The wrapper only supplies the documented environment, records execution, and invokes a separate read-only dependency/source observer. The transient service's requested limits and completion journal are preserved; defaults queried after service unload are not measurements of its former limits.

The shared harness hashes match the publication checkout. Its source-lock SHA-256 is `b3833b07916e5db77579b9cc53ca582282f6a841f36d6a60d693e5b02d342b6b`, pinning Forsythe `8d1b0c0545a77b40245e84705aa7d273e6c81e62`. The authenticated bootstrap binds actual Comparator/exporter/Landrun binaries and the strict adapter. The temporary fresh snapshot's ten dependency Git HEADs matched every manifest pin; all 394 snapshot source files matched the input. Mathlib is `0df444a360eaa60ab8c11dca51a86af692955474`, LeanCert is `621a43d7cf21f87872392a01e874f2f1dbddc926`. Actual LeanCert Verification source matched the pinned Git blob and SHA-256 `2c576708b528acdde17796b0b715b83079f9cad377182dbd87c248323ff0a58c`.

The run used retained Mathlib cache objects, with no cache downloads according to the log; **upstream Mathlib was not rebuilt from source**. All **47 project proof modules**, including all 31 vendored IE21 modules and 16 new IE22 modules, were freshly compiled, as checked by exact built-module-set equality. Authentic LeanCert Verification and Solution were built. Challenge was built/exported first, with exactly 20 deliberate reference placeholders. There were no non-Challenge warnings or errors in the full Comparator proof log.

Every control passed: both build/export sandbox modes; private namespaces; denied host process, network and socket access; no effective capabilities; no_new_privs; filesystem, export-write and symlink escape restrictions; and prohibited adapter options. Nested namespace entry was rejected at UID-map setup, not successfully entered. All three raw-kernel cases and five Comparator regressions behaved as required. The fixture named `simple_kind_mismatch` tests an illegal helper axiom; `type_mismatch` tests a theorem-type mismatch. Both forbidden-axiom controls rejected `sorryAx` and native_decide's generated axiom with expected exit 1. These are successful negative tests, not failed verification attempts.

Separate Challenge/Solution exports contained every selected declaration. The actual final log records default-kernel acceptance and Comparator success. `export-receipt.json` retains complete logged declaration lists. The pinned Comparator deletes temporary raw streams; no raw-stream hashes are claimed. The exact analytic proofs need no numerical approximation or interval certificate; LeanCert supplies its authentic kernel-trust audit.

The exported evidence archive is **425,560 bytes**, SHA-256 `88b84a1785e98114095ab9f74e2ecc4ad6f875f3a812ca6f197c93d62fef04b2`. Every extracted file matched its archive bytes. `python3 audit_evidence.py` passed, checking input/evidence archives, source and independent source-review bindings, dependencies, all controls, all 47 builds, all 20 selected exports/axioms and actual kernel acceptance. Its output is `evidence-audit.log`; this evidence audit does not rerun Lean. `evidence-manifest.json` binds the frozen retained evidence, including this report.

The VM remains running for the root task's requested independent completion reviews. Its later stop will be recorded separately without modifying this report or frozen evidence. The operator added only this evidence directory to the publication worktree and made no canonical, proof, specification, metadata or PR changes.
