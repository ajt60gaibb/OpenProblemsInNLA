# IE-22 spectral restriction — author evidence

Result: the two frozen selected statements `projection_semantics` and `spectral_projection` are implemented in `NLA/IE22/SpectralProjection.lean` and pass the fresh module, exact statement, and permitted axiom checks recorded in this directory. This is author evidence by Codex AI agent `/root/ie21_final_correctness`, not an independent review, human peer review, or official Tau Ceti endorsement. Final independent review and authentic Linux LeanCert/Comparator verification of the complete IE-22 package remain separate gates.

The source uses the actual `projectedMatrix`, Euclidean matrix map, unit-row hypothesis, deletion infimum, and operator norm from the frozen boundary. Its proof of the deletion inequality uses the existing IE-21 theorem that attains the precise floor-row minimum. An actual projected minimizing pair gives an ambient admissible pair with identical retained norm. Thus neither an empty-set infimum convention nor an unattained infimum is used. The inherited minimum theorem covers zero retained rows and rank deficiency.

The spectral proof constructs the continuous Gram operator `A†A`, proves its symmetry through the adjoint inner-product identities, and uses Mathlib's **decreasingly sorted** real eigenvalues and corresponding orthonormal eigenvectors. Each eigenvalue equals the squared norm of its image under A and is therefore nonnegative. Parseval on every actual row shows that their sum is exactly m. The index r is zero based: the initial interval through r has exactly r+1 members, which proves `(r+1)*eigenvalue(r) ≤ m`. No simple-spectrum, full-rank, eigenvalue-gap, or strict-positive-eigenvalue hypothesis is imposed.

The embedding sends a vector of dimension n-r to its linear combination of the eigenvectors at indices r through n-1. The source proves this is a linear isometry using orthonormality, rather than assuming an isometric restriction exists. Its exact energy identity is the weighted sum of coordinate squares, so monotonicity of the eigenvalues bounds every projected vector by eigenvalue r. Actual attainment of the Euclidean operator norm then yields the selected squared operator norm bound `m/(r+1)`. Bessel's inequality proves each projected row contracts, and direct finite sums identify the Gram trace with the sum of the squared projected row norms. All three conclusions belong to the constructed embedding.

The selected statement retains the original `1 ≤ m` premise even though the implemented spectral reasoning proves the result without using it. The premise is not strengthened or removed in the exported theorem. The other dimensional premises ensure the retained dimension is positive when operator-norm attainment is used. Equal and zero eigenvalues require no special case or division by an eigenvalue.

`ExactSelectedTypesAndAxioms.lean` contains the two complete type expressions copied from the frozen reference boundary, with proof terms referencing only the implementation. It does not import Challenge. It also prints the transitive axiom closure of all seven public declarations in this module; each is exactly `propext`, `Classical.choice`, and `Quot.sound`. There are no admitted statements, custom axioms, native evaluation certificates, or Challenge imports in this implementation.

`fresh-build.py` builds Definitions, SpectralProjection, and the audit in a newly created external directory using Lean 4.33.1. The recorded successful run produced no warnings or errors. All 31 vendored IE-21 source hashes match the previously verified dependency receipt; this bounded check reuses their independently checked object files and the pinned upstream dependency cache. It does not claim to rebuild those dependencies, authenticate a remote cache, run Linux, or run the full Comparator. Development iterations and their errors are retained as `iter-*.log`; they are not passing receipts.

The short row-pair operator estimate initially drafted here was transferred to the owner of `ProjectionEvent.lean` for adaptation and integration. It is not present in the final spectral source and is not included in this module's seven-declaration audit. Its provenance must be recorded by its integrating author. No other agent's proof, frozen boundary, vendored IE-21 source, canonical page, or metadata was edited for this task.

After the successful build, the reusable build script was tightened to check all four frozen boundary hashes up front and corrected the numerical-target filename to `NUMERICAL_TARGETS.md`. The already completed three Lean commands were not rerun for this script-only preflight correction. A separate post-build audit checked all four boundary bytes and all 31 vendored sources and records their unchanged hashes in `receipt.json`.
