# IE-22 independent final correctness review

**Verdict: APPROVE for the complete frozen source package and local proof/type/axiom gate.** This is not approval of the pending authoritative Linux run, operational evidence, status promotion, or publication. No substantive source correction is requested.

Reviewer: `ie22_final_correctness`, an OpenAI Codex AI agent, fresh nonauthor of every IE-22 statement and proof module. I independently read the full canonical IE-22 README, the entire retained Colbrook IE-21/IE-22 manuscript, all four frozen mathematical boundary files, every one of the 47 mathematical modules, Solution, package configuration, README and formalization metadata. I did not use another referee's verdict as evidence. The review applies the repository's adapted Tau Ceti correctness, proof-quality, generality, reuse/API, documentation and attribution requirements; it is neither human peer review nor an official Tau Ceti service review.

The reviewed campaign revision is `889d196121682287d5ee26ba2be44c8502845527`. The complete 57-file package is bound by `reviews/package-source-freeze.json`, SHA256 `2a07dedc9ca3774cd9fc5a760f421dd47f105cc257c770f401ae73513dea1226`. My before/after hash records and package inspection receipt verify every entry, including the four unchanged preproof boundary inputs. The entire original mathematical problem statement occurs verbatim in NUMERICAL_TARGETS.md. The canonical page remains Solved. All 31 vendored IE-21 sources were also independently compared against the actual Git objects at `1eb284b84ecc0d3c958d022b3e020be7fa111391`; every byte matches.

## Mathematical correctness

The supremum is the specified real `sSup` over all actual real unit-row matrices. `SupremumSemantics` identifies this set with a continuous image of a finite product of Euclidean unit spheres. Positive dimension supplies a unit vector, so the image is nonempty, compact, bounded and contains its supremum. The selected theorem proves attainment and domination, rather than relying on a default value of an empty or unbounded real supremum. Nonnegativity and the square identity connect the unsquared canonical statistic to the reused normalized squared deletion minimum. The crude square-root dimension bound uses actual row inner products and exact retained cardinality. The reused minimum theorem proves attainment over every exact floor-cardinality selection and unit direction, including empty selections and kernels. The cutoff is the actual unique positive Gaussian central quantile, and `constant_semantics` identifies the displayed density integral exactly.

`SpectralProjection` constructs a genuine Euclidean linear isometry by synthesis from the retained orthonormal eigenvectors of the symmetric operator A-adjoint A. The proof establishes nonnegative eigenvalues and total sum m from unit row norms. Its index r is the (r+1)st eigenvalue in decreasing order; summing the first r+1 eigenvalues gives the precise m/(r+1) bound, and all retained indices are at least r. This remains valid with zero or repeated eigenvalues. The restriction matrix is literally defined by the row inner products against the embedded coordinate vectors, and its linear map is proved equal to A applied to the embedded vector. Row contraction follows from Bessel's inequality. The deletion inequality has the required direction: restricting the unit directions can increase the variational minimum. No rank or invertibility assumption is introduced.

`GaussianMean` compares each row's hinge with a unit Gaussian coordinate. A zero row is explicitly represented using an arbitrary unit vector with zero scaling. Gaussian projection laws are used individually, so the projected row values need not be independent. Bounded hinges prove integrability, the floor supplies k/m <= theta, and the proved population dual yields the exact upper mean. `GaussianEnergy` proves every needed moment is integrable, obtains the fourth cross moment by polarization of exact one-dimensional fourth moments, and sums the actual row inner products. The resulting mean and variance are precisely trace(B-transpose B)/m and 2 trace((B-transpose B)^2)/m^2. The trace-square bound follows from the actual operator norm applied to each row, without a Frobenius-norm substitution.

The sharp variance estimate does not assume Gaussian Poincare. `GaussianPoincare` derives the independent-copy variance identity, differentiates the literal standard Gaussian density, proves both truncated first moments by improper FTC, and obtains the crossing kernel exactly equal to that density. Nonnegative Tonelli rearrangements are justified with measurable weights and are valid even before finiteness is imposed. Both orientations of a pair give the exact factor two, which cancels the half in variance. `GaussianPoincareHinge` then proves the required interval energy for the actual affine hinges: local Lipschitz regularity implies absolute continuity on compact intervals, a bounded measurable derivative agrees almost everywhere, FTC identifies its integral, and an integrated square proves Cauchy--Schwarz. The exceptional set is at most two points for a nonconstant affine row; zero coefficients, coefficients exactly at the threshold, and t=0 are covered explicitly. No differentiability at a hinge breakpoint is assumed.

`VarianceTensorization` proves the two-factor variance decomposition and Jensen contraction with all section measurability, boundedness and second-moment hypotheses supplied. Induction through the measure-preserving finite-product coordinate equivalence proves the coordinate-variance sum, including the empty product. A separate resampling identity transports each bounded squared partial derivative correctly. `GaussianVariance` identifies every coordinate fiber with the actual hinge sum, proves the partial derivative formula, and bounds the sum of derivative squares using the adjoint operator norm and the active row vector norm squared <= mt. It transports the product Gaussian through the actual `WithLp.toLp` standard-Gaussian equality. This proves exactly 4t times the operator norm squared divided by m, for arbitrary theta and correlated rows. All helper assumptions are discharged by the selected theorem.

`TrimmingThreshold` proves threshold attainment from the exact finite trimming minimum and the first moment bound, with t=0 handling empty retention. The one-Lipschitz threshold estimate uses the increment of the whole dual, not a two-Lipschitz triangle bound. The actual finite floor grid lies in [0,L], covers the entire interval within delta, and has the required cardinality. `ProjectionEvent` proves the full event closed by an arbitrary intersection of closed threshold conditions; it does not assert that an uncountable intersection is measurable merely from countable closure. Chebyshev gives the exact three failure terms. The grid estimate extends to every threshold through the proved Lipschitz estimate, and no row independence enters this argument.

`DeterministicBound` turns strict failure probability below one into an actual good vector. The radius lower bound and positivity of retained dimension and 1-delta prove this vector nonzero before normalization. Exact finite trimming scaling and the correctly directed restriction inequality preserve the ambient n in the final numerator. The supremum version uses actual attainment and proves both squared and unsquared bounds. The final selected finite theorem supplies all spectral, moment and event inputs; none remains an extra premise.

`DeterministicSchedule` uses the literal natural floor of the real power n^(2/3) and delta=n^(-1/6). Exact real-power identities imply the floor, reciprocal and retained-dimension estimates. Eventually delta <= 1/2, the failure is at most (6+4L^2+8L)delta and the deterministic excess at most 16delta. Thus C=22+4L^2+8L is positive and bounds both, independently of m and A. The final rate chooses N before either matrix dimension is quantified, and the unsquared upper bound is uniform over every positive m, with no aspect condition.

`SphericalRealization` uses IE-21's squared numerator estimate and full unit-row support under the actual normalized surface product measure. It extracts deterministic unit-row witnesses whenever the exact failure budget is below one. For every high-aspect sequence, the unchanged IE-21 aspect schedule makes this possible eventually and makes the error tend to zero. The lower square-root comparison handles both epsilon greater than the sharp constant and epsilon no greater than it. The final supremum limit combines these witnesses with the all-row upper bound for every admissible sequence. Optimality fixes epsilon=(c-C)/3 before extracting N and R, then uses n=j+1 and m=(j+1)^2 to defeat those thresholds. This establishes the original fixed-epsilon quantifier failure for every smaller real constant, not just failure along one dimension or with threshold-dependent epsilon.

## Consumed dependencies and proof quality

I read all 31 reused IE-21 modules, including the Haar-to-sphere normalization and Gaussian polar law, radius independence and moments, all-orders spherical moments and MGF, the atom-robust fractional quantile selector, exact Hoeffding/Chernoff constants, ambient-ball sphere net, symmetric quadratic control, uniform trimming estimate, finite-size numerator result and unrestricted aspect schedule. The lower witness proof uses those actual facts, not the ratio limit alone. The reused source chain has no assumed probability, geometry or asymptotic conclusion.

The implementation uses existing Mathlib compactness, finite-dimensional spectral, Gaussian measure, FTC and finite-product integration APIs. I inspected the critical sorted-eigenbasis, absolutely-continuous FTC and Gaussian/product transport API source text as well. New lemmas concentrate on the missing crossing-kernel and bounded tensorization steps, with explicit integrability obligations. Exact algebra, integrals and limits eliminate unnecessary numerical integration, finite-dimensional eigenvalue searches or asymptotic truncation. Some helpers duplicate small existing project identities, but this introduces no mathematical or maintainability defect requiring a source change.

## Independent mechanical evidence and limitations

My `rebuild.py` created a new empty output directory and compiled all 47 project mathematical modules in dependency order, using the specified Lean 4.33.1 executable and the existing upstream dependency cache. It did not reuse any project olean. All 47 returned exit zero with no warnings or errors. The independently generated `ExactFrozenTypesAndAxioms.lean` imports Final, never Challenge, and checks each full universally quantified signature extracted from the immutable reference against its selected proof. All 20 type checks passed, and all 20 transitive closures are exactly `propext`, `Classical.choice`, `Quot.sound`. The successful audit log retains 69 unused-binder linter warnings caused by preserving the reference's proof-binder names in universal signatures. These are not holes or changed statements; no source alteration or linter suppression was used.

The static audit finds no executable sorry/admit, custom axiom, unsafe declaration, native_decide, run_tac, custom elaborator/macro, extern/implemented_by or Challenge import in mathematical modules. There are exactly 20 deliberate isolated Challenge placeholders. Solution explicitly selects `leancert.trust "kernel"` and contains one authentic `#assert_trust kernel` and `#print axioms` command for each selected target. The upstream LeanCert trust implementation's default is native, so that explicit setting matters; kernel assertions reject native compiler, sorry and custom axiom dependencies. I inspected this source behavior, but did not compile Solution's authentic LeanCert wrapper in this local cached build.

The compiled upstream cache does not contain complete source checkouts; initial source lookups there failed, and the separate Mathlib source extraction has no Git metadata. Those read-only lookup failures and the source-inspection limitation are recorded in my package receipt. This local rebuild is not a fresh authentication or reconstruction of every upstream cached object. The authentic non-root Linux build, dependency receipts, isolation and rejection controls, separate Comparator exports, exact statement comparison, permitted-axiom gate and default-kernel replay remain required and are not preapproved here.

The source README and formalization.yaml truthfully identify those gates and final reviews as pending at this freeze. Their historical draft-boundary text is explicitly explained. Formalization credit is George Stepaniants, Department of Computing and Mathematical Sciences, California Institute of Technology; the complete Colbrook original proof attribution, Apache license and AI-role disclosures are preserved. No contact email is added to the package. No canonical identity, page, mathematical target, original history or prior verification was edited by this reviewer.
