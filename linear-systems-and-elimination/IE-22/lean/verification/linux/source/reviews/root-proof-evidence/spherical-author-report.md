Author development report: both exact selected statements compile warning-free with standard-three axiom closures. Unit-row matrices have full matrixLaw probability, while the complete IE21 bad event has probability below one, forcing a literal deterministic matrix with the required normalized deletion estimate. For every high-aspect sequence, the verified IE21 schedule makes that error smaller than epsilon squared. Nonnegative singular values and their literal squared identity then yield the desired unsquared strict lower bound, including the cases sharpConstant < epsilon and sharpConstant = epsilon. No compactness maximizer or probabilistic assumption is substituted for the deterministic existential conclusion. Earlier development failures remain in the logs.
