# IE22 Gaussian energy and projection event: partial referee review

**Verdict: APPROVE** both frozen modules, with no mathematical or statement-correspondence defects.

Reviewer: AI `/root/infrastructure_audit`. I authored no source in GaussianEnergy or ProjectionEvent. I authored the separate GaussianPoincare, GaussianPoincareHinge and GaussianVariance dependencies and some unchanged reused IE21 Gaussian modules. The row-pair operator bound in ProjectionEvent is attributed to the independent spectral author, not to me. This is a module-only independent review, not a final whole-package nonauthor review. I made no edits to the reviewed modules.

- `GaussianEnergy.lean`: `cfee54bc30ce0c18738a9911210c92a947be55a8ad86effa6730805c3b85257b`
- `ProjectionEvent.lean`: `fb0fd31a8956a53ee1ae17b4085c68a989eeedc2bca4579a9654ea34373bdf2b`

I read both complete sources, their frozen Challenge types, the canonical problem/manuscript and NUMERICAL_TARGETS, and the consumed threshold/mean dependencies. The exact original constants, floor-based trimming, actual standard Gaussian and literal event are preserved. Both selected wrappers supply all their intermediate moment inputs from proved declarations; none leaves a variance or energy result as an assumed final premise.

The energy proof handles arbitrary B, including singular or zero matrices. It proves every needed moment integrability, transports the actual unit-direction Gaussian moments by scaling, and treats a zero direction separately. The fourth mixed moment is derived by the polynomial polarization identity for b+c and b−c, yielding ||b||²||c||²+2< b,c >² without independence of row coordinates. The row-Gram/column-Gram trace identities are exact rectangular-matrix cyclic-trace identities. Finite sums give the literal mean trace(BᵀB)/m and variance 2*trace((BᵀB)²)/m², together with MemLp exponent two. The row norm bound is used only later to bound the mean; it is not a hidden assumption in the exact moments.

The projection event proof applies Chebyshev to the exact centered quantities. The radius tail uses mean d and variance 2d, with d and delta proved positive before cancellation. The event constrains every real threshold in [0,L]; it is proved closed by an arbitrary intersection of closed continuous-objective sublevel sets, so there is no unjustified countable measurability reduction. The finite grid covers the closed interval and its cardinality is at most L/delta+2. Exact 1-Lipschitz threshold control raises a grid bound h+delta to the event bound h+2delta, including both endpoints and ties. The failure set is explicitly contained in the energy, finite-grid and radius bad-event union. The finite union bound preserves all three source terms.

For the energy variance estimate, the proof sums ||B row_i||² ≤ ||B||²||row_i||², which bounds the full row-pair covariance sum. This retains all correlations. With row norms ≤1 and ||B||²≤m/(r+1), the energy variance is at most 2/(r+1). For thresholds t≤L the already proved objective variance is at most 4L/(r+1), and Chebyshev contributes the precise grid term 4L(L/delta+2)/((r+1)delta²). The selected result is exactly measurability plus complement probability ≤ 2/(r+1) + 4L(L/delta+2)/((r+1)delta²) + 2/(d*delta²). The selected assumptions m,d,r≥1 and 0<delta<1 remain present; some internal helpers valid under weaker assumptions are not substituted for a different target.

A new independent build directory rebuilt all nine IE22 dependency/proof sources and a test containing two verbatim frozen Challenge signatures. All ten commands exited zero without diagnostics. Every one of the 27 public declarations in the reviewed modules has an actual printed standard-three-only axiom closure. The receipt verifies source bytes before and after, matches both author receipts, and records cached verified IE21 source/object provenance. No proof imports Challenge.

After that completed build, root authorized a documentation-only correction to the imported GaussianPoincare header. The build receipt correctly remains bound to its original hash a0636eda…; all imports and declaration/proof bytes are unchanged. `gaussian-poincare-author-evidence/header-correction/receipt.json` records the exact old/new relation, preserved prior source and separate clean compilation. This review's binding receipt includes that additive correction. Neither reviewed source changed.

This is local source and cached-dependency verification. It does not claim complete-package Linux kernel replay, authentic LeanCert/Comparator success, final independent whole-source approval or status promotion. Those remain separate campaign requirements.
