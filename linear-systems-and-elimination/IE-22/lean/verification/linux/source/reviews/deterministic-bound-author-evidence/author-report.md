# Deterministic finite bounds author evidence

Role: AI proof author `/root/ie21_final_fidelity`, also an author of the IE-22 statement draft; this is not an independent final referee approval or a claim of human/official Tau Ceti endorsement.

Both selected `deterministic_finite_bound` and `supremum_finite_bound` are implemented with the exact frozen signatures and no added assumptions. The original positive m, 1 ≤ r < n, 0 < delta < 1 and strict deterministicFailure < 1 hypotheses are preserved. The matrix result uses all unit-row matrices, including rank-deficient matrices, with the literal floor-row deletion singular value and ambient n/m normalization. The supremum result is both the squared and unsquared bound on the actual canonical M, with no replacement supremum surrogate.

The actual spectral theorem constructs an isometric (n-r)-dimensional restriction, whose operator norm and row bounds feed the exact Gaussian event theorem. Strict failure below one supplies a genuinely nonempty good event. Its radius lower bound excludes the zero vector. The proof normalizes that vector, uses exact nonnegative finiteTrim scaling, and obtains the attained bounded-threshold dual with the original floor count; empty retained subsets and ties are covered by the unchanged minimum semantics and threshold theorem. The singular minimum can only increase under restriction. Keeping the ambient normalization n/m cancels m exactly and yields n/((n-r)(1-delta))*(gaussianTrim+2delta). Actual attainment of the unit-row supremum then proves the squared bound; the real square-root implication proves the unsquared form.

Every intermediate certificate, spectral and moment input is supplied by a proved theorem in the selected wrappers. None survives as a hidden final premise. The spectral module was independently recompiled from source before assembly, and its current hash agrees with its producer's frozen receipt. Both full frozen-signature examples and all eight public theorem axiom queries passed without diagnostics, with exactly propext, Classical.choice, Quot.sound. The build driver checks the four frozen boundary files and all 31 unchanged vendored IE-21 source hashes. No Challenge import, sorry/custom axiom or native computation is used in the proof.

This is local cached Darwin author evidence, using pinned cached upstream and unchanged IE-21 objects. It is not authentic Linux verification, independent final review, LeanCert kernel replay or Comparator verification. Those publication gates remain required. Historical event/extraction helper checkpoints are retained unchanged; this completed source adds the two selected finite bounds to the previously completed six selected targets authored here.
