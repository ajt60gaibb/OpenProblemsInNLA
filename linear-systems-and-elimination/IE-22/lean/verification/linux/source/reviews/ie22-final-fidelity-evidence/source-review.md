# IE-22 independent final source referee — fidelity, scope and proof chain

**Verdict: APPROVE the frozen source package for the authentic Linux completion gate.** No unresolved mathematical, statement-correspondence, attribution or packaging blocker was found. This is a source-review decision, not approval to promote the canonical status and not a claim that this referee has run Linux Comparator or the authentic LeanCert wrapper.

Reviewer: fresh Codex AI agent `/root/ie22_final_fidelity`, an independent nonauthor of the IE-22 boundary, all IE-22 proofs, the vendored IE-21 proofs and project packaging. This is an agent review under the repository's adapted Tau Ceti protocol, not human peer review or official Tau Ceti endorsement. The review covers fidelity/scope plus the complete mathematical proof chain, reuse/API, proof quality, naming/placement, documentation and attribution. I changed no frozen source or canonical file; my additions are confined to this evidence directory.

The decision binds all **57 core files** in `reviews/package-source-freeze.json`, SHA256 `2a07dedc9ca3774cd9fc5a760f421dd47f105cc257c770f401ae73513dea1226`. Its 47 mathematical modules match `reviews/proof-source-freeze.json`, SHA256 `8c2d4862b0b723689fa07dcac51aca6c83f3e694a58e13270fd1c90cd8fe43a9`. The four preproof boundary files still match `reviews/statement-freeze.json`, SHA256 `9f218c0afeb9899c93609c3ea733deddee2689d1e850a7bceadc28b1a886f329`. I recomputed every core hash before and after compilation and at final inspection. The per-file hashes are retained in `build-receipt.json` and `inspection-receipt.json`.

## Original target and semantic correspondence

I read the complete canonical IE-22 README, the entire retained IE-21/IE-22 manuscript, all frozen numerical targets and Challenge declarations, every line of all 47 NLA modules, Solution, package metadata and dependency configuration. The canonical README has SHA256 `504984076c0ea84bd8f1017dd10ab70858ea40c292cd70dc5adca81ddfb93f80`; the retained manuscript has SHA256 `31a1949c07f63408538f04e3803d90e8d3d0c3d1e47dc89a2ffa5a04ef4f3880`. Both match their Git objects at campaign scope base `54f93060c0c4e5dab81096c6496e0d2b4251f3ef`. The numerical inventory includes the complete original mathematical statement verbatim. The canonical status remains Solved.

`Space n` really is real Euclidean space; `Mat m n` is the actual finite real matrix. `matrixMap` is its Euclidean linear map and `operatorNorm` its continuous-linear-map norm. The retained selection has exactly natural floor `floor(theta*m)` rows; `retainedNorm` is the norm of the actual selected-row map. The infimum defining `deletionSingular` is proved attained, nonnegative and no greater than any admissible retained norm. The finite minimum, dual identity and nonnegative scaling are also proved. Thus no empty real infimum, surrogate singular value or rank assumption is silently used.

`UnitRows` literally requires every row norm to equal one. `normalizedSingular` uses `sqrt(n/m)*s_theta`, without IE-21's operator-normalization denominator. `extremalValue` is the supremum over all such matrices. Compactness of the finite product of spheres and continuity of the actual variational minimum prove nonemptiness, boundedness and attainment, domination of every admissible matrix, and the harmless bound `sqrt(n)`. `normalizedSingular_nonneg_sq` identifies its square with `(n/m)*s_theta^2`. Positive m,n hypotheses match the canonical dimensions and are not strengthened to full rank, nonempty retention, m>=n or a fixed aspect ratio.

The constant is the square root of the literal canonical density integral. The inherited `gaussian_constant` proves the infimum-defined cutoff is positive, has central Gaussian mass exactly theta and is unique, and identifies the density integral with the actual Gaussian truncated second moment. This is not an arbitrary quantile convention. Theta is fixed strictly between zero and one; no endpoint-uniform claim is made.

## All selected obligations and their proof paths

| Selected target(s) | Independent mathematical assessment |
| --- | --- |
| `supremum_semantics`, `constant_semantics` | The compact attained supremum and actual Gaussian integral bridges above are discharged, including nonnegativity and squared normalization. |
| `projection_semantics`, `spectral_projection` | The projected matrix entries are inner products with an actual linear isometry. Basis expansion proves `B*g=A*(J*g)`; Bessel gives row contraction. The minimum inequality has the correct direction `s_theta(A)<=s_theta(B)`. For `A* A`, a sorted orthonormal eigenbasis is supplied by Mathlib; eigenvalues are nonnegative and sum to m by row norm/Parseval identities. The tail beginning at zero-based index r has dimension n-r and largest eigenvalue at most m/(r+1). No low-norm subspace is assumed. |
| `gaussian_objective_mean` | Each row is contracted from a unit Gaussian projection, with zero row vectors handled explicitly by choosing a unit direction. Pointwise hinge monotonicity, actual Gaussian law transport and integrability give the lower hinge bound. The floor coefficient is at most theta, and the proved population dual equals h_theta. No row independence enters. |
| `gaussian_objective_variance` | The factor 4 is established for arbitrary theta and t>=0. The scalar variance identity uses independent copies, exact truncated first moments, a crossing kernel equal to the Gaussian density, and Tonelli with nonnegative energies. Absolutely continuous hinge fibers and interval Cauchy–Schwarz establish the analytic premise; exceptional breakpoints are null and constant/zero coefficients are treated directly. Finite-product variance tensorization is itself proved by variance decomposition, scalar Jensen and induction. Product-coordinate resampling and the standard-Gaussian map are justified. The sum of actual partial derivative squares is bounded by `(2/m)^2*||B* active||^2 <= 4*t*||B||op^2/m`. Correlated rows are never treated as independent. |
| `gaussian_energy_moments` | Gaussian second/fourth moments and polarization give the exact cross-fourth identity `E[(b.g)^2(c.g)^2]=||b||^2||c||^2+2<b,c>^2`, with integrability. Finite sums and Gram trace identities prove the mean and exact variance `2*trace((B^T B)^2)/m^2`, including singular/zero B. |
| `bounded_trimming_threshold`, `threshold_lipschitz`, `threshold_grid` | Average energy at most two implies enough observations are below L=2/(1-theta). The exact finite minimum is attained by a threshold in [0,L], also for k=0 and ties. One-sided hinge increments give Lipschitz constant one. The floor-spaced finite grid lies in [0,L], has the claimed cardinality bound and genuinely covers every threshold within delta. |
| `projection_good_event_bound` | The energy covariance bound uses the full row-pair sum and the operator norm; it does not discard correlations. Chebyshev gives exactly `2/(r+1)`, `4*L*(L/delta+2)/((r+1)*delta^2)` and `2/(d*delta^2)`. The grid bound extends to all t in [0,L]. The complete all-threshold event is closed, hence measurable; there is no invalid uncountable measurable-union step. The selected theorem supplies all moment premises of its assembly helper from proved modules. |
| `deterministic_finite_bound`, `supremum_finite_bound` | Strict failure budget below one forces an actual good g. Its radius lower bound and positive d,1-delta prove g nonzero before normalization. Finite trimming scales exactly, and the ambient minimum comparison retains n in the numerator. This yields the literal boxed `n/((n-r)*(1-delta))*(h_theta+2*delta)` uniformly in m,A. Supremum attainment transfers the squared and square-root bounds to the actual M. |
| `deterministic_schedule` | Both exponents are real: floor(n^(2/3)) and n^(-1/6). Exact power identities and floor bounds prove eventual admissibility and failure<1. The error tends to zero. The same explicit positive C=22+4*L^2+8*L bounds failure and excess above h_theta by C*n^(-1/6); the finite constants are unchanged. |
| `universal_squared_rate`, `uniform_upper_all_rows` | Final wrappers supply the actual deterministic finite theorem to the conditional assembly lemmas. C,N depend on theta alone and apply to every positive m and all unit-row A. The theorem also controls M squared and gives the unsquared all-m upper result; no aspect-ratio hypothesis survives. |
| `spherical_realization_from_finite_bound`, `high_aspect_near_extremizers` | The inherited law is normalized Euclidean Haar-to-sphere measure, with actual product row law and probability-one unit rows. The complete IE-21 finite theorem controls the normalized squared numerator, not merely the different ratio. A strict finite failure bound and unit-row support yield an actual admissible matrix. The exact 32*sqrt(log(Q)/Q) schedule becomes admissible and has failure/error tending to zero for every positive sequence with n and Q tending to infinity. Square-root lower witnesses use established nonnegativity and do not impose a growth speed or coupling across dimensions. |
| `high_aspect_supremum_limit`, `canonical_sharp_constant` | Every-sequence lower witnesses are bounded above by the actual supremum and combined with the all-m upper theorem to prove convergence to c_theta. The original epsilon/N/R order is literal in `EventualUniformUpper`. For any C<c_theta, epsilon=(c_theta-C)/3 is chosen before arbitrary thresholds; m=(j+1)^2,n=j+1 eventually defeats both thresholds. Thus the failure uses one fixed positive epsilon. Natural thresholds are without loss relative to integer thresholds. |

I traced the complete 31-module IE-21 dependency chain rather than accepting its earlier verification as a substitute for review. Its actual surface/Gaussian polar decomposition, nullity at zero, independent radius, all-order moments, centered MGF, Hoeffding selectors with atoms, exact finite trimming, sphere nets and covariance/all-direction control supply the advertised finite event. The all-aspect schedule has no added relation between the growth rates of n and m/n. Every vendored source matches the original Git blob at immutable IE-21 commit `1eb284b84ecc0d3c958d022b3e020be7fa111391`; all 31 were rebuilt in this review, with no IE-21 project object reuse.

## Mechanical evidence and honest limits

My independently written `rebuild.py` topologically enumerated and rebuilt all 47 NLA sources into fresh directory `/var/folders/yr/766r3vcd65n38k3xsp24d0xm0000gq/T/nla-ie22-fidelity-fresh-vn0vp5s1`. The compiler reports Lean 4.33.1, arm64 Darwin, commit `819816b2e0a3bf405af45ae5c7af2491d8f5bee6`. All 47 compiler exits were zero, with zero warnings/errors in project-source logs. Cached upstream package objects under the disclosed IE-15 cache were used; this is not an independent upstream bootstrap or Linux sandbox run.

The first audit generator incorrectly assigned unapplied polymorphic theorem terms to examples whose parameters were already introduced. It failed at all 20 examples. The complete original generator, audit source, compiler diagnostics and transcript are retained. `complete-audit.py` generates the additive `ExactTypesAndAxioms2.lean`: original frozen binders become an explicit full forall type, assigned the full theorem term with `@`. All 20 full types then elaborate and all 20 transitive `#print axioms` outputs are exactly `propext`, `Classical.choice`, `Quot.sound`. The corrected reviewer audit has 138 unused bound-variable-name warnings and zero errors; those warnings concern the generated exact-type audit, not proof modules. No proof or frozen boundary was changed to resolve the generator defect. A separate scouting read used an incorrect relative documentation path once, then the corrected path; no source/build effect.

A comment-aware proof-source scan found no `sorry`, `admit`, custom axiom, native decision, unsafe declaration, implementation substitution or Challenge import. The only 20 reference holes are in the isolated frozen Challenge. Solution imports the authentic LeanCert Verification module, sets kernel trust explicitly, and has exactly one kernel assertion and axiom print for every selected theorem. Comparator permits only the same three axioms and has zero definition holes. The metadata schema/coverage validator passes all 20 declarations. These local checks supplement, and do not replace, actual separate-environment Comparator exports, sandbox rejection controls, authentic LeanCert assertions and default-kernel replay.

## Reuse, API, documentation and attribution

I inspected the retained pinned Schiffer/Forsythe Challenge patterns and the pinned LeanCert trust implementation. The project follows statement/solution separation, explicit numerical targets and complete selected coverage. The LeanCert API collects transitive axioms and distinguishes foundational/native/sorry/custom classes, so the explicit kernel assertions are appropriate. Schiffer is a structure reference at a different toolchain; no incompatible proof import was introduced. I searched the available Mathlib Probability tree for Poincare/product variance APIs and inspected sorted spectral APIs. This bounded search did not locate the needed sharp Gaussian hinge inequality. The new scalar interval-energy and bounded tensorization abstractions are suitable, transparently stated and proved. The exact crossing-kernel route replaces the manuscript's Hermite/approximation sketch without losing a constant or assuming the analytic conclusion. Imported standard spectral, integration, moment and finite-dimensional APIs are reused. The extracted API tree was not newly authenticated remotely in this pass; source hashes and that limitation are recorded.

Names and module boundaries follow the mathematical steps. Some small row-inner/trace helper overlap is harmless and does not justify changing frozen proofs. No floating-point quadrature, eigenvalue search, finite sampling, truncated series surrogate or artificial interval computation is used. Computation is minimized by exact algebra and analysis. The earlier GaussianPoincare header-only correction truthfully locates tensorization in its separate module; retained prior-source and correction evidence show the proof content is unchanged, and the current file was freshly rebuilt here.

George Stepaniants is credited with the exact Department of Computing and Mathematical Sciences, California Institute of Technology affiliation, with no added contact email. Matthew J. Colbrook retains original mathematical proof attribution. Apache licensing, AI assistance, distinct author/reviewer roles and lack of human/Tau Ceti endorsement are disclosed. Historical draft wording in immutable statement records is clearly explained by the current README. Metadata accurately marks authentic Linux/Comparator and final reviews pending; it does not claim canonical promotion from local compilation.

**Remaining gate:** independently review successful authentic Linux LeanCert/Comparator/axiom/default-kernel and rejection-control evidence for these same bytes, then separately review the concrete publication and regenerated canonical artifacts. This source approval alone does not authorize a Lean-verified status claim.
