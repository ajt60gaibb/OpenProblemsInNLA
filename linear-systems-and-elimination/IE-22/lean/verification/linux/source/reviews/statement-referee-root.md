# IE-22 independent preproof statement review

Reviewer: Codex AI root. **APPROVE the exact four-file mathematical boundary bound by `root-statement-review-receipt.json`.** No IE-22 proof implementation had begun at review. I authored none of the new IE-22 definitions, reference statements or numerical inventory. I authored some reused IE-21 proof modules; this is an independent review of the new IE-22 boundary, not a claim of independence from every transitive dependency. IE-21 has its separate nonauthor whole-proof reviews and authenticated Linux verification.

I read the complete canonical IE-22 README, the complete retained IE-21/IE-22 manuscript, every new definition, all twenty Challenge statements, the complete numerical inventory and Comparator configuration. My earlier source analysis is retained separately. I independently typechecked both new Lean files in a fresh output directory, using byte-checked earlier IE-21 outputs and cached pinned dependencies. Definitions emitted no warnings; Challenge emitted exactly twenty intentional placeholder warnings. This checks types only and proves no IE-22 result. The receipt identifies commands, logs, source hashes and this limitation.

The numerical inventory reproduces the complete original mathematical problem section verbatim, checked mechanically. The canonical matrix type, Euclidean norm, deletion minimum and floor convention are inherited unchanged from IE-21. The new normalized statistic is `sqrt(n/m)*s_theta(A)`, not the operator-norm ratio from IE-21. The supremum is over all actual unit-row matrices; nonemptiness, finite boundedness, attainment, comparison and nonnegativity are compulsory conclusions. Its crude sqrt(n) bound is legitimate. Squaring the statistic is explicitly bridged to the already defined normalized deletion value. The sharp constant is exactly the square root of the original density integral, with quantile semantics inherited from the proved unchanged IE-21 source.

Every finite matrix has positive m and n; eventual claims do not exclude any relevant dimension or aspect sequence. No full rank, positive retained count, faster aspect growth or Gaussian substitution for deterministic matrices is imposed. The projection is an actual Euclidean isometry, its matrix is literal row restriction, and its singular minimum inequality has the correct direction. The spectral operator and trace bounds are conclusions of an existence theorem. They are not hidden assumptions in the universal result.

I checked all finite constants against Sections 6 and 7. The threshold objective has the exact empirical normalization. Its correlated Gaussian mean and variance estimates, genuine second-moment integrability, quadratic-energy mean/variance identities, bounded threshold, one-Lipschitz estimate, finite grid and all-threshold good-event measurability are explicit obligations. In particular, the difficult variance estimate is not licensed as an axiom or a premise of the final theorem. The row norm and spectral premises of the intermediate good-event theorem are supplied by the separately required projection theorem. The failure budget is exactly the sum of the three source terms, and its strict inequality below one permits a nonzero Gaussian witness. The boxed normalized squared deletion bound has the correct retained dimension and positive normalization denominator.

The source schedule uses real exponents 2/3 and -1/6, with the exact natural floor. Eventual feasibility and vanishing error are conclusions. Both the failure budget and the bound's excess over h_theta have an explicit existential O_theta constant, independent of m and A. The all-m upper theorem therefore cannot be replaced by a result for a particular aspect sequence. The actual supremum's squared bound is separately required.

For lower bounds, the selected realization theorem uses the simultaneous normalized deletion conclusion from IE-21 and actual unit-row support. A ratio limit alone is insufficient, and the draft does not make that substitution. Every high-aspect sequence gets eventual deterministic near-extremizers and the genuine supremum limit. The final uniform property has epsilon outside both N and R; its negation for every smaller real constant preserves the canonical sharpness quantifiers. Natural thresholds lose no generality because arbitrary integer thresholds can be increased. These endpoints retain the original mathematical target and the stronger claims asserted by the supplied resolution.

The Comparator list contains exactly all twenty reference declarations, with no duplicate or omitted target and no definition holes. The only future permitted axioms are `propext`, `Classical.choice` and `Quot.sound`. Original proof credit remains Matthew J. Colbrook; formalization credit is George Stepaniants with the requested Caltech affiliation and no contact email.

There are no unresolved mathematical or scope findings. The missing Gaussian variance foundations, spectral construction, asymptotic assembly and the rest of the proof obligations still require actual implementations. A second independent preproof approval and an explicit freeze are required before implementation. Portable packaging of unchanged IE-21 imports must be tied to immutable verified bytes; the current local cache runner is not the eventual verification route. Final nonauthor proof reviews, authentic LeanCert kernel assertions, actual Comparator comparison, permitted-axiom checks and reproducible Linux execution remain mandatory before any IE-22 status promotion.
