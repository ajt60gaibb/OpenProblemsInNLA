# IE-21 complete Linux verification: operational record

**Result: PASS, first full verification attempt.** The unchanged shared verifier returned exit 0 after all real controls and a fresh full 23-target Comparator run. LeanCert's authentic kernel assertions compiled, all 23 selected axiom closures were exactly `propext`, `Classical.choice`, `Quot.sound`, and the actual Lean kernel accepted the solution. No proof, specification, selected target, checker or control was changed to obtain acceptance.

Operator: Codex AI agent `/root/infrastructure_audit`, 2026-09-22. This is a mechanical operation/evidence audit, **not an independent final mathematical review**. I authored SphericalLaw, RowLaw, GaussianPolar, GaussianMoments, SphericalMoments and SphericalMGF. Both fresh nonauthor final source reviews are retained in the immutable input snapshot; final completion review remains separate.

The input is publication commit `1eb284b84ecc0d3c958d022b3e020be7fa111391`, based on `daf313133bfe730c32a266ea85cd9ca0fbe2d5ed`, at canonical path `linear-systems-and-elimination/IE-21/lean` in `ajt60gaibb/OpenProblemsInNLA`. I checked all **289** committed project files against their Git blobs, the host worktree, the input archive and extracted guest files before verification. All **41** files bound by both final source approvals match package-freeze hash `b4ab97645ec9f8c46c7130aabc4bac48cbab48af0c96d4f454ad58c32a981744`. Challenge, Comparator selection, all kernel assertions and all axiom-print commands enumerate exactly the same **23** declarations, including `original_random_row_limit`.

The exact input archive is 1,228,800 bytes, SHA-256 `6b61efee12a30cde167f1e2ac22b1b0662be0324475e37b222e3cd8ee466c6dd`. The fresh guest repository commit is `bd72d630841a1660b9f7652d469186c11728925f`; this is an intentionally distinct local verification commit with identical project bytes, not the publication Git hash. No `.lake` directory or compiled project objects entered the source archive. The guest checkout was clean before and after verification, and all 289 source hashes stayed unchanged. `source/` retains the original pre-run metadata and reviews; later publication metadata/addenda are not retroactively claimed as verifier inputs.

The preserved Tart VM was started without graphics, audio, clipboard, host filesystem sharing or configuration changes. Execution used guest `admin`, UID 1000, aarch64 Linux `7.0.0-31-generic`, Lean 4.33.1 (`819816b2e0a3bf405af45ae5c7af2491d8f5bee6`), Go 1.27.1 and Bubblewrap 0.9.0. Its non-root user systemd manager was running. Reboot reset `kernel.apparmor_restrict_unprivileged_userns` to 1; the previously authorized shared-CI prerequisite was restored to 0 with recorded before/after values and workflow hash. No sandbox protection was skipped or substituted. A host evidence filename alias was corrected before guest preparation or verifier execution; `host-preparation-issue-1.json` retains that operational issue. There were no failed actual verification attempts.

The detached service `nla-ie21-full-20260922-r1.service` ran exactly:

```bash
cd /home/admin/nla-ie21-full-20260922/repo
/home/admin/mf21-harness/tools/lean/verify.sh \
  linear-systems-and-elimination/IE-21/lean /home/admin/nla-lean-tools
```

The run started `2026-09-22T20:53:03.368999+00:00` and completed `2026-09-22T20:55:38.670324+00:00`, exit 0. Original logs: `/home/admin/nla-lean-tools/logs/verify-20260922T205303Z-973`, retained as `successful-verification/`. The wrapper only supplies the documented environment, records execution and invokes a separate read-only observer of the temporary source/dependency snapshot. It does not alter verification. The requested service limits and completion journal are retained; defaults reported after a transient unit unloads are not measurements of its former limits.

The unchanged harness/tool bootstrap validated before execution. Its source lock is SHA-256 `b3833b07916e5db77579b9cc53ca582282f6a841f36d6a60d693e5b02d342b6b`, pinning Forsythe `8d1b0c0545a77b40245e84705aa7d273e6c81e62`. The retained bootstrap binds actual Comparator/exporter/Landrun binaries and the strict adapter. The driver's fresh snapshot materialized all ten exact manifest revisions; while it existed, the read-only observer checked every actual Git HEAD and all 289 snapshot source hashes. Mathlib is `0df444a360eaa60ab8c11dca51a86af692955474`; LeanCert is `621a43d7cf21f87872392a01e874f2f1dbddc926`. Actual `LeanCert/Tactic/Verification.lean` matched its pinned Git blob, SHA-256 `2c576708b528acdde17796b0b715b83079f9cad377182dbd87c248323ff0a58c`. The log records retained Mathlib cache use with no cache downloads; this is not a claim that every upstream dependency object was rebuilt from source.

Every real sandbox/control gate passed: both build/export modes, private namespaces, denied host process/network/socket access, no capabilities, no_new_privs, filesystem and symlink-escape restrictions, and rejected prohibited adapter options. The nested namespace attempt was rejected at UID-map setup before entry; the evidence does not claim a successful nested namespace. All three raw-kernel controls and all five pinned Comparator regressions behaved as specified. The fixture named `simple_kind_mismatch` actually tests an illegal helper axiom; the separate `type_mismatch` fixture tests theorem-type mismatch. Both forbidden-axiom controls rejected `sorryAx` and native_decide's generated axiom, with their expected exit 1. These expected control rejections are successful tests.

Comparator built/exported Challenge before building Solution, with exactly the 23 intentional Challenge placeholders and no Solution warnings. Solution imported authentic LeanCert verification, selected `leancert.trust "kernel"`, and executed all 23 assertions and axiom checks. The actual final log states “Lean default kernel accepts the solution” and “Your solution is okay!”. Both complete export declaration lists are recorded in `export-receipt.json`; temporary raw streams are removed by the pinned Comparator and no raw-stream hashes are invented. The exact probability/analysis proofs require no numerical approximation or interval certificate; LeanCert's role here is its authentic kernel-trust audit.

The exported evidence archive is **346,734 bytes**, SHA-256 `ba04ba6bc5973591c25b3fe6729c683d85966d3dac375bbd2083c8e58d1d5853`. The host checked its bytes and every extracted file. Run `python3 audit_evidence.py` here to check both archives, all source/dependency/hash bindings, the two source-review bindings, all controls, all selected exports and axioms, and actual kernel acceptance. That retained-evidence audit passed; output is `evidence-audit.log`. It is not a substitute for rerunning Lean.

At this report's freeze, the VM remains running at the root task's request for final evidence review. A separate lifecycle/cleanup receipt will record its eventual stop; this report need not change. No canonical status, metadata, commit or PR was modified by the operator beyond adding this verification evidence directory.
