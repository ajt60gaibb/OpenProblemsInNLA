# Independent module review: actual spherical and row laws

Reviewer: Codex AI root agent, 2026-09-22. Decision: APPROVE the two identified proof modules at development stage. I did not author either module. I independently reviewed the statement boundary and authored the separate aspect-schedule proof, so this is not an independent final review of the whole problem and is not a human referee report.

I read both complete modules and independently rebuilt their actual sources against the frozen Definitions in a separate output directory. The exact three selected declarations `surface_probability`, `product_row_semantics`, and `independent_row_transport`, plus `gaussian_projection_law`, have only `propext`, `Classical.choice`, and `Quot.sound` in their transitive axiom reports. Compilation succeeded, with no diagnostics. The attached log and audit identify the actual checks.

`surface_probability` uses the literal `volume.toSphere`, proves its positive finite mass for n≥1, normalizes it, and pushes it into ambient Euclidean space; the unit-norm support calculation is an exact subtype preimage. No unproved uniqueness or spherical-distribution characterization is used. The row maps are measurable mutual inverses; independence plus the stated marginals gives the genuine finite product law, whose pushforward is exactly `matrixLaw`. The product-row theorem independently obtains the marginals, joint independence and simultaneous almost-everywhere unit norms. Empty row products are allowed, without imposing any rank condition. The scalar Gaussian projection bridge uses the actual Gaussian functional mean and variance and the unit-vector norm.

The frozen statements remain unchanged. These lemmas do not prove Gaussian polar independence, the trimming/concentration estimates, or the final canonical target. This local sparse-toolchain rebuild does not claim a full authenticated clean Linux run, LeanCert trust assertions, Comparator acceptance, or campaign completion; those remain mandatory for publication.
