# Independent module review: SphericalTrimming

Verdict: **APPROVE**, module-only. Reviewer: Codex AI agent `/root/infrastructure_audit`, 2026-09-22. The reviewer did not author SphericalTrimming.lean; author `/root/reference_review` did. The reviewer authored the SphericalLaw, GaussianPolar, GaussianMoments and SphericalMoments dependencies, so this is not an independent final review of those dependencies or the whole IE-21 package.

Reviewed source: `NLA/IE21/SphericalTrimming.lean`, SHA-256 `b32b30a9d219524df83319537388966f3632a03c2304ebcd34f6438780dda0de`. The complete source was read. The exact selected declaration `spherical_gaussian_trimming` has the same whitespace-normalized signature as frozen Challenge.lean, and a separate reviewer example with that full signature compiles. All eight rebuilt project source hashes match the author's evidence receipt. The four frozen mathematical-boundary hashes still match statement-freeze.json.

The generic absolute-first-moment bound uses MemLp at exponent two and the nonnegativity of the variance of |F|; measurability and square integrability are hypotheses, so it does not rely on a totalized nonintegrable first integral. The normalized radius calculation proves the integrable squared deviation and evaluates its integral to exactly 2/n, using n≥1 to justify division. This yields the literal sqrt(2/n) constant.

The coupling identity is proved pointwise, including g=0 through norm_smul_gaussianDirection; no nonzero vector restriction is silently imposed. Directional energy is nonnegative, permitting the absolute-value factorization. The proved Gaussian norm/direction independence factors the expectation into the radial absolute deviation and the directional energy mean, which is exactly one under the actual normalized surface measure. The factorization invokes Mathlib's measurable independent-function integral theorem, not a newly assumed joint law.

The selected bound transports the two population-trimming duals through the actual direction map and the Gaussian projection. It establishes integrability of both coupled variables, uses symmetry of the real inner product explicitly, identifies the projection lower-tail functional with the canonical Gaussian integral, and applies the proved coupling Lipschitz inequality. It preserves every frozen quantifier: all 0<θ<1, n≥2, and all unit x. There is no asymptotic replacement, numerical quadrature, inferred sphere distribution or extra concentration premise.

Fresh check command: `python3 reviews/spherical-trimming-referee-infrastructure-evidence/build.py`. Lean 4.33.1 (compiler 819816b2e0a3bf405af45ae5c7af2491d8f5bee6) compiled eight project modules and the separate signature/axiom audit in a new external build directory, all exit 0 and no Lean warnings/errors. All four public theorem closures are exactly `propext`, `Classical.choice`, `Quot.sound`. No implementation file imports Challenge or contains sorry, admit, a new axiom, unsafe, or native_decide. Sources were unchanged during the run. The exact commands, hashes and closures are retained in receipt.json/typecheck.log.

This macOS source check reuses the retained pinned upstream package build cache. It does not independently reauthenticate all cached upstream artifacts. No fresh Linux sandbox, LeanCert kernel assertion, full Lean4 Comparator or negative-control run is claimed for IE-21. The verdict covers this completed module only and does not promote the canonical problem status.
