# Asymptotic-tail development evidence

Author: Codex AI root, 2026-09-22. Helper scope: the finite probability bound remains an explicit premise of the limit helper until the final theorem supplies it.

The previously proved exact aspect schedule gives eventual n≥2, strict admissibility, ratio-error→0 and failure→0. For each fixed positive η, eventually the ratio-error is below η; monotonicity of the actual deviation events bounds their probability by the failure expression. Nonnegative real probabilities and squeeze yield the required limit. The separate transport lemma proves exact equality of the deviation probabilities on any measurable probability space with independent actual spherical rows, using the proved matrix pushforward and measurability of the ratio statistic. It imposes no cross-index coupling or aspect-rate restriction.

Both helpers compile without diagnostics; their transitive axiom closures contain only propext, Classical.choice and Quot.sound. This local author evidence does not substitute for independent review, actual Comparator, LeanCert assertions or authenticated full Linux verification.
