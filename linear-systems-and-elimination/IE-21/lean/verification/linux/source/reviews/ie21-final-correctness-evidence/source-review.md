# IE-21 independent correctness review

Reviewer: Codex AI agent `/root/ie21_final_correctness`. I authored none of the IE-21 definitions, statements, proofs, packaging or mathematical metadata. I did not treat an author's or earlier module referee's PASS as evidence of correctness. This is independent AI-agent review under the repository's adapted Tau Ceti review protocol, not external human peer review, official Tau Ceti endorsement, or a claim of novelty.

Verdict: **APPROVE the complete mathematical proof sources and the frozen 41-file source package**, bound by `final-source-review-receipt.json`. There are no unresolved mathematical or package findings. This approval is suitable for advancing to authenticated verification, not for promoting canonical status before the remaining gates. Authentic Linux LeanCert kernel/Comparator verification is a separate pending gate.

## Independently inspected scope

I read the entire canonical `linear-systems-and-elimination/IE-21/README.md`, the entire retained `references/colbrook-recovered-2026-09-11/manuscripts/IE-21-22.tex` (including the IE-22-only sections to verify the scope boundary), all of Definitions, all 23 Challenge statements, NUMERICAL_TARGETS, comparator.json, the dependency manifest/toolchain/lake configuration, and all 31 NLA/IE21 source modules. The detailed hashes are in `initial-input-hashes.json` and `rebuild-receipt.json`; `final-source-review-receipt.json` binds the source documents and final 41-file package freeze. I also read the repository's `docs/lean/REVIEW.md` and its reproducibility/attribution instructions.

The canonical claim is the limit in probability of the actual squared variational deletion singular value divided by the actual Euclidean operator norm squared, for every positive-dimension sequence with n tending to infinity and m/n tending to infinity. Exactly floor(theta*m) rows are retained. Quantitative bounds are part of this claim. No bound on the rate of aspect-ratio divergence, full-rank premise, Gaussian-row substitution or common sample space is introduced. The separate deterministic universal-constant IE-22 theorem is outside this problem's target and is not claimed here.

## Adversarial mathematical checks

**Literal semantics and infima.** MatrixSemantics proves actual coefficient multiplication and the operator-norm maximum on a nonempty compact Euclidean unit sphere; retained submatrices have their genuine Euclidean image norm and exact sum-of-squares identity. The deletion domain is the finite set of cardinality-floor selections times the compact nonempty sphere. An admissible selection is proved to exist, so the real infimum cannot succeed through an empty-set convention. Attainment and comparison hold for every admissible pair. Zero retained rows and a nonzero kernel vector explicitly give zero, without excluding rank-deficient maps. FiniteTrimming likewise proves finite attainment, all-selection comparison, and attained threshold duality, including k=0 and tied values. Directional minimum then proves the exact n/m scaling and interchange of minima, using nonnegativity before squaring.

**Probability laws.** SphericalLaw starts with Mathlib's actual Euclidean Haar-to-sphere measure and proves its positive finite mass before normalization. RowLaw proves probability, unit-row support, coordinate marginals and independence for the literal product pushforward, then equality in law for every independent random matrix with these marginals. GaussianPolar derives the standard Gaussian radial density from the finite product of univariate densities and volume-preserving Euclidean coordinates. It uses the genuine polar measure-preserving map, proves Gaussian zero mass, normalizes the product by finite nonzero surface mass, and obtains radius-direction independence and the exact direction law. These are conclusions, not hidden assumptions.

**All moments and MGF.** GaussianMoments obtains all even radial moments by the exact Gamma-integral recurrence and normalizes at order zero. Its second and fourth moments prove the stated radial variance with explicit integrability. SphericalMoments factors radius and direction using the proved independence, derives the complete product formula for arbitrary natural r (including r=0), and proves the exact 2^r*r! bound. The moment denominator is proved positive. SphericalMGF proves absolute centered moments, uses an integrably dominated exponential series, cancels the mean-zero first term, and bounds the remaining geometric tail with 4*abs(a) <= 1/2. The 32*a^2 constant and both signs of a are preserved; boundedness and integrability are supplied rather than assumed.

**Gaussian constant and population optimization.** GaussianTrimming establishes continuity and strict increase of central Gaussian mass, exact positive quantile existence/uniqueness, identification of the infimum-defined cutoff and equality of the literal canonical density integral with the truncated second moment. It proves the constant lies in [0,1]. PopulationTrimming proves nonempty, bounded-above dual objectives, law invariance and the coupling Lipschitz estimate without a supplied optimizer or conditional-expectation axiom. Its Gaussian dual bridge is exact. SphericalTrimming uses the established radius-direction independence and mean-one directional energy to reduce the coupling distance to radial deviation; the exact second moment and Cauchy-Schwarz give sqrt(2/n).

**Five tails, ties, atoms and floor.** QuantileTrimming proves a mass-straddling quantile for arbitrary real laws, not just atomless ones. For nonnegative mean-one data its threshold is bounded by 1/(1-theta). The fractional selector uses exact mass below the threshold plus an explicit fractional contribution at an atom; the zero-atom-mass division case is handled separately. It has exact expectation theta and attains the population dual. EmpiricalTrimming compares the exact cardinality-constrained finite minimum with this selector using an attained threshold in [0,L], including ties and k=0. TrimmingConcentration separately supplies two selector tails, two weighted-selector tails and one count tail. Hoeffding is invoked for independent bounded samples with the correct common means; no independence between these three families is asserted or needed. The count event follows from the proved mean/Markov estimate with L=2/(1-theta). The deterministic estimate includes the literal floor discrepancy bounded by one, yielding exactly 2*L*epsilon+L/m and 5*exp(-2*m*epsilon^2). The matrix-to-row-energy normalization is proved, so this does not silently certify a differently scaled statistic.

**Finite nets and genuine operator control.** SphereNet uses disjoint ambient balls and finite positive volume cancellation to prove exactly (1+2/delta)^n, with actual unit-sphere centers and coverage of every unit vector. It does not replace the target by finitely sampled directions. QuadraticNet proves the symmetric operator bound by Rayleigh quotients, with the 1/4-net factor exactly two. Chernoff uses integrable independent exponential products at +/-s/64; the exponent is exactly -m*s^2/128. CovarianceConcentration then takes s=t/2, giving the exact denominator 512 and 9^n net size. Its conditional helper receives the fully proved MGF in the selected Covariance wrapper.

**Uniform event and ratio.** UniformTrimmingGeometry identifies normalizedOperator with the norm of the actual scaled adjoint composition; it does not use an entrywise matrix norm. Submatrix norm domination and the exact finite minimizers produce the 2*(1+t) directional Lipschitz constant. The all-directions GoodEvent is a closed set, as an arbitrary intersection of closed directional constraints, hence Borel measurable. UniformTrimming supplies all analytic inputs to the generic union-bound helper and yields the exact sum of the covariance and five-tail probabilities. FiniteSizeGeometry proves positive denominator on this event, attained directional deletion control, and the ratio estimate with h_theta in [0,1]. FiniteSize proves failure of any of the simultaneous finite-size conclusions is contained in the bad event. The declared failure bound need not be below one; no hypothesis is inserted to force it to be informative.

**Every divergent aspect ratio and changing spaces.** AspectSchedule proves delta=32*sqrt(log(Q)/Q) tends to zero and is eventually admissible. Its exact identities give m*delta^2=1024*n*log(Q), while the net base is eventually at most Q. Both failure terms are bounded by a combined 7/Q, with no growth-rate condition involving n. It separately derives m tending to infinity, the radial error tending to zero, and the ratio denominator tending to one. AsymptoticTail uses event inclusion and a squeeze argument for every eta>0. FiniteSize supplies the real finite theorem to its conditional helper. The final original_random_row_limit uses equality in distribution at each index independently and allows arbitrary changing probability spaces. No unproved asymptotic or concentration premise survives in the selected theorem.

## Independent mechanical evidence

`rebuild.py` topologically compiled all 31 NLA project modules with pinned Lean 4.33.1 into the fresh reviewer-only directory recorded in `rebuild-receipt.json`. The shared Mathlib/dependency artifacts were cached, so this run is not independent source authentication of dependencies. No project author's compiled artifact was used. The source modules compiled with zero warnings/errors.

`ExactTypesAndAxioms.lean` independently restates each complete frozen Challenge type as an explicit dependent function type and checks the implementation constant against it without importing Challenge. All 23 type checks succeeded. All 23 transitive axiom closures were exactly `propext`, `Classical.choice`, `Quot.sound`; there was no sorryAx or custom/native axiom. A textual inspection and scan found no proof placeholder, unsafe declaration, native_decide, external implementation or Challenge import in the proof modules. The intentional reference placeholders remain only in Challenge and are not proof inputs.

The first generated reviewer audit emitted unused-binder linter warnings because the complete quantified function types intentionally retain all original premise names. That audit still compiled with exit code zero and the correct axiom closures. Its log is preserved. Only the reviewer audit's unused-variable linter was disabled, then that audit alone was rerun against the unchanged independent build with zero warnings/errors. The resolution and exact commands are retained; no project proof or mathematical boundary was altered.

This local source approval does not itself execute LeanCert, establish authentic Linux Comparator acceptance, authenticate dependency Git checkouts or promote canonical status. Those gates require separately inspected completion evidence. The final package preserves every reviewed mathematical hash. I inspected the added Solution wrapper, final lake configuration, metadata, README and LICENSE as described below.

## Proof quality and reuse

The implementation minimizes numerical computation through exact algebra, analytic integration, compactness, finite-cardinality reasoning, genuine measure transport and mathematical concentration arguments. No finite sampling, approximate quadrature, interval narrowing or weakening of an original quantifier is used. Existing Mathlib Gaussian, Haar-to-sphere, CDF, sub-Gaussian, Rayleigh and covering APIs are used rather than replacing their semantics with custom assumptions. Module-level conditional helpers are legitimate reusable lemmas because their inputs are discharged explicitly in every selected full theorem. There are no unresolved mathematical findings.

## Final source-package check and finding resolution

The root confirmed the package freeze. I independently verified all 41 packaged file hashes and that every mathematical source/Challenge/numerical/configuration hash matched my independent rebuild; the sole changed preexisting build input was the reviewed lake configuration that switches the default target from Challenge to Solution and declares the Solution library. I read the complete final Solution, formalization.yaml, README, lake configuration and Apache license. Solution imports the actual LeanCert verification module, explicitly sets kernel trust, and has exactly one kernel assertion and one transitive-axiom print for each of the 23 selected declarations. It does not import Challenge. I did not execute this wrapper with the sparse local toolchain; authentic LeanCert execution remains pending and is accurately stated in the metadata.

Two documentation/package findings were resolved without mathematical changes. The initial wrapper referenced a missing LICENSE; the fidelity reviewer requested its addition, and I independently observed the absence and checked the subsequently added license equals the repository's existing Apache-2.0 template byte for byte. I also requested that project.description explicitly name the squared deletion singular value, since the original prose omitted the numerator's square. The root made that metadata-only correction. The current package freeze records the resulting exact bytes and the correction history.

Formalization credit is George Stepaniants with Department of Computing and Mathematical Sciences, California Institute of Technology; no contact email was added. Matthew J. Colbrook remains identified as the original mathematical proof author, separately from the formalization author. Automation, author roles, local-build limitations and still-pending final gate status are disclosed. The preserved statement draft language is explicitly identified as historical rather than a claim that the present proofs are absent. The metadata does not claim an actual Linux or Comparator run that has not happened. Subsequent truthful gate/status metadata updates require an explicit delta review while keeping this source approval and its historical hashes intact.
