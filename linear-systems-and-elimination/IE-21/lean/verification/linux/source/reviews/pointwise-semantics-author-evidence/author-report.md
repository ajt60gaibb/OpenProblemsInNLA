# Pointwise trimming semantic bridge author evidence

Author: Codex AI `/root/canonical_inventory`. This is author evidence, not independent review or complete IE-21 verification.

Stable source: NLA/IE21/PointwiseTrimmingSemantics.lean SHA-256 `7db858d56b5e6ce949eaf7a0b2c7e1c82ea56c7839b29891bf1ad95bad187456`.

`finiteTrim_smul_nonneg` proves exact scaling of the genuine finite minimum for k≤m, c≥0 and arbitrary real data. Both inequalities use attained minima; no strict positivity, distinct values, or special handling by an empty-infimum default is needed. It includes c=0 and k=0.

`directionalTrim_eq_energy_trim` proves the exact frozen matrix statistic equals the row-energy trimmed sum divided by m. It uses the actual matrix-row inner-product identity and nonnegative scaling by n. No positive-dimension, positive-row-count, unit-vector, or rank assumption is added; totalized division handles m=0 consistently. The only θ restriction guarantees the retained count is feasible.

Fresh eight-step build `/private/tmp/nla-ie21-pointwise-semantics-author-tepqbv_0` rebuilt Definitions, all stable local dependencies, this module, two literal applied signature checks, and two transitive axiom checks. All exits were zero with no warnings or errors. Both declarations use only propext, Classical.choice and Quot.sound. No proof placeholder, Challenge import, native computation or custom axiom was added. Frozen source files and all previously stable modules were preserved. No new selected target is claimed by this helper module.

Log SHA-256 `746c24589b59e3a2237d684dd39a42b525fd85cc0941989ea551fcf40512fcb3`.
