# Spherical and Gaussian modules: author evidence

Author/operator: Codex AI subagent `/root/infrastructure_audit`. This is author evidence, not an independent referee approval or full IE-21 verification claim. All six modules are frozen at the hashes in `complete-stable.json`.

The exact selected declarations `surface_probability`, `gaussian_surface_correspondence`, `product_row_semantics`, `independent_row_transport`, `spherical_moments`, and `spherical_quadratic_mgf` are proved. The original definitions, Challenge, numerical boundary and Comparator configuration are unchanged from the two-referee statement freeze. Original proof attribution remains Matthew J. Colbrook; the requested formalization credit/affiliation is preserved without a contact email.

The proof identifies the literal normalized Euclidean `volume.toSphere` law with Gaussian direction by proving the finite-product Gaussian radial density, transporting it through the existing measure-preserving polar homeomorphism, normalizing the factors, and handling the null origin explicitly. A Gamma-integral recurrence gives every even Gaussian radius moment; its first two cases prove the exact radius assertions. Radius/direction independence and standard Gaussian projections yield all spherical moments, including the empty-product order-zero case. The mean and factorial bounds then prove the MGF estimate: centered absolute moments are bounded by `4^r r!`, bounded exponential series may be integrated by dominated convergence, the first-order term cancels, and the geometric tail is at most `32*a^2` for `abs(a) <= 1/8`.

A fresh external build at `/private/tmp/nla-ie21-spherical-author-final-build` recompiled unchanged Definitions and all six modules, followed by `CompleteSphericalAudit.lean`; all eight commands exited zero without diagnostics. All six selected closures and both additional series/moment audit closures contain exactly `propext`, `Classical.choice`, `Quot.sound`. The receipt script checked the frozen boundary hashes and absence of `sorry`, `admit`, axiom declarations, unsafe/native proof shortcuts or Challenge imports in the six modules. Prior failed development iterations are retained separately.

This used the campaign's cached macOS Lean 4.33.1 dependencies. It is not a replacement for the later authenticated fresh Linux LeanCert kernel assertions, full Comparator equality check and real sandbox controls. No status promotion or PR is justified by these modules alone. Independent source review and full-target assembly remain required.
