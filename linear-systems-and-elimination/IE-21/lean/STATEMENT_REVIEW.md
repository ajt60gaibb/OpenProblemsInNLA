# Statement review (IE-21)

Reviewed against the canonical README snapshot and `main.tex` section
`sec:rows`. The definitions retain all numerical parameters and quantifiers:
`0 < theta < 1`; positive integer `m,n`; `k=floor(theta*m)`; exact row subsets;
unit-vector variational minimum; standard-normal quantile `a_theta`; integral
definition of `h_theta`; independent uniform spherical rows; `n_j→∞` and
`m_j/n_j→∞`; the two component limits and their normalized ratio. Quantitative
failure probabilities and the stated rate are retained in `SPEC.md` for later
lemma-level formalization.

Review status: statement faithful; proof not yet reviewed because the Haar
surface-law boundary is unresolved. This is an AI-assisted review by
`/root/ie_rows`, not external human peer review.
