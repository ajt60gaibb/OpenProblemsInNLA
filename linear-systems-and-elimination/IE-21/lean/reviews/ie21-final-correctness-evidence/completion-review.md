# IE-21 independent correctness referee: Linux completion addendum

**Verdict: APPROVE the complete IE-21 mathematical target and the authenticated Linux completion gate for the exact source package identified below. No blocking findings.** This supplements, without changing, my complete [source review](source-review.md) and [source receipt](final-source-review-receipt.json). Publication status, canonical prose, and metadata changes made after this review require a separate delta review; none is preapproved here.

Reviewer: Codex AI agent `/root/ie21_final_correctness`, 2026-09-22. I authored none of the IE-21 definitions, statements, proofs, wrapper, checker, or verification operation. This is independent AI review, not human peer review, official Tau Ceti endorsement, or a novelty claim. I independently rebuilt all 31 project modules and checked the exact 23 statement signatures and axiom closures during the earlier source review. **For this completion addendum I inspected retained evidence and ran local hash/log audits; I did not execute a new Lean, Comparator, dependency build, or Linux VM run.**

The completed execution is `verify-20260922T205303Z-973`, starting at `2026-09-22T20:53:03.368999+00:00` and finishing at `2026-09-22T20:55:38.670324+00:00`, exit 0. Its publication input commit is `1eb284b84ecc0d3c958d022b3e020be7fa111391`. The guest commit `bd72d630841a1660b9f7652d469186c11728925f` is an intentionally distinct local verification commit containing identical project bytes. The records correctly distinguish these commits and identify the operator as an author of some Gaussian/spherical modules, rather than an independent final referee.

I wrote and executed [an independent binding audit](completion-audit.py), retained its [successful output](completion-audit.log), and separately reran the supplied evidence audit with [its output](completion-operator-audit.log). My audit read every one of the 289 publication Git blobs and compared its SHA-256 against the live project, retained `source/` snapshot, input archive, preparation receipt, successful result, input-authentication record, and read-only dependency observer. All matched. No compiled project artifacts entered that input. The input archive is 1,228,800 bytes, SHA-256 `6b61efee12a30cde167f1e2ac22b1b0662be0324475e37b222e3cd8ee466c6dd`. I compared all 326 ordinary files in the exported evidence archive to their retained bytes and checked all 350 entries of the evidence manifest. The exported archive is 346,734 bytes, SHA-256 `ba04ba6bc5973591c25b3fe6729c683d85966d3dac375bbd2083c8e58d1d5853`; the manifest hash is `e684400fe12bb40fe5b685bdb9ff64241fe8435e30b1ebaf6e0362dec13528ab`.

All four mathematical boundary files still match the independently reviewed preproof freeze. All 41 package files match the final source approval and freeze `b4ab97645ec9f8c46c7130aabc4bac48cbab48af0c96d4f454ad58c32a981744`. My original report, receipt, exact-type audit, build logs, and explicit resolution of audit-only unused-binder warnings are unchanged. The canonical README and complete retained manuscript still have the hashes read in my source review. The Linux evidence therefore applies to the full mathematical statements I reviewed, including the original random-row convergence theorem, not merely a helper or a weakened conditional result.

I read the complete nine successful-verification logs, the input/transport/preparation/execution/dependency/export records, preparation and observer scripts, the driver's snapshot/control/verification code, the strict sandbox adapter, relevant authentic LeanCert implementation, and the operational record. The retained shared checker files match their publication Git blobs. The validated bootstrap and runtime hashes bind Forsythe commit `8d1b0c0545a77b40245e84705aa7d273e6c81e62`, Lean 4.33.1 and the recorded Comparator/exporter/Landrun binaries. The older kernel version in the bootstrap records the bootstrap environment; the execution inspection records the current `7.0.0-31-generic` guest. The temporary AppArmor namespace prerequisite restoration is recorded against the unchanged CI workflow and does not replace any isolation control.

All ten actual dependency Git HEADs recorded by the read-only observer equal the exact manifest revisions and URLs. In particular Mathlib is `0df444a360eaa60ab8c11dca51a86af692955474` and LeanCert is `621a43d7cf21f87872392a01e874f2f1dbddc926`. The captured authentic `LeanCert/Tactic/Verification.lean` hashes to `2c576708b528acdde17796b0b715b83079f9cad377182dbd87c248323ff0a58c`; the observer compared it byte-for-byte with its pinned Git blob. I inspected its actual `#assert_trust` implementation: it collects the transitive axiom closure, rejects sorry/custom axioms, and rejects native-compiler axioms in kernel mode. Its explicit kernel certificate branch cannot fall back to native. This is evidence of the recorded source/HEAD checks, not a claim that I independently re-fetched remote repositories. The Mathlib log explicitly reports reuse of 8,689 cached files and no downloads; it does not establish a source rebuild of every upstream object.

The actual project Comparator log builds Definitions and Challenge first, exports all 23 frozen Challenge declarations, then builds the remaining project modules, authentic LeanCert verification, and Solution. Every one of the 31 project modules is present in the build log. Solution imports the authentic verification module, sets `leancert.trust "kernel"`, and executes exactly one kernel assertion and axiom print for each selected declaration. All 23 printed closures are exactly `propext`, `Classical.choice`, and `Quot.sound`. The only warnings are the 23 intentional Challenge placeholders; no Solution or proof-module warning/error appears. Both complete export lists contain exactly the frozen 23 IE-21 declarations in order, including `original_random_row_limit`. The real final replay reports “Lean default kernel accepts the solution” and “Your solution is okay!”. Temporary raw export streams were removed by the pinned Comparator; retained export lists and execution results are not misrepresented as raw-stream hashes. This analytic proof requires no numerical approximation certificate; authentic LeanCert supplies the requested kernel trust audit.

I inspected the controls individually. Both build and export sandbox modes pass private user/PID/mount/network/IPC/UTS namespaces, host process/signal isolation, denied host loopback and AF_UNIX access, zero capabilities and `no_new_privs`. Outside-file writing, truncation, creation, and symlink escape are denied. Only the designated build `.lake` is writable; export `.lake` writes/truncation are denied. Nested namespace attempts are rejected at UID-map setup, not claimed to execute successfully. All four unsupported/writable-path adapter cases exit 2 as expected. The actual kernel probe accepts the honest inductive/quotient fixture, rejects an invalid raw proof, and rejects the quotient postcheck mismatch. All five Comparator regressions pass; the fixture named `simple_kind_mismatch` rejects an illegal helper axiom, while `type_mismatch` checks theorem-type disagreement. Separate sorry and native fixtures fail with the precise expected forbidden axioms and exit 1. Those expected failures are successful controls, not failed IE-21 verification attempts.

Together with the unchanged whole-source mathematical review and my earlier independent build, this completes my correctness gate for the exact IE-21 source input. The attached [completion receipt](completion-receipt.json) records the review and evidence hashes. Any later documentation/metadata delta is outside this receipt's approved bytes and must preserve this historical execution provenance.
