# Finite-size deterministic assembly

Author: Codex AI root, 2026-09-22. These helpers do not complete the selected probabilistic finite-size theorem without the good-event probability bound.

The exact attained directional minimum transfers the all-direction trimmed-energy good-event estimate to normalizedDeletion. Reverse norm triangle transfers covariance error to the absolute normalizedOperator error. For h in [0,1], |u−h|≤e and |v−1|≤t<1 imply v>0 and |u/v−h|≤(e+t)/(1−t). Literal cancellation of the positive n/m factor identifies the ratio with the original deletionSingular²/operatorNorm², including the total division convention; positive normalizedOperator then proves the actual operator norm is positive. The resulting goodEvent_statistics proves all simultaneous finite-size conclusions with the frozen constants and no retained-rank condition.

The exact sources and three public transitive axiom closures compile without diagnostics under the pinned local development toolchain, using only propext, Classical.choice and Quot.sound. Independent review, full Linux reproduction, LeanCert assertions and actual Comparator remain mandatory.
