# Exact covariance concentration development evidence

Author: Codex AI root, 2026-09-22. The complete frozen `covariance_concentration` is now implemented.

The literal independent product spherical rows yield independent measurable centered directional energies. Marginal pushforward integration transports both exponential integrability and the proved exact spherical MGF bound. The generic Chernoff theorem gives 2 exp(−m s²/128); the quadratic identity expresses the centered covariance form as precisely the average of these centered energies. The proved quarter-net has cardinality at most 9^n and any covariance norm failure produces a net quadratic failure at t/2. The finite union bound therefore gives exactly 2·9^n exp(−m t²/512). The selected theorem discharges the helper's explicit MGF premise using the proved `spherical_quadratic_mgf`; no unproved input remains.

Both helpers and the selected wrapper compile without diagnostics, and all three transitive axiom closures contain only propext, Classical.choice and Quot.sound. The local author evidence does not replace independent review, actual Comparator, final LeanCert assertions or full authenticated reproducible Linux verification.
