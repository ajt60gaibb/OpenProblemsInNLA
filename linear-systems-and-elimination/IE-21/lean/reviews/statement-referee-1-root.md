# IE-21 independent preproof statement review

Date: 2026-09-22. Reviewer: `/root`, Codex AI agent. I did not author these definitions or Challenge statements. I independently read the complete canonical IE-21 README, complete retained Colbrook IE-21/IE-22 manuscript, Definitions, all 23 Challenge declarations, NUMERICAL_TARGETS, Comparator configuration and package pins. This is a mathematical statement and scope review, not proof verification, external human peer review or official Tau Ceti endorsement.

**Verdict: APPROVE the exact boundary recorded in the adjacent root receipt.** Proof implementation must still wait for the second independent statement approval and an explicit freeze. No proof is established by this verdict or by the intentional Challenge placeholders.

The final random-row limit has the original quantifiers: every fixed theta in (0,1), every sequence of positive matrix dimensions with n tending to infinity and m/n tending to infinity, every positive error threshold, and independent uniform spherical rows. The last theorem permits arbitrary changing probability spaces and imposes no coupling across dimensions or additional aspect-ratio growth. Positive matrix dimensions are the ordinary dimensional convention of the original statement; finite-size n>=2 is eventually satisfied and is not a restriction on the sequence conclusion.

The finite quantitative target is also compulsory. Its covariance, trimmed-energy and ratio estimates are simultaneous, with explicit denominator positivity, source constants, strict parameter domains and the floor correction L/m. The weaker threshold F>1 merely makes the probability estimate uninformative, not an excluded case. The exact Gaussian density integral is the canonical constant. Its cutoff positivity, uniqueness and exact mass theta are required conclusions, not supplied assumptions.

The underlying norms are correct: Space is real EuclideanSpace, matrixMap comes from Matrix.toEuclideanLin, and operatorNorm is the actual continuous linear map norm. Retained rows are a genuine subtype-indexed matrix. The coefficient action and sum-of-squares bridge are required. The deletion infimum ranges over exactly floor(theta*m) rows and unit vectors; its nonnegative, attained-minimum and universal lower-bound theorem rules out empty-infimum defaults. Explicit zero-row and kernel cases include the rank-deficient variational convention. Finite trimming also has attained primal and dual extrema; directional_minimum gives the required interchange of minima.

Uniformity is actual normalized Euclidean surface measure from volume.toSphere, pushed into Euclidean space. Finite positive mass, normalization and sphere support must be proved, including dimension one. Gaussian normalization is separately defined with an explicit value at zero. Its equality in law with surface measure, nullity of zero, independence from radius and exact radius moments are mandatory proof obligations. Product-row probability, independence and marginals must be proved; independent_row_transport then covers any independent random rows. No probability or concentration conclusion is hidden in IndependentSphereRows.

The population trimming dual is an auxiliary proof interface; it does not define the final Gaussian constant or assume an unproved quantile/selector equivalence. The spherical comparison and concentration theorems must establish its actual behavior. GoodEvent quantifies over every unit direction, with measurability required separately. The finite net is a proof device, not a restriction of the target to finitely many vectors.

I checked the source constants mathematically. The moment identity has the correct odd numerator and n+2j denominator, including the empty product at r=0. The centered absolute-moment geometric series gives 32*a^2 for |a|<=1/8. Chernoff and the quarter-net yield exponent -m*t^2/512. Bounded truncated values/counts and the below-L count give five tails and 2*L*epsilon+L/m; the k=0 case is covered. The covariance event gives the all-direction Lipschitz term 2*(1+t)*delta. Independent Gaussian radius coupling gives sqrt(2/n). Combining the normalized operator bound with h_theta<=1 gives the displayed ratio error divided by 1-t.

The explicit aspect schedule is not a hidden stronger growth assumption. Writing Q=m/n, its square is 1024*log(Q)/Q, so the first failure exponent is n*(log(9)-2*log(Q)); the second is n*log(1+2/delta)-2048*n*log(Q). Both tend to minus infinity for any Q tending to infinity with n tending to infinity. Log(Q)/Q tends to zero, so every fixed-theta parameter restriction eventually holds and the ratio error tends to zero. These facts remain Lean proof obligations.

Comparator selects exactly all 23 Challenge names, including the quantitative theorem, arbitrary-space quantitative transport and both final limit statements. There are no replaceable definition holes. The permitted set is exactly propext, Classical.choice and Quot.sound. No implementation/Solution exists yet. A future complete proof must discharge every selected intermediate interface, including the substantial sphere/moment/net foundations; a conditional wrapper or partial quantitative lemma would not finish IE-21.

I independently typechecked Definitions and Challenge using the pinned Lean 4.33.1 executable and existing exact-pin package cache into a separate external output directory. The retained root-statement-typecheck.log records the results. This checks types only and makes no claim of fresh Linux replay or dependency authentication. Final verification must use the shared reproducible kernel/Comparator infrastructure and two nonauthor final referees.

Original mathematical proof attribution remains Matthew J. Colbrook. George Stepaniants has the requested Department of Computing and Mathematical Sciences, California Institute of Technology formalization credit, without adding a contact email. Canonical files, IDs and prior verifications were not changed by this review.
