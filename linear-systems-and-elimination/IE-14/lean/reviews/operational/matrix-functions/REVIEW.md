# IE-14 independent overlap and verification review

**Verdict: approve the complete mathematical coverage and the actual seven-export Linux verification at commit `6e48f25fffdae2cf93e4985dc515abbd15e0481b`. Do not duplicate this IE-14 proof.** No mathematical or mechanical defect was found. Repository promotion and an upstream pull request remain separate unfinished publication steps.

Reviewer: `/root/next_matrix_functions`, an independent OpenAI Codex AI agent. I wrote no definitions, Challenge, certificates or proof code in this upstream package. I previously reviewed our separate, unpublished 26-export IE-14 statement proposal; that proposal is not a dependency of this review. This is not external human peer review or official Tau Ceti endorsement.

## Exact boundary and actual evidence

Repository: `ajt60gaibb/OpenProblemsInNLA`, branch `codex/lean-ie14`, immutable commit above. I obtained the public Git tree, downloaded the text sources and verified their Git blob hashes from the exact tree, then separately matched every one of the **143 actual CI input SHA-256 values** to those downloaded files. The complete snapshot manifest is `SNAPSHOT-COMPLETE-MANIFEST.json`; raw API responses and downloaded CI artifacts are retained beside this report.

Actual normal-push run: [35035525244](https://github.com/ajt60gaibb/OpenProblemsInNLA/actions/runs/35035525244), attempt 1. Its actual IE-14 verification job is **104603738111**, artifact **10423009444**, named `lean-IE-14`. The API reports success for the exact reviewed commit. I read the downloaded raw logs and result, rather than treating that green indicator as sufficient.

The local `audit.py` records **1,135 successful checks**, including all immutable snapshot hashes, all 143 actual-run inputs, 23 sealed final-source inputs, ten frozen pre-proof inputs, both pre-proof referee-report hashes, every configured theorem signature, dependency revisions, schema/coverage and actual control outcomes. `CHECKS.json` has SHA-256 `1e8016fb58bd53f8d60867954cc2e7f16c5bb4dbf999e5546f5c0897b19d38b8`. The script also records its limited independent rational diagnostics. I did **not** run Lean, Lake, local compilation, cache restores or downloaded compiled proof artifacts.

The canonical README and complete Colbrook manuscript exactly match published base `d8c38a795876b132c90df8d1be8682d3dcde394c`: SHA-256 `ebbd61a71ba0aa17f4bd076f1f2408e5db63aa3426247b4e4db0bcb0b582d869` and `4c20c2c20e6d05945911883682bc823096ef0ff1011cbd31bf941fd1f309b869`, respectively. All 19 reviewed shared-harness/workflow/protocol/source-context files remain byte-identical to that base; see `BASE-PRESERVATION.json`. The original ID, canonical path and target were not changed.

## Mathematical scope and proof inspection

I read the complete original target, full retained manuscript, all twelve `NLA/IE14` modules, the seven Challenge declarations, the numerical dossier, proof entrypoint, metadata and both final source-referee reports. The result is the original full extremum

\[
 c_n=F_{n+1}+1\qquad(n\ge4),
\]

with an **attained greatest value**, not just a numerical example or lower bound. Its seven exports are numerical bounds, exact maximum semantics, full admissible-path existence, the universal active-entry upper bound, witness data, witness attainment and the greatest-value/supremum result. Fewer public intermediate exports than our separate proposal do not reduce the final scope.

- **Inputs and actual algorithm.** `Mat n` is complex. `CyclicInput` requires actual determinant nonzero, the original three bands and two permitted corners, and both corner entries nonzero. All band entries may vanish. `trajectory 0=A`; subsequent steps perform literal physical row swaps, exact division by the selected pivot and the trailing Schur update. There is no preliminary reordering. `AdmissiblePath` permits every maximal-modulus active-column tie and requires nonzero pivots. Every active entry of stages 0 through n−1 contributes to growth, including the input and last scalar.
- **No vacuity.** `GEPP.lean` proves complete path existence for every nonsingular complex square matrix. It maintains supported-vector injectivity of the actual active block, obtains a nonzero active column and a finite maximizing pivot, and lifts any new Schur kernel vector to an old one. It never assumes that the zero-padded full matrix stays nonsingular. Dimension zero in this auxiliary existence theorem correctly admits the empty path; the sharp theorem requires n≥4.
- **The front is derived, not assumed.** `origin`, `frontPair` and `FrontInvariant` track the actual composed swaps. Every later original row stays active with unchanged active entries until entering the front. Sparsity and pivot nonzeroness force the pivot into the two-old/one-fresh front. The induction checks original labels, distinctness, coverage and survivor positions. The norm update uses a multiplier of modulus at most one and handles all three possible removed rows; it does not require the pivot to maximize an unrelated target-column entry.
- **All columns and endpoint cases.** The zero-fresh maximum/sum inequalities M′≤T and T′≤T+M prove the Fibonacci run. The last column has its correct nonzero fresh arrival before the final two-row bound. The penultimate column receives two bounded arrivals, with a zero-length initial run when n=4. First, second and all middle columns have their full active histories bounded separately. Future rows retain the original entry bound. The final partition exhausts every active stage and column, and handles the last scalar separately.
- **Actual attaining input and path.** Unit-lower L and upper U, followed by the explicitly defined inverse row assignment, define the input. Triangular diagonal products and an actual finite permutation prove determinant nonzero. `WitnessEntries` proves support, corners 1 and −1, and maximum one, including the exceptional second column and last-column Fibonacci cancellation. `Tail` proves the active trailing-product identity in physical row coordinates under the specified current-last-row swaps. Nonzero U pivots and lower-factor norm bounds prove each actual pivot legal. The final scalar is exactly F_(n+1)+1.
- **The supremum is justified.** The initial nonzero pivot makes the denominator positive. Finite maxima pass the all-entry theorem to growth. The actual normalized witness supplies a member and the matching lower bound. `IsGreatest` explicitly proves membership and universal domination; `csSup_eq` then uses its actual nonempty least-upper-bound property. No empty-set, unbounded-set or inverse convention manufactures the result.

My separate standard-library Fraction script reconstructed L, U and the input directly from the frozen definitions for n=4,…,14. It checked support/corners/normalization, every physical pivot, every active tail identity, observed pivot labels and the complete growth. All eleven witnesses passed; the initial values are 6, 9, 14, 22. These finite rational checks are supplementary only. They do not prove arbitrary complex inputs or the universal all-dimensional theorem; those are established by the inspected Lean proof and the actual kernel acceptance.

## LeanCert, Comparator and actual operational checks

`Certificates.lean` proves 0<1/2 and 1/2≤1 with two explicit `interval_decide (trust := kernel)` calls. Both are consumed: the first proves the complex half pivot nonzero, and the second bounds the half entries of the witness. The actual target proof calls those helpers through determinant/path legality and normalization. The retained independent compiled dependency audit confirms both paths in `witness_data`, `witness_attainment` and `canonical_result`. This is a small genuine numerical certificate; dimensions, recurrence and permutations remain symbolic.

The actual Linux artifact records:

1. Fresh Challenge and Solution builds from the exact Git snapshot; the seven Challenge holes are deliberate specifications, and no solution import reaches Challenge. Solution completes **3,642 jobs**, with each of the seven target closures reporting only `propext`, `Classical.choice`, `Quot.sound`.
2. Export of all seven declarations from both Challenge and Solution, followed by the explicit messages **“Running Lean default kernel on solution.”**, **“Lean default kernel accepts the solution”** and **“Your solution is okay!”**, with exit status zero.
3. Actual rejection of a `sorryAx` fixture and a native-decide axiom fixture, each with exit status one for the stated illegal axiom.
4. All five Comparator regressions, including the theorem-type mismatch case. The matching example passes; the wrong types/kinds and extra axioms are rejected.
5. The actual default-kernel replay controls: honest proof accepted, invalid raw proof rejected, and quotient post-check mismatch rejected.
6. Both real sandbox modes pass. They use UID 1001, private namespaces, read-only outside-project access, no effective capabilities and no-new-privileges; host loopback and AF_UNIX access are denied. Build writes are limited to the allowed `.lake`, export cannot write it, and malformed sandbox options are rejected. The separate workflow `checker-controls` job is skipped because shared tools were unchanged; the **per-project actual verification did run all controls**, as its logs show.
7. Lean 4.33.1 on x86_64 Linux, the pinned Forsythe checker source lock `b3833b07916e5db77579b9cc53ca582282f6a841f36d6a60d693e5b02d342b6b`, LeanCert `621a43d7cf21f87872392a01e874f2f1dbddc926` and Mathlib `0df444a360eaa60ab8c11dca51a86af692955474`. Every manifest dependency revision is present in the actual materialization log.

The unchanged harness copies only ordinary tracked Git blobs to a fresh directory, forbids tracked build artifacts, checks source hashes after dependency/cache setup and after Comparator, and writes the accepted result only after those checks. No target-specific harness exception was introduced. I independently verified the metadata against its pinned v0.4 schema and checked its seven declared results against Comparator.

## Referee independence, reuse and authorship

The two retained final source reviews are by independent non-implementing agents `/root/reference_api_review` and `/root/existing_verification_audit`. Both bind the same 23-source seal and all ten pre-proof bytes. They explicitly distinguish their local macOS compiler checks from the then-pending Linux gate. Those historical local checks are not being misreported as this reviewer's work or as the current Linux result. Their final reports have SHA-256 `0e3a69d8449a497751f1a62de3ccef81ea6492517630a3418b26d5af4af99830` and `c8df1d4ec2da9bea66cce72a23608eed416e7f371a8d366f41fcedd0eb032d27`. The retained earlier elaboration finding and subsequent repaired source are transparently documented, and current proof bytes match their final seals.

The proof uses Mathlib finite maxima, complex norms, matrix injectivity and triangular determinants, finite permutations, Fibonacci identities and conditional suprema. The existing IE-05 active-block and tail-product patterns are adapted with attribution; no real or rook-pivot theorem is silently substituted for complex GEPP. Maximum/sum recurrences avoid unnecessary sorting, symbolic triangular determinants avoid expansion, and carrying the entry scale avoids an unnecessary normalization proof. The module organization and thin public wrappers keep the approved contracts identifiable. Forsythe and Schiffer structure references and the repository's Tau Ceti adaptation are accurately disclosed.

Original mathematical credit remains **Matthew J. Colbrook, Department of Applied Mathematics and Theoretical Physics, University of Cambridge**. Formalization credit is **George Stepaniants, Department of Computing and Mathematical Sciences, California Institute of Technology**. No George email is introduced. The original source's historical attribution is retained, and substantial AI assistance is disclosed.

## Remaining publication work

The final status API read, retained in `LATEST-STATUS.json`, still reports branch head `6e48f25f…` and **no associated pull request**. The canonical page remains **Solved**, and README/formalization metadata retain the historical candidate wording that complete-source and Linux checks were pending. This is conservative unfinished publication metadata, not a mathematical failure.

Before claiming upstream integration, the existing submitter should append the actual run and independent operational reviews, update current metadata and the canonical verification status, run repository publication/index checks, and open its own problem PR. A second independent operational audit and any final publication-commit checks belong to that publication process. This review did not alter the branch, post externally, create a PR or merge anything. It is sufficient evidence to hold our redundant 26-export draft and move our proof effort to another unresolved formalization; it is not a claim that IE-14 has already been merged or that the campaign count has been updated.
