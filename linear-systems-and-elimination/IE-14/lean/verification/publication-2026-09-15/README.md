# IE-14 publication transition

The complete original seven-target proof passed actual canonical run 35035525244
at 6e48f25fffdae2cf93e4985dc515abbd15e0481b. Its two original complete-source
referees, subsequent independent full source/runtime referee and separate runtime
audit are retained with authentic evidence. All previous mathematical, definition,
Challenge, numerical-dossier, Comparator and dependency bytes remain unchanged.

Current README and formalization metadata now report those observed gates. The
only build-configuration change selects Solution as the default target. The
unchanged frozen record still describes its original Challenge default, whose
exact file is preserved in before/lean/lakefile.toml. TRANSITION.json binds both.

The canonical source target remains intact; the new date records verification,
not a new literature search. Publication/merge commits and runs are separate
future checks. No staging, commit, push or local Lean/Lake execution is performed
by this publication preparation.
