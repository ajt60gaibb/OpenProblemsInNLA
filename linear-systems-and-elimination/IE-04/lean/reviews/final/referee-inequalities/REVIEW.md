# IE-04 independent canonical operational acceptance

**Verdict: ACCEPT the complete IE-04 candidate at commit `026b3e5534a4d6e15ebffb85318c2ff031df32bf`.** All 21 frozen targets passed actual isolated Comparator comparison, permitted-axiom checking and Lean default-kernel replay, with the required positive and negative controls in the same project job. The full previously reviewed Gaussian-tail counterexample is accepted. No mathematical or operational correction is requested. Acceptance of a later publication commit, upstream PR or merge is not asserted here.

Reviewer: `/root/next_inequalities`, independent non-implementing Codex AI agent, 15 September 2026. I contributed no IE-04 mathematical proof or definition. After completing the independent mathematical review, I prepared the canonical package's metadata and tooling transition; that packaging role is disclosed separately and was reviewed by `/root`. This report independently audits the runtime evidence and unchanged mathematical bytes, rather than claiming an independent review of my own packaging prose. I edited no held candidate during this audit and ran no local Lean, Lake or cache operation.

## Source and execution identity

Actual [run 35034399633](https://github.com/sgstepaniants/OpenProblemsInNLA/actions/runs/35034399633) used the standard repository Lean verification workflow. Project job `104600154206` succeeded on Ubuntu 24.04, UID 1001, using Lean 4.33.1 on x86_64 Linux. Artifact `10423196054` has ZIP SHA256 `8822abbaa7066d6845df1e6b7e6597e36ec0efed09287eb710fd67263066f69c`. The checkout, job metadata, artifact metadata, fetch identity and result receipt all identify the same commit and project `linear-systems-and-elimination/IE-04/lean`.

I independently compared the downloaded ZIP to its published digest and the raw uploader line, and every extracted member to the ZIP. Every one of the nine command outputs appears in the raw job transcript after removal of GitHub timestamps. All **184 tracked project inputs** match their actual Git blobs, the receipt and the held worktree. Full source copies, raw metadata, job logs, artifact files and the archive are retained in this review package. No tracked compiled artifact enters the snapshot.

`AUDIT.json` has SHA256 `33f8847c6c9780918a57b970240e5368943e081be5e8c8f60bc3b8ee3379fa91`; `INPUTS.json` has SHA256 `e79d12ac7e46dcf45c8d7a5aa81ac87aedef0d74ecc7751661075f9e18474a8a`. The independent audit script uses only local file reads, hashing and read-only Git commands. The coordinator's separate audit conclusion was not substituted for these checks.

All **23 unchanged inputs** from my complete source review match exactly, including every mathematical Lean file, Challenge, numerical targets, statement freeze, reuse records, Comparator configuration and dependency pins. The only changes among that review's 27 inputs are README, source-correspondence status prose, the truthful manifest, and the documented Lake transition to a declared/default Solution target. The historical versions and exact diff are retained. These packaging changes introduce no mathematical definition, assumption or theorem change.

My complete mathematical source review has SHA256 `cdd334884bbf0b2d653ef7be37f290d5f7df170ae6c1045b6dcb2eef1ebbb7b1`, with input manifest `a71e4cef672f460c2befe3bd74fba306c1acc886f8e7fec22762bb4401bb4bc8`. The actual full development acceptance addendum has SHA256 `4159a8c6a32277270ade99753cc7e1a3188c6e81cce06e67cf95e59f1c65889c`. Those reports are retained under `prior-review/`; the same already reviewed Robustness, GaussianBox and ProbabilityTail repairs are the bytes now canonically accepted.

## Actual checks

The Comparator transcript has SHA256 `a50f46417e0dad0d668d65b8f85c5b2532d79df9cebcd7da51a2eb438a9c08d0`. I read the full transcript. It builds both independent environments, exports all 21 requested declarations from Challenge and Solution, and records `Lean default kernel accepts the solution`, `Your solution is okay!`, and exit zero. All 14 files in the implementation graph, including Definitions and Solution, were built. Every Solution axiom report contains only `propext`, `Classical.choice` and `Quot.sound`, and every LeanCert kernel assertion passed. The fixed LeanCert inequality `1/8 < exp(-2)` also has an actual accepted standard-axiom report. Challenge's 21 deliberate placeholders remain isolated from the proof; there is no Solution placeholder or native/custom axiom. Comparator's replaceable-definition list is empty.

I read all mandatory control transcripts. The honest kernel replay succeeds; the raw proof of False and altered quotient constant are rejected. Comparator's honest match succeeds and the mismatch/custom-axiom cases fail at the expected phase, including explicit theorem-type mismatch. Separate `sorryAx` and Lean 4.33.1 native-decision fixtures are rejected. Sandbox probes establish private namespaces, denied AF_UNIX and host access, no effective capabilities, write access only to build `.lake`, read-only export storage, and rejection of all four unsupported option/writability cases. These controls all execute before the solution in the successful project job.

The infrastructure-only `checker-controls` job was skipped because shared tools were unchanged; the mandatory per-project controls above were run and passed. The exact committed harness snapshots tracked Git blobs into a fresh directory and checks unchanged input hashes after dependency/cache setup and after Comparator. Its acceptance receipt is written only after these gates. The same pinned Forsythe source lock `b3833b07916e5db77579b9cc53ca582282f6a841f36d6a60d693e5b02d342b6b` selects commit `8d1b0c0545a77b40245e84705aa7d273e6c81e62`. All ten actual dependency revisions match the manifest, including LeanCert `621a43d7cf21f87872392a01e874f2f1dbddc926` and Mathlib `0df444a360eaa60ab8c11dca51a86af692955474`. Tool, source and log hashes are retained in the audit.

## Full mathematical scope and next boundary

The unchanged accepted proof refutes the complete original uniform exponential tail. It preserves genuine independent Gaussian entries, the ordinary product sigma algebra, actual GEPP trajectories and active-entry growth, all admissible tie rules, a full nonsingular perturbation box, arbitrary positive real proposed constants, and the true Euclidean operator-norm bound on the smoothing center. The admissible first-available rule is explicitly constructed, so the final negation is not vacuous. A lower bound on the nonsingular tail event already violates the proposed upper bound; singular-input behavior and an unproved almost-sure assertion are not used. The identity center and noise scale one are legitimate witnesses to the original universal claim.

The fixed scalar certificate, exact error budgets, every-stage robustness induction, genuine product Gaussian mass and all-real exponent asymptotics remain the argument approved in my complete source review. The current formal result is the entire negative canonical resolution, not a finite example or restricted probability model.

George Stepaniants retains mathematical and formalization credit, Department of Computing and Mathematical Sciences, California Institute of Technology; the original conjecture's historical attribution is retained. No personal email is added. The reviews are independent AI-agent applications of scoped Tau Ceti standards, not official endorsement or external human peer review.

This candidate's canonical gates are complete. The coordinator may promote its publication metadata while preserving the accepted mathematical source. The resulting published commit still needs its normal verification run and source reconciliation; this review does not claim that future publication check or upstream action has already completed.
