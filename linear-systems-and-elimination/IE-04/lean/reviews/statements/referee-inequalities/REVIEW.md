# IE-04 independent statement referee

Reviewer: Codex agent `/root/next_inequalities`, separate from the IE-04 author.
Date: 15 September 2026. Scope: complete target fidelity, all numerical
statements, definitions, reusable GEPP foundations and metadata under the
repository's adapted Tau Ceti protocol.

**Verdict: APPROVE the current statement boundary.** No mathematical or scope
correction is required. This approval is bound to the exact files in
REVIEWED-SOURCE-HASHES.json. It does not assert successful elaboration or prove
any mathematics. A second independent approval and actual Linux declaration
check are still needed before the coordinator freezes the boundary and permits
proof implementation. The draft author does not count as its own referee.

## Original target and actual probability law

I read the complete canonical IE-04 README and solution at immutable upstream
8f04b905eb2e0827b6b84f37d9d080ae1f05b202, and checked their recorded Git-blob
bytes and SHA256. I also read all three cited generic IE-05 source files,
the entire new Definitions/Challenge/numerical-target files, source correspondence,
reuse record, Comparator configuration and formalization metadata.

UniformExponentialTail retains arbitrary positive REAL c₁,c₂, every n≥1,
every deterministic real center with its ACTUAL induced Euclidean operator
norm at most one, every 0<σ≤1 and every real x≥1. The same constants must
work throughout. The all-rule form includes every deterministic admissible
tie rule with measurable growth, and the Challenge explicitly proves the
first-available rule belongs to that class. It cannot become vacuous through
an empty rule class. Proving violation for this measurable subclass is already
sufficient against the original all-admissible-choice convention; the strong
box and probability targets apply to every member of that subclass.

Gaussian input is the genuine nested finite product of n² Mathlib
gaussianReal 0 1 measures. I inspected the pinned distribution definition:
its second parameter is variance, and at value one this is precisely the real
standard normal with density exp(-z²/2)/sqrt(2π). I inspected the finite-product
measure API as well. The probability-measure, measurable rectangle, exact
product and measurable actual tail-event assertions are separate obligations,
not assumed certificates or an unspecified random-variable law.

## GEPP and growth semantics

The scan, row swap, Schur update, arbitrary path and first-available path are
actual finite functions, matching the retained generic IE-05 implementation.
AdmissiblePivot requires an active nonzero pivot maximizing absolute column
magnitude; first-available explicitly resolves ties by current row index.
The previous IE-05 proofs supply a genuine reusable route from nonsingularity
to supported-active-block injectivity and valid first paths. The new draft
neither swaps columns nor substitutes a prescribed LU factor as its algorithm.

Exactly the n active stages k=0 through n−1 enter growth. The padded zeros
outside the current active block do not replace the active Schur complement.
Input and active maxima are actual finite NNReal suprema, with explicit maximum
semantics and positivity before division. Singular inputs have total arithmetic
only as a function convention. The event intersects with det A≠0, so no
singular-set nullity fact is silently needed. This smaller event's lower bound
already violates any compatible total-extension upper tail by measure
monotonicity. No equality of probabilities or unproved almost-sure no-tie fact
is used to identify different events.

## Exact universal quantitative statements

I independently checked the zero-based model: active final-column entries are
(3/2)^k, nonfinal pivots are one with competitors −1/2, and the final pivot is
(3/2)^(n−1). The box uses B=2^(n+2), δ=2^-(n²+n+1), for which
(n+2)(n−1)−(n²+n+1)=−3. Thus B^(n−1)δ=1/8 for every n≥2. No finite dimension
sampling replaces this identity.

The scalar quotient hypotheses imply pivot≥7/8, competitor magnitude≤5/8,
|a/p|≤5/7 and |a/p+1/2|≤2e. The complete Schur update bound follows from
1+5/7+2·2^n≤2+2^(n+1)≤2^(n+2), including its all-natural-n scalar statement.
Full-box robustness quantifies over every real perturbation in the box, proves
positive strict pivots and an actual no-swap path, equates every admissible path
to it, and proves det A≠0. It makes no false nonsingularity assertion about a
zero-padded full matrix after stage zero.

For growth, input maximum≤9/8 and final pivot≥c−1/8 yield
(8c−1)/9≥7c/9>c/2. The denominator positivity is explicit. The Gaussian box
centers W−I lie in [-1,1], and each interval lies in [-2,2]. The required density
lower bound 1/32 is valid: exp(1)<3 and π<4 give exp(-2)/sqrt(2π)>1/27>1/32.
An interval of length 2δ therefore has probability at least δ/16. Independence
gives (δ/16)^(n²)=2^-K, K=n²(n²+n+5), on the actual entire rectangle.

The final asymptotic target quantifies over every fixed positive real c₁,c₂
and constructs an n≥2 with x=(3/2)^(n−1)/(2n^c₁)≥1 and c₂x>K. Exponential
versus arbitrary fixed real power is an explicit proof obligation; no finite
list of constants or dimensions is substituted. Identity center has actual
spectral norm one and σ=1 is allowed. The resulting strict ENNReal inequality
contradicts the full universal proposition. The last export is its actual
negation, not only the deterministic growth example or probability lemma.

## Reuse, attribution and remaining gates

All 21 advertised targets appear in Comparator with no replaceable definition
holes and only the three standard permitted axioms. Their deliberate Challenge
placeholders are clearly isolated. The source's optional center-zero corollary,
coarser 2^-3n⁴ estimate and supplementary finite interval script are unnecessary
for this complete negative target and are not advertised as proved. No stronger
smoothed tail, expectation result or bit-level algorithm is claimed.

The generic IE-05 semantic source is properly attributed to the prior
AI-assisted implementation for George Stepaniants. Historical conjecture
attribution remains Spielman–Teng; solution/formalization name, department and
university match the user's authorization without email. LeanCert kernel trust,
actual Linux Comparator/default-kernel/axiom checks, rejection controls and two
independent final source/evidence referees remain mandatory after implementation.

No IE-04 source was modified. I did not run local Lean/Lake or restore any
compiled cache. This is an independent AI-agent statement review, not external
human peer review, formal verification, or official Tau Ceti endorsement.
