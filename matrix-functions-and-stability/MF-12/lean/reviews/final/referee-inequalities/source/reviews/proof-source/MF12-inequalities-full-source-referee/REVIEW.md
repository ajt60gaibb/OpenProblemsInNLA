# MF-12 independent complete mathematical source review

**Verdict: APPROVE the complete current mathematical source for canonical verification.** I found no mathematical error, hidden hypothesis or weakening of the original target. All 28 frozen contracts and the complete 19-file Lean implementation graph are covered. This is a source review, not canonical Comparator/default-kernel acceptance, external human peer review or a publication/count claim.

Reviewer: `/root/next_inequalities`, independent non-implementing Codex AI agent, 15 September 2026. I wrote or edited no MF-12 proof or definition and ran no local Lean, Lake or cache operation. I read every implementation file, the Definitions and Challenge, the complete retained canonical statement and Colbrook manuscript, and the numerical/source-correspondence dossier. My conclusion was formed independently of the coordinator's review.

## Exact scope and inputs

`INPUTS.json` seals all 35 current source/configuration files, the three immutable original-source files, the four retained statement-review records, the checked Mathlib sources and the development Git mapping. Its SHA256 is `1a605d5ae60e4730e190fb88ea3592e2766fb97089b5470a76e550d2ea5ed007`. Complete reviewed copies are retained under `source/` and `canonical/`.

All ten frozen files and all four recorded statement-review hashes match `STATEMENT-FREEZE.json`. Every one of the 28 public theorem signatures matches its independent Challenge signature after whitespace normalization. All 19 implementation/definition/entrypoint files are reachable from Solution through an acyclic local import graph; Challenge is not imported. No implementation hole, custom axiom, unsafe command or native decision shortcut appears in that graph. All 28 exports have an explicit LeanCert kernel-trust assertion in Solution.

The exact current Lean files, including Challenge, match Git commit `a1efcbfc59263b9e5bb00914ee709112348c1e54` under the shared development namespace. This source identity check does not by itself assert successful runtime execution. `CHECKS.json` has SHA256 `d9e0f371d3527442ef70e46a4a6858366800be0fd9acfa820f73ca108c160a5f`; the independent checking script is retained alongside it.

## Original mathematical target and definitions

The canonical problem asks for every real exponent gamma at least zero: a fixed positive dimension, finite nonempty real matrix family, positive fixed constants c at most C, two-sided growth bounds at every positive integer length, and joint spectral radius one. The final formal theorem supplies exactly two distinct matrices, a stronger cardinality statement, while retaining the complete requested existence assertion. Neither matrices nor constants depend on the word length.

`spectralNorm` is the actual norm of `Matrix.toEuclideanCLM` on real EuclideanSpace. I checked its pinned Mathlib definition and action on vectors. `wordNorms` includes every list of the required length drawn from the finite family, and `matrixProduct` reverses the chronological list so later factors act on the left. `familyGrowth` initially uses the real supremum, but `Words` proves the set is finite and nonempty, its supremum is attained by an actual admissible word, and every admissible word is bounded by it. The binary-word equality is proved in both directions without an injectivity assumption, even when the two nominal generators coincide. There is no unproved maximum or restricted word set hidden in the final target.

The final joint-spectral-radius assertion is a genuine `Tendsto` statement for the actual growth sequence raised to `1/n`. It is not a value assigned to a newly named spectral-radius function. The irrelevant value at n=0 does not enter the eventual squeeze.

## Complete proof assessment

**Norms, Tensor and Words.** The coordinate bound comes from applying the real Euclidean operator to a unit basis vector. The reverse norm bound applies Cauchy–Schwarz to every row and sums every coordinate. The actual finite entry maximum is proved attained in positive dimension. Kronecker entries are reindexed through the genuine product/Fin equivalence; entry maxima multiply because both factor maxima are attained. These give the stated two-sided dimension-factor norm comparison, followed by an exact induction for every binary word. No norm surrogate replaces the norm in the final result, and no unproved singular-value tensor formula is assumed.

**Parameters and MatrixAlgebra.** The six-dimensional source arrays are unchanged: lambda is 1/4, mu is lambda raised to `1-alpha`, and the reset is the actual product VU. The proof verifies UV=I, reset idempotence, and generator distinctness using the negative off-diagonal reset entry. The explicit sparse matrix-power induction retains q=0 and every natural q. The actual compressed power is exactly the required triangular matrix. Parameter bounds use 0<alpha<1 precisely where needed; the loss/gain real-power identity includes zero q legitimately.

**Budgets, HolderBound, Gaps, PowerBounds and Upper.** The later-gap weights agree with chronological multiplication. Their telescoping sum is proved exactly, including empty lists and zero gaps. Mathlib's finite real Hölder theorem is applied with conjugate exponents `1/alpha` and `1/(1-alpha)`; all bases are nonnegative and all endpoint exponent conditions are proved. This bounds the entire compressed product, not a selected switching pattern. Every binary word is decomposed into nonnegative gaps with exact length accounting. The no-reset case and one-reset case are included. Uniform bounds for every actual A-power entry, four rectangular multiplication estimates, and the genuine six-dimensional norm bound yield `1728 H² n^alpha` for every positive-length word. The enlarged constant is harmless and fully derived.

**GapBounds and Lower.** The natural logarithm and division arguments prove the necessary bounds for every n at least four, including the exact leftover length and `k*loss(q) >= 1/4`. The Bernoulli estimate is proved by induction with a positive denominator and gives lost mass at least 1/5. The prescribed chronological word has exactly length n and actual product `A^r (P A^q)^k`. Its compressed geometric-series entry is proved by matrix algebra, and the left padding preserves the relevant first coordinate. The sum of two genuine matrix entries is at most twice the Euclidean operator norm, yielding the constant `lambda^alpha/10` without an approximate square root. The first three positive lengths use A-powers and are explicitly bounded. This is an every-length lower bound, not a subsequence result.

**JordanEntries and JordanBounds.** Actual Jordan-power entries are proved by induction from matrix multiplication and Pascal's identity, with all row/column boundary cases. The lower estimate uses a symbolic descending-factorial comparison, not a numerical binomial assertion. The proof separately handles n<m and m=0. The upper estimate uses every entry and the true norm bound. The zero second generator is distinct, and the full finite-family maximum equals the norm of the Jordan power, since every word containing zero vanishes.

**Lifted, RootLimit and Realization.** The same Jordan factor accompanies both lifted generators, so every lifted word is exactly the corresponding base product tensored with J to the word length. A diagonal entry one in J proves the two lifted generators remain distinct. The fixed dimension factors alter only c and C; the exponent remains alpha+m. The constants' positivity and ordering are proved. The root limit follows by an actual squeeze between positive polynomial models, using the standard real-power limit and continuity; I checked the pinned Mathlib theorem used. Finally, the natural floor of every nonnegative real gamma splits the integer and noninteger cases, proves 0<alpha<1 in the latter, constructs the fixed family, and obtains the full root limit. The gamma=0 case is included.

## Independent checks and proof quality

The retained Python script performs only exact rational/integer diagnostics and source checks. It independently verifies UV=I and P²=P; 21 compressed power/loss-gain cases; 781 arbitrary gap lists including empty and zero gaps; all 255 binary words through length seven and their exact reset factorization; the logarithm/division/mass inequalities for every integer n from 4 through 10000; 64 actual lower-word matrix cases; 91 Jordan/binomial cases including m=0 and n<m; and 31 noncommuting tensor-word identities. These diagnostics passed. They supplement the full analytic and Lean-source review and are not claimed as universal proof certificates.

The implementation minimizes computation appropriately. Only the fixed 6-by-6 and 2-by-2 source identities are expanded; arbitrary words, lengths, logarithmic gaps, Jordan dimensions and tensor factors are handled symbolically. Exact Bernoulli bounds avoid unnecessary exponential intervals, and dimension-factor norm comparisons avoid a new SVD development. LeanCert is used in kernel mode for every public export. No artificial interval calculation or growing-matrix enumeration is required for the formal argument.

Original mathematical credit remains **Matthew J. Colbrook, Department of Applied Mathematics and Theoretical Physics, University of Cambridge**. Formalization credit remains **George Stepaniants, Department of Computing and Mathematical Sciences, California Institute of Technology**. Substantial AI assistance and Mathlib/LeanCert/Comparator reuse are disclosed, including the relation to the earlier MF-24 norm proof structure. No personal George email is added. This review applies scoped Tau Ceti requirements on fidelity, proof completeness, reuse and attribution without claiming an official endorsement.

The deliberately weaker constants, fixed lambda=1/4, and dimension-factor tensor bounds preserve the full canonical target. Optional dyadic/rational-entry results, density, particular irrational exponents, sharp constants and optimal dimensions are correctly excluded from the claimed exports. Historical statement-stage metadata must be updated truthfully during packaging; the frozen mathematical files remain unchanged.

## Remaining acceptance boundary

The complete mathematical source is approved with no requested correction. A final acceptance report still requires actual source-matched canonical Comparator/default-kernel/permitted-axiom/control evidence and two operational referee reconciliations. This report does not substitute a reported CI status, the exact finite diagnostics or the source-Git match for those remaining gates, and it does not increment the verified-problem count.
