# MF-24 independent root source review

Reviewer: `/root`, independent of all MF24 definition, statement and proof authors.

**Source-only verdict: no mathematical obstruction found in the complete 22-target proof route. Mechanical acceptance is withheld.** This is a review of the exact 23-file snapshot listed in `SNAPSHOT.json`, including the independent Challenge, Definitions, all 20 proof modules and the Solution wrapper. The two generic determinant modules use the post-diagnostic trust-assertion additions. The snapshot still includes the Polynomial and Heights tactic failures from development run 35021977948; those errors require actual repair and recompilation.

I read the full canonical README at upstream 8f04b905eb2e0827b6b84f37d9d080ae1f05b202 and the complete `references/mf24-counterexample/proof.tex`, all numerical obligations, all definitions, the 22 Challenge declarations, and every implementation module. The original mathematical attribution to Georg Maierhofer is preserved; George Stepaniants receives formalization credit.

The actual source word lengths are N−1 with N=(m+1)²=m(m+2)+1. The weave induction identifies the original edge lists with the two quotient/remainder height functions; the exact shift-power proof telescopes along one possible path for every exponent and coordinate, including zero exponent. Distinct positive stride multiples then give the actual polynomial and its full supported entry formula. None of these bridges is assumed as an input.

For spectral equality, `last_border_det` performs two symbolic Laplace expansions with only the last two possible entries retained. Its sign is the usual negative product of the two border entries, and the empty second minor at n=0 is the true determinant one. `GramEntries` derives the actual complex Gram pencil: its last diagonal is |z|²+η+w² and off-diagonal product is |z|²w². It also proves actual leading-corner compatibility. The resulting recurrence and prefix induction retain arbitrary complex shifts and η; reversing the list puts each new edge transfer matrix on the left. The fixed 2×2 compressed-commutator identity and Cayley–Hamilton span reduction require no invertibility. Applying it to both unchanged source words gives determinant equality for every η. Polynomial extensionality gives Gram characteristic-polynomial equality, and the explicit adjoint/Euclidean-linear-map bridge invokes the complete ordered singular values, including multiplicities and zeros. The code does not replace super-identical pseudospectra with a weaker definition.

For norms, the numerator has exactly m first-row entries t². Applying the adjoint to an actual Euclidean unit coordinate vector proves the lower bound in the induced operator norm; the denominator's (0,N−1) entry separately proves strict positivity of its norm. Every residue class retains all coordinates. Quotient injection bounds its size by m+1; congruence cancellation proves at most one height-zero and height-two vertex per class. The positive outer-product bound is applied to every complex vector, with finite Cauchy–Schwarz and a genuine partition sum. It yields (1+m/t²)(t⁴+mt²)=(t²+m)² and the actual induced operator norm upper bound. This is intentionally weaker than the source's t²+m−1 but still proves the complete canonical negative result.

At t=m≥2 the ratio is at least (2/3)√m. The existential natural m>(3(C+1))²+2 gives finite witnesses beyond every real C≥0; `no_uniform_comparison` contradicts the full universal comparison statement with these witnesses. No finite sample, informal SVD calculation, unbounded-set supremum convention, constant-size restriction, nonzero-eigenvalue premise or unproved padding claim is used. The related sharp constants and stronger source corollary remain outside the stated formalization.

All proof imports stay within the implementation graph plus pinned Mathlib/LeanCert; Solution imports no Challenge. LeanCert kernel assertions cover all 22 final exports. This source review does not establish that these assertions ran successfully: fresh full compilation, actual Linux Comparator/default-kernel replay and controls, source-byte reconciliation, truthful formalization.yaml and a second independent final referee are still required.
